# Umer OS Python Dependencies
# Install with: pip install -r requirements.txt
#
# Core (stdlib — no install needed):
#   asyncio, multiprocessing, ctypes, mmap, hashlib, logging
#   lzma, zlib, json, threading, os, sys, time, socket
#
# ── Quantum Simulation ──────────────────────────────────────────────────────
numpy>=1.26.0               # BSD         — numerical core for state-vector sim
# Optional quantum SDKs (uncomment when needed):
# qiskit>=1.0.0             # Apache 2.0  — IBM quantum framework
# qiskit-aer>=0.14.0        # Apache 2.0  — Aer high-performance simulator
# cirq>=1.3.0               # Apache 2.0  — Google quantum framework
# pennylane>=0.35.0         # Apache 2.0  — ML-focused quantum framework

# ── Security & Cryptography ──────────────────────────────────────────────────
cryptography>=42.0.0        # Apache/BSD  — AES-256-GCM, Ed25519, TLS primitives
# Optional post-quantum (uncomment when liboqs is available on the platform):
# liboqs-python>=0.9.0      # MIT         — CRYSTALS-Kyber, Dilithium

# ── AI / ML ──────────────────────────────────────────────────────────────────
# Optional — AI features degrade gracefully to stubs without these:
# onnxruntime>=1.17.0       # MIT         — on-device LSTM inference
# transformers>=4.40.0      # Apache 2.0  — Hugging Face model hub
# sentence-transformers>=2.7.0  # Apache 2.0 — semantic file search embeddings
# llama-cpp-python>=0.2.0   # MIT         — local LLM (Llama/Mistral/Phi)
# scikit-learn>=1.4.0       # BSD         — anomaly detection (AIFirewall)
# torch>=2.2.0              # BSD         — optional on-device training

# ── UI ───────────────────────────────────────────────────────────────────────
# Optional — GUI degrades to headless mode without Kivy:
# kivy>=2.3.0               # MIT         — cross-platform adaptive GUI
# Pillow>=10.2.0            # PIL         — image processing for icons

# ── Networking ───────────────────────────────────────────────────────────────
# Optional — network stack works without these (uses stdlib sockets):
# aiohttp>=3.9.0            # Apache 2.0  — async HTTP client/server
# dnspython>=2.6.0          # ISC         — DNS-over-HTTPS fallback
# zeroconf>=0.131.0         # LGPL        — mDNS cross-device discovery

# ── Build & Tooling ──────────────────────────────────────────────────────────
# Optional — for hot-path acceleration:
# cython>=3.0.0             # Apache 2.0  — Python→C compiler
# numba>=0.59.0             # BSD         — JIT compiler

# ── Testing ──────────────────────────────────────────────────────────────────
# Required for running the test suite:
# pytest>=8.0.0             # MIT         — test framework (optional; stdlib unittest works)
# pytest-asyncio>=0.23.0    # Apache 2.0  — async test support

# ── Documentation ────────────────────────────────────────────────────────────
# sphinx>=7.2.0             # BSD         — API docs generation
