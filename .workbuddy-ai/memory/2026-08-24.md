# 2026-08-24 — UmerOS remediation log

## Mount-path cap-gate cluster CLOSED (session 10) — H156 (media) + H166 (mnt)

Continued the **full YELLOW/BLUE sweep** at the highest-leverage, same-family chunk: the
un-gated privileged mount paths. Mirrored the established `core/capability_gate.py` pattern
(`gate.require(CAP_FS_ADMIN)`, fail-closed-when-wired / permissive-when-unwired).

### Edits (all carry `# [FIX Hxxx]` comments)
- **H166** `mnt/`:
  - `mnt/mount_ops.py` — `MountManager.mount`/`umount`/`remount` now require `CAP_FS_ADMIN`.
  - `mnt/mount_point.py` — `MountPointManager.create`/`remove` require `CAP_FS_ADMIN`.
  - `mnt/fstab.py` — `Fstab.write_file` (writes `/etc/fstab`) requires `CAP_FS_ADMIN`.
- **H156** `media/`:
  - Gated at the single chokepoint `media/mount_ops.py` `mount`/`unmount`/`remount`
    (`gate.require(CAP_FS_ADMIN)`). This transitively protects `auto_mount._handle_hotplug`
    and `udisks2.UDisks2Client.mount`, which both funnel through `mount_ops.mount`.
  - Added `# [FIX H156]` traceability comments at those two callers.

### Tests
- Extended `tests/test_cap_gate.py` with 8 new integration tests (deny-when-unprivileged +
  allow-when-held) covering all gated entry points + `mnt`/`media` callers.
- `tests/test_cap_gate.py` + `tests/test_media.py` = 133 passed.

### Verification
- `py_compile` clean on all 6 modified modules.
- Full `pytest tests/` = **1696 passed, 54 skipped, 0 failures, 0 errors (EXIT=0)** — 0 regressions
  (the +8 over the prior 1688 are exactly the 8 new mount-gate tests).

### Bookkeeping
- `remediation_progress.md`: flipped H156 + H166 boxes to `[x]`; added session-10 NEXT note.
- `MEMORY.md`: DONE bullet + folder map updated (H156/H166 FIXED).

## Next (loop continues)
Remaining in the **full YELLOW/BLUE sweep**: other un-gated privileged paths (H1–H307), the
broader H7 *folder* strays (H183/H200/H269/H278 + missing headers sbin/scripts/sdk/sources/
network/usr/security/media/etc/drivers/root/var/initrd), and baseline smells. The next natural
same-family cluster after mount paths is the `security/` and `kernel/` zero-trust gaps
(H17/H51 fail-open gates) — but any RED hotspot is fair game; resume at the first `[ ]` in
`remediation_progress.md`.
