# 2026-08-24 — UmerOS remediation log

## Mount-path cap-gate cluster CLOSED (session 10) — H156 (media) + H166 (mnt)

Continued the **full YELLOW/BLUE sweep** at the highest-leverage, same-family chunk: the
un-gated privileged mount paths. Mirrored the established `core/capability_gate.py` pattern
(`gate.require(CAP_FS_ADMIN)`, fail-closed-when-wired / permissive-when-unwired).

### Edits (all carry `# [FIX Hxxx]` comments)
- **H166** `mnt/`:
  - `mnt/mount_ops.py` — `MountManager.mount`/`umount`/`remount` now require `CAP_FS_ADMIN`.
  - `mnt/mount_point.py` — `MountPointManager.create`/`remove` require `CAP_FS_ADMIN`.
  - `mnt/fstab.py` — `Fstab.write_file` (writes `/etc/fstab`) requires `CAP_FS_ADMIN`.
- **H156** `media/`:
  - Gated at the single chokepoint `media/mount_ops.py` `mount`/`unmount`/`remount`
    (`gate.require(CAP_FS_ADMIN)`). This transitively protects `auto_mount._handle_hotplug`
    and `udisks2.UDisks2Client.mount`, which both funnel through `mount_ops.mount`.
  - Added `# [FIX H156]` traceability comments at those two callers.

### Tests
- Extended `tests/test_cap_gate.py` with 8 new integration tests (deny-when-unprivileged +
  allow-when-held) covering all gated entry points + `mnt`/`media` callers.
- `tests/test_cap_gate.py` + `tests/test_media.py` = 133 passed.

### Verification
- `py_compile` clean on all 6 modified modules.
- Full `pytest tests/` = **1696 passed, 54 skipped, 0 failures, 0 errors (EXIT=0)** — 0 regressions
  (the +8 over the prior 1688 are exactly the 8 new mount-gate tests).

### Bookkeeping
- `remediation_progress.md`: flipped H156 + H166 boxes to `[x]`; added session-10 NEXT note.
- `MEMORY.md`: DONE bullet + folder map updated (H156/H166 FIXED).

## Next (loop continues)
Remaining in the **full YELLOW/BLUE sweep**: other un-gated privileged paths (H1–H307), the
broader H7 *folder* strays (H183/H200/H269/H278 + missing headers sbin/scripts/sdk/sources/
network/usr/security/media/etc/drivers/root/var/initrd), and baseline smells. The next natural
same-family cluster after mount paths is the `security/` and `kernel/` zero-trust gaps
(H17/H51 fail-open gates) — but any RED hotspot is fair game; resume at the first `[ ]` in
`remediation_progress.md`.

## Fail-open zero-trust gate cluster CLOSED (session 11) — H17 (security) + H51 (compatibility)

Continued the loop at the next named RED cluster: **fail-open zero-trust gates** where a
privileged/trust check returned "allow" when it should deny.

### H17 — `security/security.py` `SecureBoot.verify_image` / `verify_bytes` (fail-open → fail-closed)
- Root cause: when an image had **no trust-store entry AND no caller-supplied `expected_hash`**
  (`stored is None`), the default `strict_mode=False` branch returned `True` — unknown
  components were silently allowed to boot/load.
- Fix:
  - Flipped the default to `strict_mode=True` (fail-closed by default).
  - In the `stored is None` branch, both strict and dev modes now **deny**: strict raises
    `PermissionError`; dev mode logs + returns `False` (never `True`).
  - Applied to both `verify_image` and `verify_bytes`. Caller-supplied `expected_hash` still
    verifies against that hash (not fail-open).

### H51 — `compatibility/container.py` `ZeroTrustContainer.execute_binary` (fail-open → fail-closed)
- Root cause: `self.capabilities.check(self.container_id, "HARDWARE")` result was only used to
  *print* a message; the binary then ran **unconditionally** and the "hardware restriction" was
  purely cosmetic. (`CapabilityManager.check` actually *raises* on denial, but the check never
  gated execution — fail-open.)
- Fix:
  - Added `import logging` + module logger `log`.
  - Execution is now DENIED unless the container holds the capability: switched to the
    non-raising `self.capabilities.query(container_id, "HARDWARE")`; on `False` it logs a denial
    and `return False` (graceful fail-closed) without running the binary. On `True` it proceeds
    and performs the hardware syscalls (capability granted).

### Tests
- `tests/test_security.py` (H17):
  - Replaced the 2 old fail-open tests (`test_verify_image_no_store_entry_permissive`,
    `test_verify_nonexistent_file_returns_true_permissive`) with fail-closed assertions.
  - Added `test_default_is_strict_mode`, `test_verify_image_unknown_component_denied`
    (strict→raises), `test_verify_image_unknown_component_denied_dev_mode` (dev→False).
- `tests/test_zero_trust_container.py` (H51, NEW): 5 tests — deny-without-cap, allow-with-cap,
  deny-unregistered-container (real `CapabilityManager`), windows-with-cap, windows-without-cap.

### Verification
- `py_compile` clean on `security/security.py`, `compatibility/container.py`, both test files.
- Targeted: `test_security.py` + `test_zero_trust_container.py` = 52 passed.
- Full `pytest tests/` = **1703 passed, 54 skipped, 0 failures, 0 errors (EXIT=0)** — 0 regressions
  (the +7 over the prior 1696 are the new H17/H51 tests).

### Bookkeeping
- `remediation_progress.md`: flipped H17 + H51 boxes to `[x]`.
- `MEMORY.md`: DONE bullet (fail-open cluster) + folder map notes (security H17, compatibility H51).

## Next (loop continues)
Remaining RED hotspots (any is fair game): `boot/` (H27/H28/H29 — kernel/EFI integrity fail-open,
auto-accept waiver), `bin/` (H37 login auth bypass), `cloud/` (H46 OTA signature fail-open),
`drivers/` (H64 static-secret auth), `home/` (H83 unsafe tar restore), `etc/` (H73), `installer/`
(H98/H99/H101), `kernel/` (H110/H112), `legal/` (H128/H130/H131/H135), `lib/` (H3/H147),
`proc/` (H205–H208), `network/` (H177/H178 SSRF/egress). Plus the ongoing H7 folder strays and
baseline smells. Resume at the first `[ ]` RED in `remediation_progress.md`.
