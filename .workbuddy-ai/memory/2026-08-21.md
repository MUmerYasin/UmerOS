# 2026-08-21

## legal/ folder study (v1.23 of the standard)
- User directed study of `legal/` (12 files, **92 KB**) — a **legal/compliance/consent Python package** (not just docs): `manager.py` (`LegalManager` master coordinator), `cli.py` (`legal_ctl` argparse CLI), `disclaimer.py` (`DisclaimerRegistry`/`RiskLevel` — clean), `consent.py` (`ConsentManager` + cryptographic consent ledger), `contributors.py` (`ContributorRegistry` + DCO), `donations.py` (`DonationsManager` — hardcoded creator IBANs/phones/emails + sponsor tiers), `licenses.py` (`LicenseManager` — multi-license scanner), `maintainers.py` (`MaintainerRegistry` — PQC/PGP fingerprints), `safety_check.py` (`SafetyChecker` — pre-exec backup), `test_legal.py` (8-test suite), `README.md`, `__init__.py`.
- **Injection-safe at Python level:** no `subprocess`/`shell=True`/`eval`/`exec`/`pickle`/`ctypes`/`os.system`/`yaml.load` anywhere (good). 10/12 `.py` have `from __future__` + `logging` + type hints + docstrings — **the strongest baseline discipline of any folder studied** (better than `dev/`'s GPLv3 model). But it is the **single largest Apache-2.0 cluster** (all 11 `.py` headers + `licenses.py` docstring say "Licence: Apache 2.0"; `README.md:40` defines the project license set as Apache-2.0/GPL-2.0/MIT with **no GPL-3.0**).
- **🔴 H128 (H7 lives here):** `legal/licenses.py` docstring (L8-13) + `get_license_text` default (L72-73) codify **Apache-2.0 as the primary/canonical license**, directly contradicting the adopted **H7 → GPL-3.0 canonical** decision. `_LICENSE_TEXTS` has **no GPL-3.0 entry** and `scan_directory` only maps `"GPL"` → `GPL-2.0` (never detects GPL-3.0). This folder is where H7 must be resolved decisively.
- **🔴 H129:** `scan_directory` is a **fail-open license compliance audit** — a file is "compliant" if it contains "Apache"/"GPL"/"MIT"/"Licence"/"License"/"Copyright" (L100-115); a file merely mentioning "Copyright" is classified "Custom/Proprietary" and counted compliant. So proprietary/unknown licenses pass; `is_fully_compliant` only fails on files with zero license-ish words. The audit cannot enforce the GPL-3.0 mandate.
- **🔴 H130:** `get_license_text(name)` **silently returns Apache-2.0 text for any unknown name, incl. "GPL-3.0"** (L72-73) — no GPL-3.0 entry exists, so callers requesting the canonical license text get the wrong license (correctness/integrity bug in attribution).
- **🔴 H131:** `consent.require_consent_interactive` **fails OPEN** — auto-grants in `dry_run` (L173-175) and, in any non-interactive (non-TTY) env, calls `grant_consent("I AGREE")` with no user input (L187-189). The mandatory "I AGREE" legal gate is bypassed in automation (same family as H29/H99; contrast `installer.py` which fails CLOSED).
- **🔴 H135:** `cli.py` `consent` subcommand **hardcodes `user_response="I AGREE"`** (L101-105) → `python -m legal.cli consent` auto-grants consent with no user action; `test_legal.py:229` encodes this as expected. The provided tool bypasses the gate.
- **🟡 H132:** the "cryptographic" consent token (L131-132) is `sha256` of *public* values with no key/HMAC/asymmetric sig — forgeable, and `load_ledger` (L77-89) doesn't verify integrity, so the audit ledger is tamperable (overstated-claim family, cf. H51/H111).
- **🟡 H133:** `consent.DEFAULT_LEDGER_PATH` is a hardcoded dev Windows path `F:/Pension Person Details/UmerOS/var/lib/umeros/consent_ledger.json` (L35) — breaks on other machines/OSes (cf. H43). **🟡 H134:** consent recorded under default "admin" with no identity check; PII (name/host/machine_id/timestamp) stored unencrypted in JSON ledger (§4.3).
- **🟡 H136:** `contributors.verify_dco` is decorative — `dco_signed` defaults `True` (L37/L89), `add_contributor` never verifies a real DCO/CLA signature; the "verification engine" returns a pre-set boolean (cf. H51/H111/H132). **🟡 H138:** `maintainers` PQC/PGP fingerprints are **hardcoded static literals** (L44-45), not derived from real keys — forgeable "cryptographic identity"; `cli.py` prints them as real "PQC Key"/"PGP Key".
- **🟡 H139:** `safety_check.verify_safety` defaults `is_safe=True` (L66) and only blocks on <500 MB free or a *CRITICAL*-level backup failure (L103-104); a failed backup on HIGH/MODERATE ops (disk partition, kernel module) yields only a warning while still "safe" — soft/fail-open safety gate (same family as H29/H99/H131).
- **🟡/🔴 H140:** `test_legal.py` Test 4 asserts `verify_dco("Antigravity AI / DeepMind Team")` is `True` (L120-121), but that name is **NOT in the roster** → `verify_dco` returns `False` and the test *fails as written*. No CI step exists to catch it (cf. H16). **🟡 H141:** tests encode the fail-open `consent` CLI as expected (L229) and don't cover `require_consent_interactive`'s non-TTY auto-grant, license-audit accuracy (H129/H130), or safety-blocking (H139).
- **💭 H137:** real personal PII/financial data hardcoded in source — `mumeryasin123456789@gmail.com` in `contributors.py:49` + `maintainers.py:42`; full IBANs/phones/emails/Wise/Payoneer in `donations.py:49-143`. Intentional (donation appeal) but sensitive; should be configurable/outside source control. **💭 H142:** `DonationTier` labels mismatch code thresholds (SILVER "$50+" vs `>=500`). **💭 H143:** `ContributorRole(str)` should be `Enum`; disclaimer text L86 "DEEPMIND AI ASSISTANTS" copy-paste artifact. **💭 H144:** `licenses.py` reads 2 KB + `errors="ignore"`; `sys.path` import hack; **0/12 `legal/` files carry a tier label**; `README.md:131` references `tests/test_legal.py` (not in `tests/`). **💭 H145:** relative imports rely on `sys.path.insert` instead of `from .consent import ...`.
- **Positives:** `disclaimer.py`/`manager.py`/`safety_check.py` are clean, well-structured models; `safety_check` actually creates `shutil.copy2`/`copytree` checkpoints (bounded, `Path.resolve()`); no injection surface anywhere; the `consent` ledger persists + revokes correctly on the happy path (only the gate's automation bypass is broken).
- Standard bumped to **v1.23**: header → +`legal/`; §4.4 added `legal/` study bullet; §9 added **H128–H145**; §11 quick-ref added `legal/` study line. Total hotspots now H1–H145.

## lib/ folder study (v1.24 of the standard)
- User directed study of `lib/` (33 source modules — 25 top-level `.py` + 8 in `lostfound/`) — a **faithful FHS `/lib` simulation**: shared-library / ELF / dynamic-linker / kernel-module / filesystem-layout modeling. Read in full: `security.py`, `ssl_libs.py`, `kernel_modules.py` (30 KB), `dynamic_linker.py` (24 KB), `fhs.py`, `var_lib.py`, `lostfound/fsck.py`, `lostfound/__init__.py`, `lib/__init__.py`, `lib/README.md`; grepped all 33 for dangerous calls (`subprocess`/`eval`/`exec`/`pickle`/`ctypes`/`shell=True`/`marshal`/`shelve`/`yaml.load`) = **none**; for fail-open/verify patterns = only `ssl_libs.py`.
- **Best-disciplined folder in the entire study.** Every module has `from __future__ import annotations` + `logging.getLogger` + type hints + Google docstrings + a `_selftest()`; consistent enums/dataclasses; 21 modules documented in `lib/README.md` TLDP coverage map. `dynamic_linker.py` faithfully models the glibc `ld.so.cache` binary format (magic + header + entry structs) and parses it with correct bounds (string-table offset) — safe. `kernel_modules.py` is a clean `depmod`/`modprobe`/`rmmod` simulation with acyclic dependency resolution + safe MD5 integrity fingerprinting. `lostfound/fsck.py` is a **model fail-closed** checker: marks the FS clean only when `superblock_ok and unresolved_orphans == 0 and errors_found == errors_fixed`; corrupted inodes are flagged unrecoverable (not silently passed). `fhs.py` audit is conservative (`ok` only when no ERROR issues). `var_lib.py` is pure metadata (documents `/var/lib` state — no secret handling).
- **🔴 H3 re-confirmed:** `lib/security.py:72` `PASSWORD = "password"` is a hardcoded module-level constant in the PAM catalogue. Latent — it is **not referenced in any active auth path** (catalog metadata only), so blast radius is low, but it is a hardcoded-weak-credential smell and must be removed/replaced (matches the known H3).
- **🔴 H146 (NEW, trust/verify fail-open):** `lib/ssl_libs.py` — `_check_is_trusted` (L414-427) returns `True` whenever ANY `ca-certificates.crt` file exists on disk ("If store exists, assume basic trust", L424); `check_trust` (L225-245) treats any `is_ca` cert as trusted and only string-matches `issuer==subject` — **no cryptographic signature validation**. So any cert in a store with a CA bundle file is reported "trusted". Same family as H17/H111/H128/H129. `_check_is_trusted` also returns `True` when `store_paths` has a dir with a bundle file, regardless of the cert — pure presence-based trust.
- **🔴 H147 (NEW, expiry fail-open):** `ssl_libs.py` `CertInfo.is_expired` (L83-87) unconditionally returns `False` ("simplified: returns False if parsing fails"), and `days_until_expiry` (L92) returns a hardcoded `365`. So expired certificates never appear expired — expiry enforcement is decorative (same family as H111 dummy-crypto). `not_after`/`not_before` are never even parsed from the cert.
- **💭 H148 (NEW):** `ssl_libs.py` whole module is a "simplified" string-parsed simulation — no `cryptography`/OpenSSL use; `is_ca` heuristic is loose ("CA:TRUE" substring in text); `key_size`/`signature_algorithm` are hardcoded defaults (2048 / SHA256withRSA); fingerprints via `hashlib` are correct but trust + expiry are not enforced. It ships as if production-ready but should carry an `[EXPERIMENTAL]`/`[FUTURE]` tier label.
- **🟡 H149 (NEW, H7 family):** `lib/` is the **largest Apache-2.0 cluster** — ~23 `lib/` files carry `Licence: Apache 2.0` headers (`security.py`/`ssl_libs.py`/`kernel_modules.py`/`dynamic_linker.py`/`arch.py`/`firmware.py`/`__main__.py`/`kbd.py`/`__init__.py`/`essential_libs.py`/`iptables_libs.py`/`ldd.py`/`iconv.py`/`libinfo.py`/`library_manager.py`/`var_lib.py`/`oss.py`/`usr_include.py`/`multiarch.py`/`tmpfiles.py`/`usr_lib.py`/`elf_parser.py`/`lostfound/__init__.py`) and `lib/README.md:138` asserts "Apache 2.0 - same as the rest of Umer OS" — directly contradicts the adopted **H7 → GPL-3.0 canonical** decision. This is the single biggest concentration of Apache strays in the repo (joins `ai/`/`boot/`/`etc/`/`feedback/`/`fs/`/`initrd/`/`legal/`).
- **💭 H150 (NEW):** `lib/security.py` registers `pam_permit.so` ("Always permit") as a stock PAM module (L116-118) — realistic FHS, but if the `security/` subsystem ever consults this registry for auth decisions a default could fail-open (metadata only, low risk; worth a note that `pam_permit` must never be auto-selected).
- **Positives / models to copy:** clean binary `ld.so.cache` parser (`dynamic_linker.py`), fail-closed `fsck` (`lostfound/fsck.py`), conservative FHS audit (`fhs.py`), full per-file baseline across 33 modules, zero dangerous calls, zero injection surface. This folder is the baseline-discipline exemplar for the rest of the repo.
- **Fix order (lib/):** fail-closed `_check_is_trusted` (verify signatures vs an actual trust store) + real expiry (`not_after` vs now) → resolve H7 (relicense ~23 modules to GPL-3.0) → remove H3 constant + add H150 caveat.
- Standard bumped to **v1.24**: header → +`lib/`; §4.4 added `lib/` study bullet; §9 added **H146–H150**; §11 quick-ref added `lib/` study line. Total hotspots now H1–H150.

## liboqs/ folder study (v1.25 of the standard)
- User directed study of `liboqs/` — discovered to be the **upstream Open Quantum Safe (OQS) C library**, vendored into UmerOS as a **plain (unpinned) directory** (no `.gitmodules` at repo root). Mapped structure, grepped all 28 Python files for dangerous calls + fail-open/crypto-verify patterns, and cross-checked the live UmerOS PQC paths (quantum/, kernel/, cloud/ota) that *actually* run in the Python runtime.
- **Scope:** 5,665 `.c`/`.h` files under `src/` — real, externally-audited PQC (constant-time, FIPS-203/204/205 aligned). KEM families: `ml_kem` (ML-KEM/Kyber FIPS-203), `kyber`, `bike`, `classic_mceliece`, `frodokem`, `hqc`, `ntru`, `ntruprime`. SIG families: `ml_dsa` (ML-DSA/Dilithium FIPS-204), `slh_dsa` (SPHINCS+ FIPS-205), `falcon`, `cross`, `mayo`, `mqom`, `snova`, `uov`. This **directly supplies the §4.2 zero-trust PQC mandate** (SHA3-512, Kyber/Dilithium, AES-256-GCM) — but only if UmerOS binds to it.
- **Python surface (reviewable under our baseline):** ~28 `.py` files = upstream `scripts/` (docs/CBOM/upstream-copy tooling) + `tests/` (KAT/Wycheproof/ACVP/valgrind/constant-time harnesses). **Third-party upstream files, not UmerOS-authored** → Python baseline (GPL header/docstrings) doesn't apply, but they sit in the vendored tree. Dangerous-call scan: `subprocess.run` in `scripts/copy_from_upstream/*.py` + `tests/helpers.py` — **all list-args, zero `shell=True`** (injection-safe, same conclusion as `lib/`/bin). **No `eval`/`exec`/`pickle`/`ctypes`/`marshal`/`shelve`/`yaml.load`/`__import__`** anywhere. `return True` hits benign: `scripts/update_cbom.py:208` = "CBOM changed → needs-update" flag; `tests/helpers.py:78/111/144` = `is_kem_enabled_by_name`/`is_sig_enabled_by_name`/`is_sig_stfl_enabled_by_name` feature-detection (build-config probes); `tests/test_wycheproof_vectors.py` uses real Wycheproof known-answer vectors ("dummy" = argv padding only).
- **🟡 H151 (NEW, H7 family):** `liboqs/` vendored **unpinned** (no submodule → can silently drift; no provenance record) and **MIT-licensed** (`LICENSE.txt`: "The MIT license … applies to liboqs in general") + per-algorithm submodule licenses (BIKE CC0, Classic McEliece public-domain, ML-KEM reference Apache-2.0). MIT→GPL-3.0 is one-way compatible, but UmerOS must preserve the MIT copyright/NOTICE, never apply GPL headers to upstream files, and pin a commit/submodule. Largest non-GPL vendored component (broader than `lib/` Apache-2.0 cluster H149).
- **🔴 H152 (NEW, fail-open under PQC mandate):** `quantum/crypto_pqc.py:36-46` — `[TODAY]`-tier module that `try: import oqs … except ImportError: _LIBOQS_AVAILABLE=False` then selects `_FallbackBackend` (Ed25519 + AES-256-GCM) with only a `log.warning`. Under §4.2 the system advertises "post-quantum" but silently runs **classical** crypto with no error/refusal. Fail-open family (H111/H146/H147). Also `crypto_pqc.py:22` is `Licence: Apache 2.0` (H7 stray).
- **🟡 H153 (NEW):** Two **divergent** PQC backends — `kernel/pqcrypto_.py` is a commented *example* using `pqcrypto` PyPI (`from pqcrypto.kem import kyber1024`, pure-Python reference wrapper, not constant-time hardened) while `quantum/crypto_pqc.py` uses `liboqs-python` (CRYSTALS-Kyber768/Dilithium3). Misleading example risk + two different threat models/vetting.
- **🔴 H154 (NEW, reinforces H46/H111):** `cloud/ota_updater/update_system.py:33` hardcodes `simulated_manifest["signature"] = "simulated_dilithium_sig_abc123"` inside a function whose docstring says "Verify cryptographic signature". The OTA path (already H46 fail-open `verify_and_apply`) ships a **fake PQC signature literal** as the value to verify. Concrete decorative-crypto fail-open.
- **💭 H155 (NEW):** Vendored `liboqs/` C library is **not wired into the Python runtime** — live PQC uses external pip packages (`liboqs-python`, `pqcrypto`), not this source tree. Unpinned, unused crypto copy = dead weight + provenance/audit liability; OR if meant as build source, there is no reviewed capability-gated Python binding.
- **Re-confirmed:** `kernel/umer_kernel.py:434` `verify(self, data, sig): return True # Dummy` (H111) still present; `ipc_bus.py:87` has a real HMAC verify (good). `kernel/pqcrypto_.py` runs Kyber keygen at import (noted in v1.22 kernel study).
- **Fix order (liboqs/):** hard-fail when PQC backend missing (remove silent classical fallback) → resolve H7 on `crypto_pqc.py` (GPL-3.0) → unify on `liboqs` as single canonical PQC backend (route `kernel/` + OTA through it; delete/mark `pqcrypto_.py` non-prod) → replace OTA fake sig with real verification → pin `liboqs/` as a submodule or drop the vendored copy.
- Standard bumped to **v1.25**: header → +`liboqs/`; §4.4 added `liboqs/` study bullet; §9 added **H151–H155**; §11 quick-ref added `liboqs/` study line. Total hotspots now H1–H155. Remediation pass re-offered (9th); `liboqs/` adds 2 new 🔴 (H152 silent PQC fallback + H154 OTA fake sig) to the fail-open family.


## opt/ folder study (v1.29 of the standard)
- User directed study of `opt/` (14 modules, ~3.6K LOC) — an FHS `/opt` add-on-software package manager (manages `/opt`, `/etc/opt`, `/var/opt` + `/etc/profile.d` + `~/.bashrc`). Read in full: `__init__.py`, `config.py`, `env.py`, `fhs.py`, `hierarchy.py`, `var.py`, `manager.py`, `package.py`, `integration.py`, `installers/{sample_app,web_server,database,development}.py`, `test_opt.py`, `README.md`.

- **Better baseline discipline than media/mnt/network.** 4 modules — `env`/`fhs`/`hierarchy`/`var` — carry `from __future__ import annotations` + `logging.getLogger` + type hints + Google docstrings + **functional `_selftest()`**. `fhs.OptFHSValidator` is a **model read-only auditor** (reserved-dir checks, package-layout checks, world-writable detection, fail-cautious). `hierarchy.PackageEntry.from_dict` does key-filtering (`{k:v for k,v in data.items() if k in cls.__dataclass_fields__}`) — safe dataclass hydration. `manager.py` hardcodes `/opt/.packages.json` as its database (cf. H43 hardcoded-path smell).

- **But the privileged install/remove/write/rmtree surface is zero-trust-gapped with concrete bugs (4 new 🔴):**
  - **🔴 H184:** No `CapabilityManager` gate on ANY privileged `/opt` op — `OptManager.install/remove/update`, `OptPackage.install/remove`, `OptConfig.install_config/remove_config`, `OptIntegration.install_package/remove_package`, `OptHierarchy.bootstrap/register`, `OptEnvManager.write_profile_d`, `VarOptManager.write_file/remove_package_dir` all mutate `/opt`/`/etc/opt`/`/var/opt`/`/etc/profile.d`/`/var/lib` with no capability check (same family as H27/H110/H156/H166/H177). Installs code that becomes a `$PATH` entry → high blast radius.
  - **🔴 H185:** Path traversal via unvalidated `filename`/`config_file`/`package_name` in file writes — `VarOptManager.write_file`/`read_file` build `pkg_dir / filename` (filename unvalidated) → arbitrary file write/read outside `/var/opt` (e.g. `filename="../../etc/passwd"`); `OptConfig.install_config`/`get_config` build `etc_opt_root / package_name / config_file` (config_file unvalidated) → write/read outside `/etc/opt`.
  - **🔴 H186:** Path traversal via unvalidated `name`/`provider` in `shutil.rmtree` — `rmtree(self.opt_root / provider / name)` etc. with no validation → `name="../../etc"` deletes an arbitrary directory outside `/opt`. (Also in `package.py` `remove_package`/`OptPackage.remove`, `config.py:remove_config`, `var.py:remove_package_dir`.)
  - **🔴 H187:** Command injection in generated launcher/wrapper scripts — `create_launcher_script` writes `exec {command} {' '.join(args)} "$@"` and `create_wrapper_script` writes `export {key}="{value}"` into a `#!/bin/bash` script with no shell-escaping → crafted `command`/`args`/`environment` yields arbitrary code execution when the script runs (it lands in `/opt/<pkg>/bin`, which `env.write_profile_d` adds to `$PATH`).

- **🟡 H188:** Shell-injection in generated PATH/profile snippets — discovered `bin_path` (a filesystem path) is interpolated unescaped into `export PATH="<path>:$PATH"` written to `/etc/profile.d/opt-paths.sh` and appended to `~/.bashrc`; a package dir name containing `"` breaks the quote → arbitrary commands in a login-sourced file (runs for all users). (env.py:251 `write_profile_d`, integration.py:222 `generate_profile_script`.)
- **🟡 H189:** `OptPackage._setup_paths` hardcodes real `/etc/opt`/`/var/opt` (Path literals), ignoring the `opt_root` override passed to the constructor (unlike `manager`/`integration` which honor overrides) → real side effects even in temp-root demo/test runs.
- **🟡 H193:** `OptServiceManager.start_service` executes discovered scripts without validation — `discover_services` picks any `.sh`/extensionless file in bin/sbin/scripts/services containing start/stop/restart/status/daemon keywords, then `start_service` does `chmod(0o755)` + `subprocess.run([script,"start"])`; any file dropped into a package bin dir gets executed (code-exec if an attacker can place one, e.g. via H186 or a malicious package).

- **💭 H190:** `verify_integrity` mislabeled — only checks subdirectory existence, yet `OptManager.verify` surfaces it as "integrity"; no hashing/signing → false assurance of package integrity (no tamper detection) vs the §4.2 zero-trust mandate.
- **💭 H191:** Missing `from __future__ import annotations` in 10 modules (only `env`/`fhs`/`hierarchy`/`var` have it) — off-baseline vs the 4 disciplined modules.
- **💭 H192:** No `_selftest()` in 10 modules; `test_opt.py` is a separate print-runner that never calls the per-module `_selftest()`s and covers happy-path only (zero security tests: no traversal/injection/cap-gate cases).
- **🟡 H183:** H7 license strays — `env`/`fhs`/`hierarchy`/`var` say `Licence: Apache 2.0` (British spelling, like mnt H176); `config`/`manager`/`package`/`integration`/`installers/*`/`test_opt` have NO header. Fix = canonical GPL-3.0 headers + `Licence`→`License`.

- **Positives / models to copy:** `fhs.OptFHSValidator` (read-only auditor), `hierarchy` key-filtered `from_dict`, the 4 fully-baseline modules with functional `_selftest()`s, and argv-list `subprocess` (no `shell=True`) in `integration.py` service exec. The one disciplined read-only auditor is the pattern to lift repo-wide.

- **Fix order (opt/):** (1) capability-gate every privileged op (H184) → (2) validate `name`/`provider`/`filename`/`config_file` (`..` rejection + `realpath` under root) for the write/rmtree/remove surface (H185/H186) → (3) `shlex.quote` every interpolated value in launcher/wrapper/profile/bashrc snippets (H187/H188) → (4) derive `etc`/`var` roots from `opt_root` (H189) → (5) require an explicit service manifest/signature before exec (H193) → (6) real integrity hashing + rename mislabel (H190) → (7) add `_selftest()` + traversal/injection/cap-gate regression tests (H192) → (8) resolve H7 license headers (H183).

- Standard bumped to **v1.29**: header → +`opt/`; §4.4 added `opt/` study bullet; §9 added **H183–H193**; §11 quick-ref added `opt/` study line. Total hotspots now H1–H193. Remediation pass re-offered (13th); `opt/` adds **4 new 🔴** (H184 no cap gate on privileged ops, H185 path-traversal file read/write, H186 rmtree path-traversal deletion, H187 command injection in generated launcher/wrapper scripts) to the fail-open/cap-gate family.


## packages/ folder study (v1.30 of the standard)
- User directed study of `packages/` (3 modules, 635 LOC) — the **user-space package manager** (`umer_pkg.py` `UmerPackageManager`/`PackageManifest`/`DependencyResolver`, `repository.py` `PackageRepository`/`PackageInfo`, `__init__`). Read `__init__.py`, `repository.py`, `umer_pkg.py` in full.

- **Best baseline of the small folders.** `umer_pkg.py` carries `from __future__` + `logging` + type hints + Google docstrings; its `DependencyResolver` is a **correct topological sort with cycle detection**, and `install` does **atomic snapshot+rollback** (copytree backup → extract → rmtree backup on failure). `_verify_hash` fails **CLOSED** on mismatch (refuses install), and `hashlib.sha3_256` is real (not a dummy). `repository.py` is a clean in-memory catalog with **no privileged ops / no dangerous calls**. Injection-safe throughout (no `subprocess`/`eval`/`exec`/`shell=True`/`pickle`/`ctypes`).

- **But the install/extract surface has 5 new 🔴 (concrete, high-severity):**
  - **🔴 H194:** Tar-slip on `tarfile.extractall` (`umer_pkg.py:357,363`) — members are filtered only by a naive string-prefix `m.name.startswith("files/")`, which does NOT stop `files/../../etc/x`; combined with `extractall` called **without `filter=`** (legacy fully-trusted behavior in Python 3.12+: no path sanitization, no perm/owner stripping) → absolute paths / `../` / symlink & hardlink members extract **anywhere** on disk. A malicious `.umerpkg` writes arbitrary files (`~/.bashrc`, cron, SSH `authorized_keys`) outside the package dir (CVE-2007-4559 family; cf. H83/H93).
  - **🔴 H195:** Untrusted manifest `name`/`version` → attacker-controlled paths (`umer_pkg.py:347,510`) — `dest = install_dir / manifest.name` and `pkg_filename = f"{pm.name}-{pm.version}.umerpkg"` use values straight from the archive manifest; a manifest `name: "../../../../.config/evil"` creates/writes outside `~/.umer/packages`.
  - **🔴 H196:** "Signed .umerpkg archives" overstated + fail-open — only a SHA3-256 **self-hash** of `manifest.json` exists (no public-key/signature anywhere), and `_verify_hash` returns `True` when the `HASH` file is absent ("dev mode" → skip). So unsigned packages pass; integrity is self-referential, not authenticated (H51/H111/H146/H154 family).
  - **🔴 H197:** Integrity check ignores the `files/` payload — `_verify_hash` hashes only `manifest.json` bytes, contradicting the docstring ("SHA3-256 of manifest.json + files/ tree"); the actual payload is never hashed/verified, so a tampered payload is undetectable even when a HASH is present.
  - **🔴 H198:** No `CapabilityManager` gate on `install`/`remove`/`update` (`umer_pkg.py:285,390,414`) — the docstring claims "system-wide requires admin grant" but there is no capability/admin check; they `rmtree`/`copytree`/`extractall` the user filesystem unchecked (H27/H110/H156/H166/H177/H184 family). Scope is user-space `~/.umer/...` (lower blast radius than `opt/`), but the zero-trust gate is still absent.

- **🟡 H199:** Lexicographic version compare in `update` (`umer_pkg.py:435` `if manifest.version <= current:`) → `"1.10.0" <= "1.9.0"` is `True` (wrong) → valid upgrades silently skipped / `remove`+`reinstall` mis-fires. (correctness)
- **🟡 H200:** H7 license strays — `umer_pkg.py:20` docstring `Licence: Apache 2.0` (British spelling, like mnt H176 / opt H183); `repository.py`/`__init__.py` have NO header.
- **🟡 H201:** `_find_in_registry` fuzzy `fname.startswith(name)` match (`umer_pkg.py:323,326`) → `umer-core` also matches `umer-core-evil.umerpkg` (wrong package fetched).
- **🟡 H204:** Symlink/hardlink extraction + no permission `filter` (`umer_pkg.py:357,516`) — without `filter=` a `files/link` → `/etc/shadow` symlink is materialized in `dest` and dereferenced on later reads → traversal; member modes (incl. setuid) applied verbatim; `build` archives symlinks as-is.
- **💭 H202:** Missing `from __future__ import annotations` in `repository.py`/`__init__.py` (umer_pkg.py has it). **💭 H203:** No `_selftest()` in any module; `repository.py:33` uses `print()` instead of `log.info`.

- **Models to copy:** `DependencyResolver` (topo sort + cycle detection), atomic snapshot+rollback on install, fail-CLOSED hash check, real `sha3_256`. The cleanest small folder studied — but the install path repeats the tar-slip + fail-open-integrity mistakes seen in `home/`/`initrd`.

- **Fix order (packages/):** (1) `tarfile.extractall(..., filter="data")` + reject members whose `realpath` escapes `dest` (H194/H204) → (2) validate manifest `name`/`version` charset `^[a-zA-Z0-9._+-]+$` (H195) → (3) verify against a pinned public-key signature, fail CLOSED, and hash the `files/` tree (H196/H197) → (4) capability-gate `install`/`remove`/`update` (H198) → (5) semantic version compare (H199) → (6) exact registry match (H201) → (7) GPL headers + `from __future__` + `_selftest()` (H200/H202/H203).

- Standard bumped to **v1.30**: header → +`packages/`; §4.4 added `packages/` study bullet; §9 added **H194–H204**; §11 quick-ref added `packages/` study line. Total hotspots now H1–H204. Remediation pass re-offered (14th); `packages/` adds **5 new 🔴** (H194 tar-slip, H195 manifest path traversal, H196 fail-open "Signed", H197 payload-not-verified, H198 no cap gate on install/remove/update) to the fail-open/cap-gate family.
