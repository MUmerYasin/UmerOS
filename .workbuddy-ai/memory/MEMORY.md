# UmerOS — Long-term Project Memory

## Standing conventions (CodeReviewExpert / Kim)
- Review-standard-first: after every study, bump `MainTask/Raw Data/Code Review Standards and Process.md` (§1/§4/§7/§9/§11).
- Authoritative sources: `Skills/Context File/umer_os_skills.json` > `MainTask/prompt/*.md` > `MainTask/Raw Data/*.docx` > repo + LICENSE/setup.py.

## Decided
- H7 → GPL-3.0 canonical — **RESOLVED (session 39):** the "Apache-2.0 strays" premise was stale; doc-scope files already declared GPL-3.0, only `Licence`→`License` + `Version 3`→`v3` normalized to match README. Code-file `Licence` sweeps tracked under H20/H30/H50/H95/H104/H160/H176/H183/H200/H215/H223/H232/H236/H240/H254/H260/H275/H291/H299/H306.
- H11 → Flutter (Dart) frontend canonical; backend = Python only; `ui/*.py` retired (H25); UI follows HCI Nielsen-10 (§4.8). **RESOLVED (session 40):** Kivy drift reconciled across `setup.py` (extras legacy note), `kernel/requirements.txt` (kivy commented), `kernel/gui.py` (retired banner), `kernel/umer_kernel.start_gui_shell` (docstring/comment reconciled), and 7 README Kivy refs; H22/H25/H122/H125 closed.
- H13 → `.umerpkg` security/package signing **RESOLVED (session 41):** was overstated crypto (docstring claimed "Signed" but code only computed a SHA3-256 *integrity* hash — no signature, no trusted key; overstated-crypto family H132/H138/H154). Now real Ed25519 signature over the integrity hash + pinned chain-of-trust anchor in `packages/trusted_keys.py` (canonical `umer-release` public key embedded; matching private key OFFLINE). `build(signing_key=, key_id=)` signs; `install`/`_verify_package` fail-closed refuse unsigned/untrusted; 4 regression tests added to `tests/test_packages.py`.
- H14 → `README.md` layout drift **RESOLVED (session 42):** verified premise was PARTIALLY stale — `kernel/scheduler.py`/`kernel/hal.py`/`quantum/simulator.py` exist, but `kernel/ipc.py`/`security/crypto.py`/`fs/quantum_fs.py`/`ui/shell.py` are missing; example APIs illustrative (real classes `HybridScheduler`/`StatevectorSimulator`, not `Scheduler`/`QuantumCircuitSimulator`); stats false (~3,500 LOC/12 modules vs ~825 modules/~270k LOC). Added "📌 Documentation status" disclaimer, 🚧 planned markers on missing tree modules, illustrative notes on Component Details/Code Examples, corrected stats, and CI doc-drift check `scripts/check_readme_drift.py` wired into `ci.yml` (fails on phantom-module imports).
- H15 → `requirements.txt` hygiene **RESOLVED (session 43):** drift-recon showed premise partly stale — legacy linters (black/flake8/pylint) already GONE from reqs and README Contributing already ruff+mypy (H9); kivy already commented in reqs (only `kernel/gui.py` imports it — H22/H25). Real fixes: `g4f>=0.4.0` REMOVED (grep = 0 import sites → pure supply-chain/ToS risk); the 3 strict `>` floats bounded `<1.0` (`httpx`/`fastapi`/`uvicorn[standard]`); security/transport caps `cryptography<46`, `python-jose<4`, `aiohttp<4`; added top-of-file reproducibility policy + `requirements.lock` recommendation (`pip-compile --generate-hashes`). `setup.py` still hard-ships `kivy>=2.3.0` (H22). 42 active specs all valid PEP 440.
- H16 → `tests/` harness framework split **RESOLVED (session 44):** drift-recon showed premise substantially stale — `pyproject.toml` already has `[tool.pytest.ini_options]`; `ci.yml` Job 1 already runs blocking `python -m pytest tests/` with `--cov-fail-under=30` and installs `requirements.txt` (heavy deps present in CI); `tests/quantum/` exists (own conftest, pytest); venv pytest 9.1.1 → the "bundled pytest imports `imp`" breakage is gone via the `pytest>=8` pin. Only real defect: 3 legacy `run_*.py` subset runners (lib/root/initrd) gave a false "green" signal → converted all 3 to DEPRECATED shims delegating to canonical `pytest tests/` (single framework, single source of truth). Discoverability verified: `pytest tests/ --co` → **2178 tests** collected cleanly (pytest collects both unittest + pytest styles). ~8 historical suite failures enforced in CI, not a harness defect.

- H19 → `ai/` consolidated AI stacks **RESOLVED (session 45):** drift-recon proved premise substantially stale. `assistant.py` is now a `LocalAIAssistant` shim alias (GPL header + H19 docstring, no print); `self_healing.py` already met the per-file baseline (logging/type hints/docstrings/[H12 GATE] tier, never executes generated code). `resource_predictor.py:ResourcePredictor` had ZERO importers and duplicated canonical `AIResourceManager` (SPIKE_Z_THRESHOLD 2.0 vs 2.5) → converted to a DEPRECATED alias of `AIResourceManager`, removing ~356 lines of dead duplicate logic. One canonical predictor chosen: `AIResourceManager`; `ai/umer_ai.py` is the single source of truth.
- H20 → `ai/` GPL license-header normalization **RESOLVED (session 46):** drift-recon showed premise PARTLY stale — `ai/providers.py:43` was ALREADY canonical `License: GPLv3` and no British `Licence` spelling exists anywhere in `ai/`. 3 outliers used the long-form `License: GPL-3.0 (GNU General Public License Version 3)` → normalized to `License: GPLv3` (`umer_ai.py:36`, `resource_predictor.py:34`, `__init__.py:17`). `ai/` now 9/9 modules consistent with the **GPL-3.0** canonical (H7 rule: `Version 3`→`v3`, `Licence`→`License`).
- H23 → `ui/flutter_ui` app registry DRY **RESOLVED (session 47):** drift-recon proved premise STALE — `app_registry.dart` already exists as the single source of truth (`AppRegistry` class); `AppDefinition(` appears ONLY in that file (no widget hardcodes its own list). Consumed by `desktop_shell.dart` (`_DesktopGrid` L474, `_GlobalSearchModal` L1011, `_LaunchPad` L1139) + `dock.dart` (3 refs); invariants guarded by `test/app_registry_test.dart`. The DRY/consistency risk the standard flagged (3 hardcoded copies) was already eliminated in a prior session; this session only corrected the stale standard/checkpoint bookkeeping. No code changed.
- H24 → `ui/flutter_ui` Power/Idle naming (HCI #2) **RESOLVED (session 48):** drift-recon proved premise STALE — the cited "CPUIdle & Governor" (`:287`) and "Power & Idle" (`:448`) launcher labels do NOT exist anywhere in `ui/flutter_ui` (repo-wide grep: 0 hits). Launcher title is plain `Power & Performance` (single source of truth in `app_registry.dart:114`, asserted by `test/app_registry_test.dart:41`); `desktop_shell.dart:325` top-bar popup matches it. The only jargon hit (`power_governor_app.dart:106` "CPUIdle & Power Governor Framework") is an inside-app section header naming the real subsystem the app tunes — accurate domain terminology, not a user-facing launcher label. All 18 `AppRegistry` titles are plain/task-oriented (Nielsen #2 satisfied). No code changed.
- H26 → `ui/flutter_ui` a11y `Semantics` coverage **RESOLVED (session 49):** REAL fix (drift-recon confirmed premise live — repo-wide `Semantics` grep returned 0 hits before). Added `Semantics` labels/button roles to every custom `GestureDetector`/`InkWell` control in `dock.dart` (app icons + window buttons), `draggable_window.dart` (focus + header drag-zone + 8 resize handles), and `desktop_shell.dart` (menubar app entries, date/time status, spotlight app icons, desktop-grid items, LaunchPad + global-search dismiss layers, LaunchPad grid items). Broadened `test/widget_test.dart` with a static-label assertion for the date/time control. `flutter analyze` green (0 new issues; 2 pre-existing unrelated info-lints remain). `flutter test` is blocked in this sandbox — native-asset resolution needs MSVC/cl.exe (Visual Studio), not installed; tests will run in a proper env. 💭 Out of scope (not acted): `apps/*` and `animations/micro_interactions.dart` still contain bare `GestureDetector`/`InkWell` controls — a separate a11y pass is warranted.
- H30 → `boot/` GPL-3.0 header strays **RESOLVED (session 50):** REAL (drift-recon confirmed live — 10 `boot/` modules declared `License: GPL-3.0 (GNU General Public License Version 3)`). Normalized per H7 session-39 rule `Version 3`→`v3`: all 10 now `License: GPL-3.0 (GNU General Public License v3)` (American `License` already correct, no `Licence`→`License`). All 10 byte-compile clean. (H7 cluster `Licence`/header sweeps tracked under H20/H30/H50/H95/H104/H160/H176/H183/H200/H215/H223/H232/H236/H240/H254/H260/H275/H291/H299/H306.)

## Resumable remediation loop
- Goal: fix all H1–H307; tests in `tests/`; `# [FIX Hxxx]` comments; resumable via checkpoint.
- Checkpoint: `.workbuddy-ai/memory/remediation_progress.md` (307 items). Maintain by Edit (flip `- [ ]`→`- [x]`), never regenerate.
- Test runner: stdlib `unittest` via venv `C:/Users/MC Raja Jang/.workbuddy-ai/binaries/python/envs/default`. `cryptography` 50.0.0 available in venv.
- Shared CWE-22 guard: `core/path_guard.py` (`safe_child`, `safe_join`, `PathTraversalError`).
- Shared zero-trust guard: `core/capability_gate.py` (`gate` singleton + `require(cap)`) — bridge to `kernel.capability_manager.CapabilityManager`; fail-closed when wired, permissive-with-warning standalone; `set_strict` to deny.
- Project facts: ~735 Python modules; `tests/` runnable via stdlib `unittest` AND pytest>=8 (the old bundled pytest `imp` issue is fixed by the pin — pytest 9.1.1 verified working in the managed venv). CI now lives in `.github/workflows/` (`ci.yml` + `security_scan.yml`). `Old Linux Code/` (~93k) reference-only (excluded from scope).
- Pre-existing broken test to ignore: `tests/test_ai.py` (collection error — `ai.providers` missing `AIConfigManager`); `home` package-shadowing collision (`bin/home.py`/`root/home.py`) — load `home_backup.py` by file path under a unique name in tests.

## Remediation status
- **RED blockers: ALL CLOSED (H1–H307).** Per-cluster detail + bookkeeping live in `remediation_progress.md` (sessions 1–33).
- **YELLOW sweep in progress (session 36+):** H4, H5, H6, H8, H9, H7, H11, H13, H14, H15, H16, H19 done (H7 resolved session 39 — "Apache-2.0 strays" premise was stale drift; docs already GPL-3.0, normalized `Licence`→`License` + `Version 3`→`v3`. H11 resolved session 40 — Flutter/Dart canonical confirmed live in `ui/flutter_ui/`; Kivy drift fixed across `setup.py`/`kernel/requirements.txt`/`kernel/gui.py`/`start_gui_shell`/README; H22/H25/H122/H125 closed. H13 resolved session 41 — `.umerpkg` real Ed25519 signing + pinned chain-of-trust; was overstated crypto. H14 resolved session 42 — README reconciled. H15 resolved session 43 — requirements.txt: g4f removed, 3 `>` floats bounded `<1.0`, kivy removal explicit, ruff+mypy already enforced, reproducibility policy + `requirements.lock` added. H16 resolved session 44 — `tests/` harness: drift-recon proved premise stale (pyproject [tool.pytest] + blocking CI `pytest` step + tests/quantum already present; venv pytest 9.1.1 so `imp` breakage gone); only real defect (3 legacy `run_*.py` subset runners) converted to deprecated pytest-delegation shims; suite discoverable=2178 collected, green-enforced in CI. H19 resolved session 45 — `ai/` consolidation: drift-recon proved premise stale; `assistant.py`=`LocalAIAssistant` shim, `self_healing.py`=H12/H21-gated wrapper over `SelfHealingEngine`, `resource_predictor.py:ResourcePredictor`→deprecated alias of canonical `AIResourceManager` (removed ~356 lines dead dup logic, one predictor chosen); `ai/umer_ai.py` is the single source of truth). H20 resolved session 46 — `ai/` GPL license labels normalized to `License: GPLv3` (drift-recon: `providers.py` already canonical, 3 outliers `umer_ai.py`/`resource_predictor.py`/`__init__.py` long-form → `License: GPLv3`, `ai/` now 9/9 consistent). H23 resolved session 47 — `ui/flutter_ui` app registry already centralised (`app_registry.dart` `AppRegistry`; consumed by desktop_shell `_DesktopGrid`/`_GlobalSearchModal`/`_LaunchPad` + `dock.dart`; `AppDefinition(` only there, no dup lists). H24 resolved session 48 — `ui/flutter_ui` Power/Idle naming drift STALE: cited "CPUIdle & Governor"/"Power & Idle" strings absent (0 hits); launcher title plain `Power & Performance` (single source in `app_registry.dart`, test-asserted). H26 resolved session 49 — `ui/flutter_ui` a11y (REAL, not stale): `Semantics` labels/button roles added to all custom `GestureDetector`/`InkWell` controls in `dock.dart`, `draggable_window.dart`, `desktop_shell.dart`; `test/widget_test.dart` broadened. `flutter analyze` green. (NOTE: `flutter test` blocked in-sandbox — needs MSVC/cl.exe for native-asset build.) H30 resolved session 50 — `boot/` GPL-3.0 header strays (H7 variant, REAL): 10 modules declared `License: GPL-3.0 (GNU General Public License Version 3)`; normalized `Version 3`→`v3` per H7 → `License: GPL-3.0 (GNU General Public License v3)`; all 10 byte-compile clean. **H31 resolved session 51** — `boot/` tier labels (§4.4, REAL): drift-recon PARTLY STALE — `bootloader.py` (L152/172/176) + `efi_system.py` (L544) already had inline `[TODAY]`, but NO module-level label existed in `boot/`. Added module-level `[TODAY]` to 22 modules + `[EXPERIMENTAL]` to `demo_boot.py`; docstring+label added to `boot/__init__.py` (no prior docstring). Verified all 23 `boot/*.py` labeled; 0 compile failures. **H32 resolved session 52** — `boot/uefi_stub.c` (C placeholder) + `boot/init.py` ctypes claim: premise CONFIRMED but overstatement, not a hard bug. `uefi_stub.c` is a documented `[FUTURE]` scaffold (cited as pseudocode by `bootloader.py:25`), not compiled/linked (setup.py builds 0 C ext) and has NO `ctypes`/`CDLL` bridge. Fix per §9 guidance: `uefi_stub.c` header relabeled `[FUTURE]` (removed false "interface with UEFI via embedded CPython/Cython" claim); `init.py:71` no longer falsely claims "via ctypes" (now "UEFI hardware layer not wired (placeholder scaffold only)" + `[FIX H32]`); added `from __future__ import annotations` to `init.py`. `init.py` byte-compiles clean. Next = **H33** (`boot/bootloader.py:153` SHA3-256 vs `boot/efi_system.py:82` SHA-256: inconsistent hashing vs §4.2 mandate), then remaining YELLOW.
- **Current YELLOW pointer (session 53):** boot/ cluster resolved 🟢 H27–H33; next 🟡 **H34** (`boot/__main__.py` CLI vs `core/command.py` `execute(args=None)->int` contract). bin/ sweep (H35+) pending.
- Sessions 1–23 clusters: var/ boot/ path-traversal, fail-open + dummy-crypto, cap-gate (8/8), collection-error reconciliation, bin/proc, H7 GPL sweep, mount (media/mnt), legal/ GPL + consent, initrd/ eval + traversal, kernel/ managers + crypto + sandbox, proc/srv priv-write, installer/ priv-write + fail-open + dataloss, home tar-restore, etc/ priv-write. (Full per-session notes in the checkpoint file.)
- Sessions 24–35 (carried RED + YELLOW): H3/H146/H147 (lib ssl/pam false-positive + cert-expiry), H157/H167/H168 (media/mnt auto-mount + symlink rmtree), H184/H187 (opt), H198 (packages), H215/H216/H217/H221 (quantum), H244/H245/H246 (security), H12/H18/H21/H42 (cross-cutting: API key, OnlineProvider, self-heal, code-signing), H4/H5 (bin host-subprocess sandbox), H6/H55 (core command base signature convergence).

## Folder scope map — hotspots (🟢 fixed / 🟡 yellow / 💭 nit / 🔴 red; blurbs in standard §9)
- boot/ 🟢 H27,H28,H29,H30,H31,H32,H33; 🟡 H34
- bin/ 🟢 H4,H5,H37,H6,H8; 🟡 H35,H36,H38–H40
- build/ 🟢 H42; 🟡 H41,H43–H45
- cloud/ 🟢 H46,H154; 🟡 H47–H49
- compatibility/ 🟡 H50,H52–H54 (H51 🟢)
- core/ 🟢 H55; 🟡 H56,H57
- dev/ 🟡 H59,H60,H61
- drivers/ 🟢 H64; 🟡 H62,H63,H66,H69; 💭 H65
- etc/ 🟢 H5,H72,H73; 🟡 H70,H71
- examples/ 💭 H74,H75
- feedback/ 🟡 H76–H79
- fs/ 🟡 H80,H81,H82
- home/ 🟢 H83; 🟡 H84–H88
- HostFiles/ 🟡 H89,H90
- initrd/ 🟢 H2,H91,H92,H93; 🟡 H94,H95,H97
- installer/ 🟢 H98,H99,H100,H101,H102,H103; 🟡 H104–H109
- kernel/ 🟢 H110,H111,H112; 🟡 H113–H127
- legal/ 🟢 H128,H129,H130,H131,H135; 🟡 H132,H136–H141; 💭 H137
- lib/ 🟢 H3,H146,H147; 🟡 H148,H149,H150
- liboqs/ 🟡 H151; 💭 H155
- media/ 🟢 H156,H157; 🟡 H158–H160; 💭 H161–H165
- mnt/ 🟢 H166,H167,H168; 🟡 H169–H171,H176; 💭 H172–H175
- network/ 🟢 H177,H178; 🟡 H179,H180; 💭 H181,H182
- opt/ 🟢 H184,H185,H186,H187; 🟡 H188–H193,H200; 💭 H183
- packages/ 🟢 H194,H195,H196,H197,H198; 🟡 H199,H201,H204
- proc/ 🟢 H205,H206,H207,H208; 🟡 H209,H210,H211; 💭 H212–H214
- quantum/ 🟢 H152,H215,H216,H217,H221; 🟡 H218,H219,H220; 💭 H222–H225
- root/ 🟢 H227; 🟡 H226,H228; 💭 H229,H230,H231
- sbin/ 🟢 H233; 🟡 H232; 💭 H234,H235
- scripts/ 🟡 H236,H237; 💭 H238,H239
- sdk/ 🟡 H240,H241; 💭 H242,H243
- security/ 🟢 H17,H244,H245,H246; 🟡 H247–H254; 💭 H255–H258
- sources/ 🟡 H259,H260,H261,H262; 💭 H263,H264
- srv/ 🟢 H265,H266,H267,H268,H271,H273; 🟡 H269,H270,H272; 💭 H274–H277
- tmp/ 🟢 H281,H282,H283; 🟡 H278,H279,H280; 💭 H284–H287
- tools/ 🟡 H288–H292; 💭 H293–H295
- usr/ 🟢 H296; 🟡 H297,H298,H299,H300; 💭 H301,H302
- var/ 🟢 H303,H304,H305,H306,H307

## Tooling cautions
- **Do NOT batch multiple `Edit` calls targeting the SAME file in one message.** They race and can silently drop some changes (observed Session 39: the standard's §9 H7 row and `MEMORY.md` Decided-H7 both reverted to old text until re-applied individually). Apply same-file edits one per message, or verify each with a Read afterward.
