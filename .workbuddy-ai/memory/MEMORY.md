# UmerOS — Long-term Project Memory

## Standing working conventions (Code Review Expert / Kim)
- **Review-standard-first workflow:** After *every* study (file/folder/design/dependency), update `MainTask/Raw Data/Code Review Standards and Process.md` — bump version, fold findings into §1/§4/§7/§9/§11. Standing user instruction; do not treat studies as read-only.
- **Authoritative sources (priority):** `Skills/Context File/umer_os_skills.json` > `MainTask/prompt/*.md` > `MainTask/Raw Data/*.docx` > repo + `LICENSE`/`setup.py` (repo wins when a source is aspirational, e.g. README "Project Structure").

## Decided inconsistencies
- **H7 → GPL-3.0 canonical.** Sweep Apache-2.0 strays in prompts/docs/code to GPLv3.
- **H11 → Flutter (Dart) canonical frontend; backend = Python only.** `ui/*.py` (Tkinter/CLI) is retired legacy (H25); UI MUST follow HCI (Nielsen 10) — §4.8.

## Folder scope map (full detail in standard §9, H1–H307)
- `boot/` = /boot toolkit. 🔴 H27/H28/H29 trust missing/disabled/auto-waiver. +H30–H34.
- `bin/` = ~44 FHS cmds. H6/H35 wrong exec sig (27/44); 🔴 H37 `-f` auth bypass. H36/H38–H40.
- `build/` = PyInstaller. 🔴 H42 unsigned; H41 Tkinter not Flutter. H43–H45.
- `cloud/` = OTA. 🔴 H46 verify fail-open. H47–H49.
- `compatibility/` = foreign-OS. 🔴 H51 ZeroTrustContainer fail-open. H50/H52–H54.
- `core/` = base Command. H55 fix exec sig (root of H6/H35); H56 privileges unenforced. H57.
- `dev/` = ~43 /dev nodes. 🟡 H60 mknod no cap; H59 0o666. H58/H61. Cleanest.
- `drivers/` = 75 modules. 🔴 H64 static JWT + unauth /metrics; 🟡 H66 no cap MMIO/PCI. H62/H63/H69.
- `etc/` = 81 /etc cfg. 🔴 H73 sudoers no cap + NOPASSWD; 🟡 H72 pam permit. H70/H71.
- `examples/` = 2 demos. 💭 H74/H75.
- `feedback/` = BROKEN pkg. 🟡 H76–H79.
- `fs/` = VFS. 🟡 H80 no cap; H81 Apache stray. H82. First tier-label folder.
- `home/` = /home. 🔴 H83 tar extractall no filter; 🟡 H84 path-traversal username. H85–H88.
- `HostFiles/` = 3 assets. 🟡 H90 host write+auto-open. H89.
- `initrd/` = 17 early-boot. 🔴 H91 eval(); H92 no cap; H93 cpio traversal. H94–H97.
- `installer/` = 3 files. 🔴 H98 double Installer; H99 waiver fail-open; H101 rmtree rollback. H100/H102–H109.
- `kernel/` = 40-file microkernel. 🔴 H110 no-op placeholders → zero-trust/IPC/mem INERT; H111 dummy CryptoEngine.verify→True; H112 SecuritySandbox.register prints. H113–H127 (sudo no auth, kill/shutdown ungated, cap unwired, GUI Popen Tkinter, ipc skips HMAC, is_root bypass, cgroup allow, kivy drift, dead REPLs, import-time crypto, udev RUN unimpl+0o666, panic taint).
- `legal/` = 12-file consent pkg. 🔴 H128 Apache-2.0 primary (no GPL-3.0; H7 lives here); H129 audit fail-open; H130 get_license_text GPL returns Apache; H131 consent fails OPEN; H135 cli hardcodes "I AGREE". 🟡 H132 forgeable token; H136 DCO decorative; H138 hardcoded fingerprints; H139 soft safety_check. 🔴/🟡 H140 broken test_legal Test 4; H141 tests encode fail-open. 💭 H137 real PII/IBANs.
- `lib/` = 33-source /lib sim. BEST-disciplined (full baseline, zero dangerous calls, fsck fail-closed). 🔴 H3 `PASSWORD="password"`; H146 ssl `_check_is_trusted` fail-open; H147 `is_expired`→False. 💭 H148 string-parse sim; 🟡 H149 ~23 Apache strays (largest cluster, H7); 💭 H150 pam_permit.
- `liboqs/` = vendored UNPINNED OQS C (MIT, conflicts H7 → H151 🟡). Real FIPS-203/204/205 PQC. Live PQC NOT wired; UmerOS uses `quantum/crypto_pqc.py` (silent fallback → H152 🔴) + `kernel/pqcrypto_.py` (H153 🟡). 🔴 H154 OTA hardcoded fake sig. 💭 H155 dead/unwired.
- `media/` = 12 /media. 🔴 H156 no cap on mount/auto-mount/udisks2; H157 rw mount w/o noexec,nosuid,nodev. 🟡 H158 authz unwired; H159 fstab skips validate; H160 5 Apache strays. 💭 H161–H165.
- `mnt/` = 7 /mnt. 🔴 H166 no cap mount/remove/fstab; H167 rmtree symlink TOCTOU; H168 fstab write un-gated/drops comments. 🟡 H169 noexec missing; H170 validator advisory; H171 mtab clobber. 💭 H172–H175; 🟡 H176 6 Apache strays. Better than media (7 `_selftest` functional; audit.py safe).
- `network/` = 6 modules. 🔴 H177 no cap egress; H178 SSRF (no internal-range block). 🟡 H179 XOR "encryption"; H180 plaintext DNS. 💭 H181/H182.
- `opt/` = 14 /opt. 🔴 H184 no cap; H185 path-traversal file rw; H186 rmtree traversal; H187 cmd-injection launcher. 🟡 H188 profile.d inject; H189 hardcodes /etc/opt; H193 svc exec. 💭 H190–H192.
- `packages/` = 3 modules (best small baseline). 🔴 H194 tar-slip; H195 manifest traversal; H196 fail-open "Signed"; H197 payload not hashed; H198 no cap. 🟡 H199 lex compare; H200 Apache stray; H201 fuzzy match; H204 symlink extract. 💭 H202/H203.
- `proc/` = 24 /proc (CLEANEST dangerous-call hygiene, zero subprocess/eval/etc). 🔴 H205 write enforces only mode (no cap); H206 ~60 sysctl ungated; H207 oom_score_adj ungated; H208 irq affinity ungated. 🟡 H209 no-op rw; H210 hostname newline/registry bypass; H211 fabricated CapEff. 💭 H212 baseline; H213 missing GPL; H214 simulated fallback.
- `quantum/` = 62 modules (ZERO dangerous calls). 🔴 H215 Apache stray; H216 silent classical downgrade (H152); H217 plaintext creds; H221 0.0.0.0:8420 + wildcard CORS + no auth. 🟡 H218 no TLS pin; H219 toy QKD; H220 simulator QRNG. 💭 H222 CLI token argv; H223 2nd Apache stray; H224 print leak; H225 REST tokens clear.
- `root/` = 9 FHS /root modules (~87 KB). ZERO dangerous calls (cleanest alongside proc/quantum) + safe `os.chmod` (0700/0644/0600) + `shell.py` strips `LD_*` + `shlex.quote` + model read-only `safety.py`. 🟡 H226 10 Apache-2.0 strays (largest single-folder cluster, H7); 🟡 H227 `passwd.write()`/`upsert()` rewrite `/etc/passwd` ungated (cap family); 🟡 H228 `mail.ensure()` creates home @umask (~0755) not 0700. 💭 H229 `.bak` world-readable; H230 `home.ensure` chmod follows symlink + arbitrary CLI path; H231 dotfile template name traversal.
- `sbin/` = 9 FHS /sbin modules (~95 KB). **CLEANEST folder overall** — fully SIMULATED (ZERO dangerous calls AND zero real syscalls; every command prints+returns, mutating in-memory dicts). Only `maintenance.MktempCommand` does a real (safe, `tempfile` 0600) write. 🟡 H232 **missing GPL-3.0 header in all 9 modules** (zero GPL/Apache/Copyright strings — "missing-header" variant of H7, distinct from the Apache strays). 💭 H233 `SbinManager.execute()` + each `execute()` no cap gate / no audit log (ungated pattern for real wiring); 💭 H234 mount/filesystem ops simulated vs in-memory tables, real wiring must harden (`nosuid,nodev,noexec` + path checks, per mnt/media H156–H168); 💭 H235 commands masquerade as functional (`halt`/`mount -a` do nothing) → false-state risk. Models: `mount.py` global-state save/restore in `_selftest`, `mktemp` safe tempfile.
- `scripts/` = 2 dev/CI tooling scripts (1,736 B) — NOT a shipped runtime (helpers only). Bright spot: both use `subprocess` **injection-safe** (argv-list, no `shell=True`). 🟡 H236 missing GPL-3.0 header in both files (H7 missing-header variant, like sbin H232); 🟡 H237 `test_endpoint.generate_test_jwt` hardcodes `secret="test-secret"` (HS256) = **same** trusted secret as `drivers.driver_service` 🔴 H64 — the test legitimates the broken auth and would PASS against the vulnerable service, masking H64; 💭 H238 fixed `time.sleep(3)` readiness wait (flaky CI) + hits endpoint with a forged token bypassing any `CapabilityManager`/auth; 💭 H239 `install_deps.py` `pip install -r requirements.txt` unpinned (supply-chain: compromised/edited requirements installs arbitrary pkgs as current user) + `capture_output=True` buffers whole log; neither file has `from __future__`. Models: argv-list subprocess in both.

- `sdk/` = 3 dev SDK modules (~13 KB) — developer SDK: `UmerApp` base + `BuildTool` scaffolder/packager + example. Clean hygiene + good zero-trust delegation (all FS/IPC/AI via bound `kernel_api` with `hasattr`+`RuntimeError` guard, never direct FS). 🟡 H240 missing GPL-3.0 header (all 3, H7 missing-header); 🟡 H241 unvalidated `app_name` → VFS path traversal (`/sdk/projects/{name}`); 💭 H242 `custom_app_code` written verbatim; 💭 H243 no `from __future__`/`__all__`/`_selftest`. Models: `app_template.UmerApp` kernel-API delegation.

- `security/` = 14 modules (~127 KB) — OS security subsystem (6 top-level + `antivirus/` 8-module subpackage). Mixed: real crypto (AES-256-GCM, requires lib)/TLS (`create_ssl_context` strict)/hashing/AV-mechanics, BUT security-specific false claims. 🔴 H244 `SecureBoot` fail-open default (allow-unknown); 🔴 H245 unauthenticated AV API (`127.0.0.1:9095`, destructive endpoints, localhost); 🔴 H246 `SecuritySandbox` inert/masquerading (no real isolation). 🟡 H247 IPC anti-replay claimed-not-implemented; 🟡 H248 `AIBehavioralMonitor` decorative+cosmetic quarantine; 🟡 H249 `verify_signature` mislabeled (hash≠key); 🟡 H250 `AIFirewall` toy masquerading; 🟡 H251 quarantine restore path-traversal/arbitrary write + deletes original; 🟡 H252 heuristics memory-DoS + Windows-centric; 🟡 H253 signatures fake hashes + silent JSON swallow; 🟡 H254 missing GPL header (all 14, H7). 💭 H255 realtime polling not FS-events; 💭 H256 dead code (~900 lines) + no `_selftest`; 💭 H257 tls_utils doc mismatch (TLS1.3-first vs min1.2) + 0.0.0.0 default; 💭 H258 crypto_engine ephemeral key + print. Bright spot: `tls_utils` is the best-engineered file (hardened TLS, secret-from-`/run/secrets`, async client).
- `sources/` = 9 modules (~65 KB / 1,724 LOC) — documentation/reference subsystem (bibliography, System V signals, glossary, kernel-doc specs, `/usr/src` skeleton manager, CLI, tests). **Zero dangerous calls** (only `os.walk`/`open` reads). 🟡 H259 8 Apache-2.0 strays (H7); 🟡 H260 `test_sources.py` missing GPL header + `from __future__` (H7 missing-header); 🟡 H261 fragile absolute intra-package imports + `sys.path` hack; 🟡 H262 hardcoded Windows `DEFAULT_SRC_ROOT` + `__init__`-time mkdir side effect. 💭 H263 unused `shutil`; 💭 H264 signals "inter-process" doc overclaim. Models: relative intra-package imports + runtime-resolved src root when real wiring.

- `srv/` = 10 modules (~2,352 LOC) — FHS `/srv` site-specific service-data hierarchy manager (`__init__`,`service`,`permissions`,`protocols`,`fhs`,`backup`,`hierarchy`,`manager`,`cli`,`test_srv`). **One of the few folders with LIVE dangerous FS calls**: `tar.extractall`/`zip.extractall` (no `filter=`), `shutil.rmtree`×3 (destructive), `os.chmod`×2 (POSIX-gated). 🔴 H265 tar extract no `filter=` (CVE-2007-4559 path traversal, `backup.py:154`); 🔴 H266 zip extract no `filter=` (zip-slip, `backup.py:157`); 🔴 H267 `restore_backup` `shutil.rmtree(dest_folder)` no cap gate (`backup.py:181`); 🔴 H268 `delete_service_tree` `shutil.rmtree(target)` gated only by `force=True` no cap (`hierarchy.py:290`). 🟡 H269 9 Apache-2.0 strays (H7); 🟡 H270 hardcoded Windows `/srv`+`.backups`+registry paths + `__init__`-time mkdir (`fhs`/`backup`/`manager`); 🟡 H271 fragile absolute imports + `sys.path.insert` hack; 🟡 H272 `protocols.generate_nfs_export_line`/`generate_samba_share_section` unvalidated interpolate into `/etc/exports`/`smb.conf` (config-injection); 🟡 H273 `permissions.apply_profile` `os.chmod` no cap gate. 💭 H274 localhost test HTTP server no auth; 💭 H275 `test_srv.py` missing GPL header + `from __future__` (H7 missing-header); 💭 H276 `service.py` unused `import json`; 💭 H277 `permissions.py` unused `Tuple`. Models: `filter="data"` on extractall + cap-gate rmtree when real-wired.

- `tmp/` = 11 modules (~65 KB / ~2,000 LOC) — FHS `/tmp` transient-files manager (`__init__`,`fhs`,`hierarchy`,`secure_io`,`lockfile`,`reaper`,`permissions`,`tmpfs`,`manager`,`cli`,`test_tmp`). **Best-engineered folder for secure-temp discipline**: grep ZERO `subprocess`/`eval`/`exec`/`os.system`/`pickle`/`ctypes`(top)/`__import__`/`tarfile`/`zipfile`/`CapabilityManager`; `secure_io` uses `secrets`+`O_CREAT|O_EXCL|O_NOFOLLOW` 0600/0700; only `shutil.rmtree` is self-scoped temp-dir cleanup. **0 🔴 + 6 🟡 + 4 💭 (no new 🔴)**. 🟡 H278 10 Apache-2.0 strays (H7); 🟡 H279 hardcoded Windows `DEFAULT_TMP_ROOT` + `__init__`-time mkdir (centralized in `fhs`, better than srv); 🟡 H280 fragile absolute imports + `sys.path.insert`; 🟡 H281 reaper deletes by age/quota, no cap gate + no `tmp_root` containment (cap-gate, 🟡 not 🔴 — bounded + excludes protected dirs); 🟡 H282 `tmpfs.sync_to_disk` unvalidated node name → path traversal/arbitrary write (CWE-22); 🟡 H283 `enforce_permissions` `os.chmod 1777` no cap gate (correct mode). 💭 H284 `test_tmp.py` missing GPL header + `from __future__` (H7 missing-header, but tests use proper `from tmp.X import`); 💭 H285 dead imports (`shutil` hierarchy/reaper/manager/test_tmp, `json` test_tmp, unused `Tuple`); 💭 H286 lazy `ctypes` Win32 PID check (low risk); 💭 H287 global singleton bootstrap hidden init side effect. Models: `secure_io` race-free mktemp (copy everywhere) + reaper protected-dir exclusion + tmpfs quota.

- `tools/` = 1 module (`installer.py`, ~967 B / 28 LOC) — standalone OS installer script (NOT a package, no `__init__`). **Clean dangerous-call surface** (grep ZERO `subprocess`/`eval`/`exec`/`os.system`/`pickle`/`ctypes`/`__import__`/`tarfile`/`zipfile`/`os.chmod`/`CapabilityManager`/`sys.path`/Windows paths); only FS mutations `shutil.rmtree(dst)` (pre-copy cleanup of allowlisted subdir) + `shutil.copytree(src, dst)`. **0 🔴 + 5 🟡 + 3 💭** (no new 🔴 — destructive rmtree bounded to allowlisted subdirs of `target_dir`). 🟡 H288 `rmtree(dst)` no cap gate (cap family H227/H233/H267/H273, 🟡 not 🔴 — bounded to allowlisted names); 🟡 H289 hardcoded `target_dir="/umer_os"` (H262 variant, project path not dev-machine); 🟡 H290 `src` resolved CWD-relative (`os.path.exists(src)`+`copytree`) — wrong-dir copy / fail-open; 🟡 H291 missing GPL-3.0 header + `from __future__` (H7 missing-header, like scripts/sbin/sdk/sources/srv/tmp); 🟡 H292 `input()` EULA, no GPL alignment / non-interactive. 💭 H293 `copytree` fails open (silent partial install); 💭 H294 `logging` unconfigured (dropped records); 💭 H295 no docstring/`__all__`/type hints. Fix order: GPL header (H7 H291) → cap-gate rmtree + explicit opt-in (H288) → runtime/arg `target_dir` (H289) → resolve `src` from script dir (H290) → surface missing sources (H293) → configure logging (H294) → non-interactive EULA flag (H292/H295).
- `usr/` = 61 modules (~22.9k LOC) + `src/` — FHS `/usr` hierarchy manager. **MIXED**: ~20 LIVE (real FS writes at system paths) + ~41 SIMULATED (in-memory). Clean dangerous surface (grep ZERO `subprocess`/`eval`/`exec`/`tarfile`/`zipfile`/`ctypes`; only scoped `shutil.rmtree` in 3 modules — `bsd_compat_manager.py:230`, `games_data_manager.py:239`, `xml_manager.py:223`). **0 🔴 + 5 🟡 + 2 💭 (no new 🔴)**. 🟡 H296 privileged FS ops no cap gate (`sendmail_manager` symlink/unlink `/usr/lib`+`/usr/sbin`, `misc_data_manager` write + magic symlink, 3× `rmtree` — cap family); 🟡 H297 construction-time `mkdir` at `/usr/share/...` (`__init__`/`_refresh`, no cap/opt-in — like H262/H270/H279 but system `/usr` root); 🟡 H298 fail-open `except Exception: return False` around privileged ops; 🟡 H299 H7 missing GPL-3.0 header — only 3/61 licensed, **58/61 unlicensed** (largest missing-header cluster in tree); 🟡 H300 hardcoded `/usr/...` system paths (no runtime/arg resolve). 💭 H301 SIMULATED privileged/kernel-surface modules masquerade (`lib`/`sbin`/`rpm`/`gcc`/`x11` managers in-memory; `io_uring`/`landlock`/`seccomp`/`ntsync`/`iommufd`/`dmabuf`/`vduse`/`tee`/`netlink`/`procfs`/`sysfs`/`syscalls` no real syscall wiring — false-state, like sbin H235); 💭 H302 baseline gaps + `usr/` ships no `_selftest()`/tests. Models: none stand out; `io_uring`/`landlock` are clean enums but aspirational. Fix order: GPL headers (H7 H299) → cap-gate LIVE ops (H296) + move `mkdir` out of `__init__` (H297) → runtime/arg `/usr` roots (H300) → narrow fail-open `except` (H298) → label/wire simulated + kernel-surface (H301) → baseline + `test_usr.py` (H302).
- `var/` = 4 Python modules (~706 LOC) + 2 data files (`lib/umeros/consent_ledger.json`, `log/umeros_initrd.log`) — FHS `/var` variable-data hierarchy manager (`LogManager` @ `/var/log`, `SpoolManager` @ `/var/spool`, `VarDirectoryManager` @ `/var/local,/var/lock,/var/opt,/var/run,/var/tmp`). **100% LIVE** (smallest AND only all-LIVE folder studied; no SIMULATED). Clean dangerous-call surface (ZERO `subprocess`/`eval`/`tarfile`/`zipfile`/`ctypes`/`sys.path`); only `shutil.rmtree`×3 (scoped to manager-owned children in `cleanup_*` — safe-ish), but ALL real FS writes fed by **unsanitized path params** (the headline risk). **1 🔴 + 3 🟡 + 1 💭**. 🔴 H303 path-traversal → arbitrary rmtree/mkdir/write/unlink + **cron RCE** (`name`/`username`/`directory`/`filename` joined as `self.<x>_path / param` with no scoping; `set_cron_user("../../etc/cron.d/x", jobs)` writes `/etc/cron.d/x`); 🟡 H304 cap-gate on privileged FS ops (all 3 managers, no `CapabilityManager`); 🟡 H305 fail-open `except Exception: return False` (bare `except Exception: pass` at `directory_manager.py:154`); 🟡 H306 H7 Apache-2.0 strays (3/4 declare `Licence: Apache 2.0`, `__init__.py` unlicensed — opposite of usr/ H299 missing-header). 💭 H307 no `_selftest()`/tests + lazy in-function `import shutil`/`gzip`. Models: `LogManager.compress_old_logs` is safe (glob on owned path); baseline otherwise good (`from __future__`, type hints, docstrings, logging). Fix order: validate path params (reject `..`, `realpath` under root) → cap-gate privileged ops → narrow fail-open `except` → GPL-3.0 headers → `test_var.py`.

## Known 🔴 hotspots (fail-open / cap-gate / dummy-crypto family) — full table in standard §9
H1 live OpenRouter key; H2/H91 eval initrd; H3 PASSWORD="password"; H17 SB verify fail-open; H27/H28/H29 boot; H37 bin auth bypass; H46 OTA; H51 compat; H60 dev; H64 drivers; H66 drivers; H73 etc sudoers; H83 home tar; H91/H92/H93 initrd; H98/H99/H101 installer; H110/H111/H112 kernel inert; H128/H129/H130/H131/H135 legal; H146/H147 lib SSL; H152 quantum fallback; H154 OTA fake sig; H156/H157 media; H166/H167/H168 mnt; H177/H178 network; H184/H185/H186/H187 opt; H194–H198 packages; H205–H208 proc; H215/H216/H217/H221 quantum; H244/H245/H246 security; H265/H266 tar/zip extract (no filter=); H267/H268 srv rmtree (no cap gate); H303 var path-traversal → cron RCE (set_cron_user "../../etc/cron.d/x", arbitrary rmtree/mkdir/write).

- `tools/` adds 0 🔴 + 5 🟡 (H288 cap-gate rmtree bounded to allowlisted subdirs, H289 hardcoded `/umer_os`, H290 CWD-relative src fail-open, H291 missing GPL header H7, H292 interactive EULA) + 3 💭 (H293 copytree fails open, H294 logging unconfigured, H295 no docstring/`__all__`/type hints) — clean dangerous-call surface, no live exploit.
- `usr/` adds 0 🔴 + 5 🟡 (H296 cap-gate across LIVE privileged FS ops — sendmail symlink/unlink, misc_data write+magic symlink, 3× scoped `rmtree`; H297 construction-time `mkdir` at `/usr/share/...` system paths via `__init__`/`_refresh`; H298 fail-open `except Exception: return False` around privileged ops; H299 missing GPL-3.0 header — only 3/61 licensed, **58/61 unlicensed**, largest H7 cluster; H300 hardcoded `/usr/...` system paths) + 2 💭 (H301 simulated privileged/kernel-surface modules masquerade as functional — `lib`/`sbin`/`rpm`/`gcc`/`x11` + `io_uring`/`landlock`/`seccomp`/`ntsync`/`iommufd`/`dmabuf`/`vduse`/`tee`/`netlink`/`procfs`/`sysfs`/`syscalls` no real syscall wiring; H302 baseline gaps + `usr/` ships no `_selftest()`/tests) — clean dangerous-call surface, no live exploit, no new 🔴.
- `var/` adds 1 🔴 + 3 🟡 (H303 path-traversal → arbitrary rmtree/mkdir/write/unlink + **cron RCE** via unsanitized `name`/`username`/`directory`/`filename` across `VarDirectoryManager`/`SpoolManager`/`LogManager` — `set_cron_user("../../etc/cron.d/x", jobs)` writes `/etc/cron.d/x`; H304 cap-gate on privileged FS ops across all 3 managers, no `CapabilityManager`; H305 fail-open `except Exception: return False` (bare `except Exception: pass` at `directory_manager.py:154`); H306 H7 Apache-2.0 strays — 3/4 modules declare `Licence: Apache 2.0`, `__init__.py` unlicensed) + 1 💭 (H307 no `_selftest()`/tests + lazy in-function `import shutil`/`gzip`) — **100% LIVE** folder (4 modules, ~706 LOC + 2 data files), smallest studied, clean dangerous-call surface otherwise (zero `subprocess`/`eval`/`tarfile`/`zipfile`/`ctypes`/`sys.path`), no construction-time mkdir.

## Project layout notes
- ~735 active Python modules; `tests/` unittest/pytest split, NO CI; `security_scan.yml` only workflow.
- `Old Linux Code/` (~93k) reference-only, excluded.
- **Cross-cutting remediation pass (ACTIVE since 2026-08-22 (remediation loop) — var/ cluster done; see 'Active Remediation Loop' section):** fix fail-open / cap-gate / dummy-crypto family — **H227**/H303. `quantum/` adds 4 🔴 + 3 🟡 + 4 💭; `root/` adds 0 🔴 + 3 🟡 (H226 Apache cluster H7, H227 cap-gate on /etc/passwd, H228 mail home perms) + 3 💭 (H229 `.bak`, H230 symlink chmod, H231 template traversal); `sbin/` adds 0 🔴 + 1 🟡 (H232 missing GPL header, H7 missing-header variant) + 3 💭 (H233 no cap gate/audit, H234 mount hardening pending, H235 simulated-command false state) — fully simulated, no live exploit; `scripts/` adds 0 🔴 + 2 🟡 (H236 missing GPL header H7 missing-header; H237 test JWT shares prod `test-secret`, legitimates 🔴 H64) + 2 💭 (H238 sleep(3) readiness + forged-token bypass; H239 unpinned pip install supply-chain); `sdk/` adds 0 🔴 + 2 🟡 (H240 missing GPL header H7 missing-header; H241 unvalidated `app_name` → VFS path traversal) + 2 💭 (H242 verbatim `custom_app_code`; H243 no `from __future__`/`__all__`/`_selftest`) — clean hygiene + good zero-trust delegation model, no live exploit. `security/` adds 3 🔴 (H244 SecureBoot allow-unknown fail-open, H245 unauthenticated AV API 127.0.0.1:9095 destructive endpoints, H246 SecuritySandbox inert/masquerading) + 8 🟡 (H247 IPC no anti-replay, H248 AIBehavioralMonitor decorative, H249 verify_signature mislabeled, H250 AIFirewall toy, H251 quarantine restore path-traversal + deletes original, H252 heuristics whole-file memory-DoS, H253 fake signature hashes, H254 missing GPL header all 14) + 4 💭 (H255 realtime polling, H256 ~900 LoC dead code, H257 tls_utils doc mismatch, H258 ephemeral master key) — best-engineered = tls_utils, masquerading controls = the security family. `sources/` adds 0 🔴 + 4 🟡 (H259 Apache-2.0 strays all-but-test H7; H260 `test_sources.py` missing GPL header + `from __future__` H7 missing-header; H261 fragile absolute intra-package imports + `sys.path` hack; H262 hardcoded Windows `DEFAULT_SRC_ROOT` + `__init__`-time mkdir side effect) + 2 💭 (H263 unused `shutil`; H264 signals "inter-process" doc overclaim) — clean dangerous-call hygiene, no live exploit. `srv/` adds 4 🔴 (H265 tar extract no `filter=` CVE-2007-4559 path traversal, H266 zip extract no `filter=` zip-slip, H267 `restore_backup` `shutil.rmtree` no cap gate, H268 `delete_service_tree` `shutil.rmtree` gated only by `force` no cap) + 5 🟡 (H269 9 Apache-2.0 strays H7; H270 hardcoded Windows `/srv`+`.backups`+registry paths + `__init__`-time mkdir; H271 fragile absolute imports + `sys.path.insert` hack; H272 NFS/Samba config-injection unvalidated interpolate; H273 `apply_profile` `os.chmod` no cap gate) + 4 💭 (H274 localhost test HTTP server no auth; H275 `test_srv.py` missing GPL header + `from __future__` H7 missing-header; H276 unused `import json`; H277 unused `Tuple`) — **LIVE dangerous FS calls** (only folder beside home/packages/opt/media/mnt/installer with real extract/rmtree); fix order = `filter="data"` on extractall then cap-gate the rmtree. `tmp/` adds 0 🔴 (no new 🔴 — its only `shutil.rmtree` is self-scoped temp-dir cleanup, all `os.chmod` use correct 1777/0700/0600 modes) + 6 🟡 (H278 10 Apache-2.0 strays H7; H279 hardcoded Windows `DEFAULT_TMP_ROOT` + `__init__`-time mkdir; H280 fragile absolute imports + `sys.path.insert`; H281 reaper deletes by age/quota no cap gate + no `tmp_root` containment; H282 `tmpfs.sync_to_disk` unvalidated node name → path traversal/arbitrary write CWE-22; H283 `enforce_permissions` `os.chmod 1777` no cap gate) + 4 💭 (H284 `test_tmp.py` missing GPL header + `from __future__` H7 missing-header; H285 dead imports `shutil`/`json`/`Tuple`; H286 lazy `ctypes` Win32 PID check; H287 global singleton bootstrap hidden init side effect) — **best-engineered folder for secure-temp discipline** (`secure_io` race-free mktemp model); fix order = GPL headers (H7 H278,H284) → runtime/arg resolve tmp root + explicit bootstrap (H279/H287) → relative imports (H280) → cap-gate reaper + assert `tmp_root` (H281) → validate tmpfs names (H282) → cap-gate chmod (H283) → drop dead imports (H285) → document ctypes shim (H286). Total hotspots now **H1–H307**.


## Active Remediation Loop (started 2026-08-22)
- User asked to FIX all hotspots H1-H307 from the standard + MEMORY, with tests in `tests/`, explanatory `# [FIX Hxxx]` comments, and resumable across sessions (user says "continue" when context/tokens end; resume from the checkpoint).
- Resumable checkpoint: `.workbuddy-ai/memory/remediation_progress.md` — 307-item checklist (66 RED / 152 YELLOW / 89 BLUE) generated from standard §9. Each session: pick first unchecked RED, fix in code with `# [FIX Hxxx]` comment, add/extend the matching `tests/test_*.py`, flip box to `[x]`, update NEXT. Do NOT regenerate the checklist (would reset status).
- Test runner: `tests/` uses pytest. Isolated venv created at `C:/Users/MC Raja Jang/.workbuddy-ai/binaries/python/envs/default` (pytest installed). Run: `<venv>/Scripts/python -m pytest tests/test_var.py -q`.
- Session 1 (2026-08-22) — `var/` cluster DONE: H303 path-traversal -> cron RCE (CWE-22) fixed via new shared guard `var/_path_guard.py::safe_child` + `PathTraversalError`, wired into all 3 managers; H305 silent `except: pass` -> `log.debug`; H306 Apache->GPL-3.0 headers (3 modules + `__init__.py`); H307 added `tests/test_var.py` (13 tests pass). H304 cap-gate DEFERRED to the cap-gate family pass (needs CapabilityManager wiring).
- Next recommended cluster: path-traversal H185/H186/H195/H265/H266/H282 using the same `safe_child` pattern.
