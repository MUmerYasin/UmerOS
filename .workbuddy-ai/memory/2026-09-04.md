# 2026-09-04 — UmerOS remediation

## Session 34 — YELLOW sweep: H5 (host subprocess audit)
- **H5** (`bin/boolean_ops.py` `EnvCommand`, `etc/issue_motd.py`): per Code Review Standard §9 ("Keep list-form, no shell=True; sandbox on Windows host"):
  - `bin/boolean_ops.py`: `EnvCommand.execute` host-exec path (`subprocess.run(real_args, env=env)`) now gated behind `CAP_SYS_ADMIN` via `core.capability_gate`; `require()` placed **before** the `try` so a denied call fails closed (`PermissionError`) instead of being swallowed by the `OSError` handler. Permissive when no trust source is wired (standalone/CLI safe), fail-closed under strict/wired posture. `# [FIX H5]`.
  - `etc/issue_motd.py`: `who`/`uptime`/`last` probes funnelled through a new `_run_host_readonly(cmd)` allowlist helper (`_HOST_INFO_ALLOWLIST`); refuses non-allowlisted commands and returns `None` on a Windows host; graceful fallback preserved. `# [FIX H5]`.
  - New test `tests/test_host_subprocess_security.py` (6 unittest cases): gate fails closed under strict, gate consulted when unwired, allowlist accepts who/uptime/last, refuses `rm -rf /` & `sh -c`, empty refused, Windows guard.
  - Verification: `tests/test_host_subprocess_security.py` 6/6 OK; `tests/test_bin.py` 243 OK (43 skipped) — no regression.
- **Verified carried-over Session-24 items** (the 429 outage had left them written but unverified): `lib/ssl_libs.py` H147 (cert-expiry now real, `[FIX H147]`) and `lib/security.py` H3 (PAM enum false-positive clarified). `tests/test_ssl_security.py` 7/7 OK. Checkpoint already recorded sessions 24–33 closed; **all RED blockers (H1–H307) confirmed done.**
- **Bookkeeping (4 surfaces):** flipped H5 in checkpoint; bumped standard §9 H5 → 🟢; consolidated `MEMORY.md` folder map (lib/media/mnt/opt/packages/quantum/security RED→🟢, plus H4/H5/H42/H72/H227/H233 done); this daily log.

## NEXT
YELLOW sweep continues: **H6 / H55** — converge `core/command.py` base `Command.execute` signature (`execute(*args)->Any` vs `execute(args=None)->int`) so `bin/*` subclasses agree on the contract. Say **'continues'**.
