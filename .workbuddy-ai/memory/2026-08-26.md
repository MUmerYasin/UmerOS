# 2026-08-26 — UmerOS remediation session 21 (initrd/ RED cluster)

Resumed the H1–H307 remediation loop (single checkpoint `remediation_progress.md`).
Closed the open installer checkpoint gap first (H102/H103 flipped to `[x]` — the
code + tests for session 20 were already complete; only the checkpoint rows were
un-flipped across the interrupted turns). Then tackled the next open RED cluster:
**initrd/** (H2/H91/H92/H93).

## Code changes
- `initrd/ai_helper.py` `_load_history` — **H2 / H91 FIXED**. The boot history log
  was parsed with `eval(line)` (code execution as PID 1 / uid 0 from an
  attacker-influenceable log). Replaced with `ast.literal_eval`; malformed /
  non-literal lines are dropped fail-closed (only dict records kept). Added
  `# [FIX H2]` / `# [FIX H91]`.
- `initrd/builder.py` `_unpack_to_dir` — **H93 FIXED**. CPIO entries were written
  with `dest = target / rel` where `rel` kept `..` segments → arbitrary file
  write outside the unpack target (CWE-22). Now validates every entry with the
  shared `core.path_guard.safe_join`; escapes raise `PathTraversalError` and the
  entry is skipped fail-closed. Added `# [FIX H93]` + import of the shared guard.
- `initrd/linuxrc.py` `_drop_to_root` — **H92 FIXED**. `os.seteuid(0)` (the most
  privileged boot op) was ungated. Now requires `CAP_SYS_ADMIN` first (fail-closed
  when a `CapabilityManager` is wired / strict mode; the denial propagates and
  aborts the boot). `pivot_root` / `mount` / `chroot_into` / `exec /sbin/init` are
  simulated VFS ops with no real privilege, so the initrd-as-PID-1 trust boundary
  is enforced at the seteuid gate. Added `# [FIX H92]` + guarded gate import
  (permissive fallback so the import never blocks boot).

## Tests (added to `tests/test_initrd.py`, stdlib unittest)
- `TestAIHelperHistorySafety` (2): parses dict literals; a malicious `eval` payload
  line is NOT executed (sentinel file absent).
- `TestBuilderCpioTraversal` (3): `../evil.txt` and `etc/../../pwned` escapes are
  refused; benign tree still written; symlink preserved.
- `TestDropToRootCapGate` (3): permissive (unwired) returns uid; strict mode raises
  `PermissionError`; full boot aborts (non-zero) without the capability.
- Result: initrd module **86 passed, 0 failures** (was 78; +8 new security tests).

## Bookkeeping updated
- `remediation_progress.md`: H2/H91/H92/H93 → `[x]`. (H98–H103 already `[x]`.)
- `MainTask/Raw Data/Code Review Standards and Process.md` §9: H2/H91/H92/H93 → 🟢.
- `MEMORY.md`: `initrd/` → 🟢 H2,H91,H92,H93; Session 21 DONE bullet.
- This daily log.

## Next open RED hotspots (per §9, after initrd/)
kernel/ H110,H112 (H111 dummy-crypto residual) → legal/ H128,H130,H131,H135 →
lib/ H3,H147 → media/ H157 → mnt/ H167,H168 → opt/ H184,H187 → packages/ H198 →
quantum/ H215,H216,H217,H221 → security/ H244,H245,H246. Plus cross-cutting:
H1 (plaintext API key), H12 (AI hot-patch), H18 (OnlineProvider no governance),
H21 (self-healing), H42 (no code signing).
Say **"continues"** to pick up kernel/ H110,H112.
