# 2026-08-22

## quantum/ folder study (v1.32 of the standard)
- User directed study of `quantum/` — a **quantum-computing SDK/simulator**: **62 modules (~24,641 LOC)** (`crypto_pqc`, `qkd`, `qrng`, `cli`, `quantum_sim`, `backend`, `quantum_server`, `quantum_api`, `providers/` IBM/IonQ/Braket/Rigetti adapters, `cloud/` auth+session, `compiler/`, `QISKIT_RESEARCH/` 3 markdown reports).
- **Strong dangerous-call hygiene:** grep across all 62 files found **ZERO** `subprocess`/`eval`/`exec`/`shell=True`/`os.system`/`pickle`/`ctypes`/`yaml.load`/`__import__`/`tarfile`/`shutil.rmtree`/`chmod`/`CapabilityManager`. `providers/base.py` even **redacts tokens in `__repr__`** (`token={'***' if self.token else None}`, L512) — good practice; contrast `backend.py:291` which `print`s init warnings (H224).
- **🔴 H215 (H7):** `crypto_pqc.py:22` docstring `GPL-3.0 (GNU General Public License Version 3)` stray — contradicts adopted GPL-3.0 canonical (same cluster as mnt H176 / opt H183 / packages H200 / legal H128). `quantum_sim.py:20` is a 2nd stray (H223).
- **🔴 H216 (re-confirms H152):** `PostQuantumCrypto` silently selects `_FallbackBackend` (Ed25519+AES-256-GCM, docstring "not quantum-safe") when `import oqs` fails, while the facade still advertises "Post-Quantum" crypto with no warning/exception — callers believe they have quantum-safe crypto.
- **🔴 H217:** `cloud/auth.py:278-288` `AuthManager.save_to_file` persists provider credentials as **plaintext JSON** (`path.write_text(json.dumps(creds.to_dict()…))`) with no `0600`/keyring/encryption (secret-at-rest); tokens also loaded from `UMEROS_IBM_TOKEN`/`UMEROS_IONQ_KEY`/`UMEROS_AWS_*`/`UMEROS_RIGETTI_KEY` env vars. Current `__repr__` is safe (no secret leak) — keep it.
- **🔴 H221:** `quantum_server.py:76-82,490-494` FastAPI binds **`0.0.0.0:8420`** with **wildcard CORS `allow_origins=["*"]` + `allow_credentials=True`** and **NO authn/authz on any endpoint** (`/api/simulate`, `/api/transpile`, `/api/algorithms/{name}/run`, `/api/status`, `/api/noise-model`, `/api/pulse/validate`, `/api/qasm/export`) — unauthenticated network surface, no zero-trust gate (H184/H198/H205 family).
- **🟡 H218:** `cloud/auth._http_request` (L69-109) provider OAuth2/REST via `urllib.request.urlopen` with no TLS cert pinning → MITM can capture provider tokens. **🟡 H219:** `qkd.key_reconciliation` (L354-383) is a toy — flips Bob's bits to match Alice (`reconciled_bob[i] = 1 - reconciled_bob[i]`), forcing QBER≈0 and defeating QKD's security argument. **🟡 H220:** `qrng`/`QuantumEntropy` are simulators deriving bytes from a seedable statevector measurement (NOT true entropy) — security callers get deterministic PRNG.
- **💭 H222:** CLI `--token`/`-t` passes provider tokens as plaintext argv (visible in `ps`/history). **💭 H223:** 2nd Apache-2.0 stray (`quantum_sim.py:20`, H7). **💭 H224:** `IBMBackend.__init__` `print(f"IBM Backend init warning: {e}")` may leak token/URL text. **💭 H225:** provider REST tokens (IBM `Bearer`/IonQ `apiKey`/AWS `AWS4-HMAC-SHA256`/Rigetti `Bearer`) sent in clear, no TLS pinning (overlaps H218).
- **Standard bumped to v1.32** (header + `quantum/`, §4.4 `quantum/` bullet, §9 H215–H225 rows, §11 `quantum/` quick-ref; 625 lines). Total hotspots now **H1–H225**. Remediation pass re-offered (**16th**).

## root/ folder study (v1.33 of the standard)
- User directed study of `root/` — the **FHS `/root` admin-home manager**: **9 Python modules (~87 KB)** (`__init__`, `__main__`, `home`, `dotfiles`, `shell`, `mail`, `safety`, `passwd`, `fhs`) + `README.md`. Pure-Python: resolve root's home from /etc/passwd UID 0 (fallback to HOME then `/`), bootstrap dir @0700, materialise hardened dotfiles, manage `~/.forward`, audit PATH/LD_*/.ssh/history.
- **Zero dangerous calls** (grep across all 9 files = ZERO `subprocess`/`eval`/`exec`/`shell=True`/`os.system`/`pickle`/`ctypes`/`yaml.load`/`__import__`/`tarfile`/`shutil.rmtree`). Every `os.chmod` uses safe modes (0700/0644/0600). `shell.py` strips `LD_PRELOAD`/`LD_AUDIT`/`GCONV_PATH` + `shlex.quote`s its `export` lines; `safety.py` is a model **read-only, severity-ranked** auditor (PATH001/002/003, LD_*, HIST/SSH/STATE). Disciplined — like `lib/`/`proc/`/`quantum/`.
- **🟡 H226 (H7):** 10 Apache-2.0 strays — all 9 `.py` carry docstring `GPL-3.0 (GNU General Public License Version 3)` AND `README.md:78` declares "GPL-3.0 (GNU General Public License Version 3) - same as the rest of Umer OS". **Largest single-folder Apache cluster in the tree** (surpasses lib/ H149), reinforcing the GPL-3.0 canonical decision.
- **🟡 H227 (cap-gate family):** `passwd.py:131-143` `PasswdManager.write()` (backs up + overwrites `/etc/passwd`) and `passwd.py:175-190` `CanonicalRootBuilder.upsert()` rewrite the privileged passwd file with **no `CapabilityManager` gate and no root-uid check** (H27/H28/H56/H92/H110/H156/H166/H205/H206 family). On real Linux the OS EACCES would stop a non-root caller, but the zero-trust model requires an explicit capability gate.
- **🟡 H228:** `mail.py:207` `RootMailForwarder.ensure()` does `self.home.mkdir(parents=True, exist_ok=True)` (default umask ≈0755) then only tightens `.forward` to 0600 — if `ensure()` runs on a missing home (before `RootHomeManager.ensure()`), the `/root` parent is left world-readable/executable.
- **💭 H229:** `passwd.py:136` `write()` copies `.bak` via `bak.write_bytes(self.path.read_bytes())` with default umask → potentially world-readable backup exposing passwd names/shells/GECOS (not hashes). **💭 H230:** `home.py:384-393` `RootHomeManager.ensure()` `os.chmod` follows symlinks (changes target perms) and the CLI `ensure [path]` accepts an arbitrary path (symlink-target redirect risk). **💭 H231:** `dotfiles.py:232,240-283` `register_template(name)` → `ensure()` writes `home/name`; a name with `/`/`..` escapes the home (caller-cooperation required, low risk, unvalidated).
- **Standard bumped to v1.33** (header + `root/`, §4.4 `root/` bullet, §9 H226–H231 rows, §11 `root/` quick-ref). Total hotspots now **H1–H231** (3 🟡 + 3 💭 for root/, no new 🔴). Cross-cutting remediation pass re-offered (**17th**). Also consolidated/trimmed `MEMORY.md` (was truncated during injection) — folded in root/, bumped 16×→17×, pruned the duplicate verbose 🔴 enumeration.

## scripts/ folder study (v1.35 of the standard)
- User directed study of `scripts/` — **2 dev/CI tooling scripts, 1,736 bytes** (`install_deps.py`, `test_endpoint.py`). NOT a shipped runtime; helpers only.
- **Bright spot:** both use `subprocess` **injection-safe** (argv-list, no `shell=True`) — consistent with `network/tcp_server.py`. grep found ZERO `eval`/`exec`/`os.system`/`pickle`/`ctypes`/`__import__`/`tarfile`/`shutil.rmtree`/`os.mknod`/`os.chmod`/`CapabilityManager`.
- **🟡 H236 (H7):** missing GPL-3.0 header in BOTH files (grep = zero GPL/Apache/Copyright/Licence/License strings) — "missing-header" variant like sbin H232.
- **🟡 H237:** `test_endpoint.py:18` `generate_test_jwt()` hardcodes `secret = "test-secret"` (HS256) — the **SAME** trusted secret as `drivers.driver_service` (🔴 H64 static JWT `"test-secret"` + unauth `/metrics`). The test harness *relies on* and *legitimates* the broken auth: it would PASS against the vulnerable service, masking H64; copying the pattern re-introduces the weak secret.
- **💭 H238:** `test_endpoint.py` `start_app()` does a fixed `time.sleep(3)` readiness wait (flaky on slow CI, no health probe) and `main()` hits `/cpuinfo` with a forged `Bearer` token, bypassing whatever `CapabilityManager`/auth the real app would enforce (only "works" because the service is unauthenticated, H64).
- **💭 H239:** `install_deps.py:6` `subprocess.run([…"pip","install","-r","requirements.txt"], capture_output=True)` — unpinned install (supply-chain: compromised/edited `requirements.txt` installs arbitrary pkgs as current user) + `capture_output=True` buffers the whole log in memory. Subprocess use is injection-safe.
- **Standard bumped to v1.35** (header + `sbin/`+`scripts/`, §4.4 `scripts/` bullet L173, §9 H236–H239 L541–544, §11 `scripts/` quick-ref L646; 648 lines). Total hotspots now **H1–H239** (2 🟡 + 2 💭 for scripts/, no new 🔴). MEMORY.md updated: `scripts/` bullet, remediation 18×→19×. Cross-cutting remediation pass re-offered (**19th**).

## sdk/ folder study (v1.36 of the standard)
- User directed study of `sdk/` — a **developer SDK**: **3 Python modules (~13 KB)** (`__init__`, `app_template`, `build_tool`). `UmerApp` app base class (lifecycle hooks + `read_file`/`write_file`/`send_ipc`/`ask_ai` delegated to a bound `kernel_api`) + `BuildTool` scaffolder/packager + a `HelloWorldApp` example.
- **Clean dangerous-call hygiene AND a good zero-trust delegation pattern**: grep found ZERO `subprocess`/`eval`/`exec`/`os.system`/`pickle`/`ctypes`/`__import__`/`tarfile`/`shutil.rmtree`/`os.mknod`/`os.chmod`/`CapabilityManager`. Every FS/IPC/AI op goes through `self._kernel_api` behind `hasattr` guards that raise `RuntimeError("Kernel API not bound.")` if unbound — the SDK never touches the FS directly (a model for other packages).
- **🟡 H240 (H7):** missing GPL-3.0 header in ALL 3 files (grep = zero GPL/Apache/Copyright/Licence/License strings) — "missing-header" variant, like sbin H232 / scripts H236.
- **🟡 H241:** `build_tool.scaffold`/`package` interpolate unvalidated `app_name` into a VFS path (`/sdk/projects/{app_name}/...`, L25/71) — a name with `/`/`..` escapes `/sdk/projects/`, so the SDK performs an unvalidated path build (VFS-scoped, lower blast radius than a real FS, but no normalization/validation).
- **💭 H242:** `scaffold(custom_app_code=…)` writes caller-supplied arbitrary code verbatim into `src/main.py` via `vfs.write_file` (untrusted-code templating, caller-cooperation assumed; becomes arbitrary code placement if the VFS is ever real-FS-backed).
- **💭 H243:** no `from __future__ import annotations` in any module, `__init__.py` is a bare re-export with no `__all__`, and there is no `_selftest()`/test.
- **Models to copy:** `app_template.UmerApp` kernel-API delegation with `hasattr` + `RuntimeError` guard (zero-trust access, never direct FS). **Fix order:** add GPL headers (H7) → validate/normalize `app_name` (reject `/`/`..`, resolve under `/sdk/projects`) → sanitize/limit `custom_app_code` (or document as trusted-developer input) → add `__all__` + `_selftest()`.
- **Standard bumped to v1.36** (header + `sdk/`, §4.4 `sdk/` bullet L175, §9 H240–H243 L547–550, §11 `sdk/` quick-ref L653; 655 lines). Total hotspots now **H1–H243** (2 🟡 + 2 💭 for sdk/, no new 🔴). MEMORY.md updated: `sdk/` bullet, remediation 19×→20×. Cross-cutting remediation pass re-offered (**20th**).

## security/ folder study (v1.37 of the standard)
- User directed study of `security/` — the **OS security subsystem**: **14 Python modules (~127 KB)** (6 top-level: `__init__`, `crypto_engine`, `firewall`, `sandbox`, `security`, `tls_utils`; + `antivirus/` 8-module subpackage: `__init__`, `engine`, `scanner`, `signatures`, `heuristics`, `quarantine`, `realtime`, `api_server`). Scope: crypto, secure boot, IPC auth, AI behavioral monitor, firewall, AV (scan/quarantine/realtime), hardened TLS, and an aiohttp API server.
- **Mixed hygiene — real crypto + real TLS, but security-specific false claims.** grep across all 14 files found **ZERO** `subprocess`/`eval`/`exec`/`os.system`/`pickle`/`ctypes`/`__import__`/`yaml.load`/`tarfile`/`shutil.rmtree`/`os.mknod` (same disciplined dangerous-call hygiene as lib/proc/quantum). Real, non-trivial crypto is present: `crypto_engine.CryptoEngine` = AES-256-GCM (imports `cryptography`, raises `RuntimeError` if missing — no insecure fallback), PBKDF2-HMAC-SHA512 `derive_key`, HMAC-SHA512 `sign`/`verify`, `os.urandom(32)` ephemeral master key; `tls_utils` = strict `create_ssl_context` (CERT_REQUIRED + hostname check + TLS1.2 floor + safe ciphers), secret from env + `/run/secrets`, async `SecureHTTPClient` (httpx).
- **🔴 H244 (fail-open):** `security.SecureBoot(strict_mode=False)` default → **allow-unknown** boot images accepted (fail-open); hash check uses real SHA3-256 streaming + `hmac.compare_digest` (good) but the default trust posture is permissive. `IPCAuthenticator` signs/verifies with HMAC-SHA3-256 but the **anti-replay nonce/timestamp is commented out** (H247).
- **🔴 H245 (cap-gate / unauth API):** `antivirus/api_server.py` `web.run_app(host="127.0.0.1", port=9095)` exposes **destructive endpoints with NO authn/authz** — `POST /api/quarantine`, `/api/quarantine/delete`, `/api/scan/directory`, `/api/realtime/watch`. localhost-only (blast radius limited) but any local process can trigger quarantine/delete.
- **🔴 H246 (dummy/masquerading):** `sandbox.SecuritySandbox` is **inert** — `resolve_path` jail (realpath + commonpath) is correctly written but **nothing enforces it**; `verify_signature` is SHA3-512 **hash-equality with no key** (mislabeled, H249); ~200 LoC commented-out dead code. Docstrings claim zero-trust isolation the code does not provide (masquerading control — mirrors kernel H110/H112).
- **🟡 H247:** `IPCAuthenticator` docstring/comments claim anti-replay + nonce; implementation has **no nonce/timestamp** and the replay guard is commented out (HMAC verify still accepts replays). **🟡 H248:** `AIBehavioralMonitor` decorative — 4-entry static rule set, `_trigger_quarantine` only `log.critical` (no real quarantine). **🟡 H249:** `SecuritySandbox.verify_signature` compares SHA3-512 hashes with no key → not signature verification; name misleading. **🟡 H250:** `AIFirewall` toy — `analyze_packet` only blocks `dst_port==22`; `add_rule` appends to a list nothing reads; merged from "Deepseek code".
- **🟡 H251:** `antivirus/quarantine.QuarantineManager.restore_file` writes to `entry.original_path` (absolute, trusted) via `os.makedirs(os.path.dirname(original_path), exist_ok=True)` (path-traversal / arbitrary-write if original_path attacker-influenced) and `quarantine_file` **XOR-obfuscates then `os.remove`s the original (irreversible)**. **🟡 H252:** `heuristics.analyze` does `content = f.read()` with **no size cap** (whole-file memory-DoS) + Windows/PE-centric pattern set. **🟡 H253:** `signatures.SignatureDatabase` builtin DB ships **fake/placeholder hashes** (Zeus `9b86c3b…`, CryptoMiner `a1b2c3d4…`); EICAR/WannaCry real; `_load_custom_signatures` swallows `JSONDecodeError`/`KeyError` with `pass` (fail-open). **🟡 H254 (H7):** missing GPL-3.0 header in **ALL 14 modules** (grep = zero GPL/Apache/Copyright/Licence/License strings; only false hit = `MEM_COMMIT`→"MIT" in heuristics) — "missing-header" variant like sbin H232 / scripts H236 / sdk H240.
- **💭 H255:** `realtime.RealtimeMonitor` uses **polling** `os.walk`+`mtime` every 2s (not FS-events) + deprecated `asyncio.get_event_loop()`. **💭 H256:** ~900 LoC commented-out dead code across `security.py`/`sandbox.py` + no `_selftest` anywhere in the package. **💭 H257:** `tls_utils` doc claims "TLS1.3-first" but min is TLS1.2, and `run_uvicorn_secure` defaults `host="0.0.0.0"`. **💭 H258:** `CryptoEngine` master key is **ephemeral `os.urandom(32)` regenerated each process** (no KMS/persistence) + `print()`s on init (leak-adjacent).
- **Bright spot / model to copy:** `tls_utils` is the **best-engineered file in the package** (hardened TLS, secret-from-`/run/secrets`, async client, has `from __future__`). `antivirus/scanner._hash_file` (real md5+sha256) and `security.compute_sha3_256/512` are correct. **Fix order:** H245 auth gate on AV API → H244 SecureBoot strict default → H246/H249 wire real isolation + keyed signature → H247 anti-replay → H251 path/restore safety → H254 GPL headers (H7) → H253 real signatures.
- **Standard bumped to v1.37** (header + `security/`, §4.4 `security/` bullet L176, §9 H244–H258 L552–566, §11 `security/` quick-ref L670; 672 lines). Total hotspots now **H1–H258** (3 🔴 + 8 🟡 + 4 💭 for security/, 0 🔴 in sdk/scripts). MEMORY.md updated: `security/` bullet + H244/H245/H246 in Known-🔴 list + Total H1–H258 + remediation 20×→21×. Cross-cutting remediation pass re-offered (**21st**).

## sources/ folder study (v1.38 of the standard)
- User directed study of `sources/` — a **documentation/reference subsystem**: **9 Python modules (~65 KB / 1,724 LOC)** (`__init__`, `bibliography`, `signals`, `glossary`, `source_tree`, `specs_parser`, `manager`, `cli`, `test_sources`) + `README.md`. Scope: FHS/TLDP bibliography, System V signals table (1-31), architecture glossary, kernel-doc specs parser (`proc.txt`/`initrd.txt`/`runlevels`), `/usr/src` skeleton manager, `sources_ctl` CLI, tests.
- **Clean dangerous-call hygiene**: grep across all 9 files found **ZERO** `subprocess`/`eval`/`exec`/`os.system`/`pickle`/`ctypes`/`__import__`/`tarfile`/`shutil.rmtree`/`os.mknod`/`os.chmod`/`CapabilityManager`. Only `os.walk`/`open` reads in `source_tree.py` (`search_source_code` opens files read-only); "mknod" appears only inside glossary *definitions* (not real calls). `signals.SignalDispatcher` is in-process simulated callback dispatch (`pid` cosmetic) — no real cross-process IPC.
- **🟡 H259 (H7):** 8 of 9 modules declare docstring `GPL-3.0 (GNU General Public License Version 3)` (all except `test_sources.py`) — conflicts with adopted GPL-3.0 canonical (Apache-stray cluster: lib H149 / root H226 / security H254 / etc).
- **🟡 H260 (H7 missing-header):** `test_sources.py` has **ZERO** `GPL`/`Apache`/`Copyright`/`Licence`/`License`/`SPDX` strings AND no `from __future__ import annotations` — the only module in the folder with neither a licence nor a future import (like sbin H232 / scripts H236 / sdk H240).
- **🟡 H261:** every module uses **bare absolute intra-package imports** (`from bibliography import ...`) instead of relative (`from .bibliography import ...`), relying on `__init__.py`/`test_sources.py` inserting the package dir at `sys.path[0]` — fragile, pollutes global `sys.path`, and collides with any other top-level `bibliography`/`signals`/`glossary`/`manager`/`cli` module on the path.
- **🟡 H262:** `source_tree.py:25` `DEFAULT_SRC_ROOT = Path("F:/Pension Person Details/UmerOS/usr/src")` hardcodes the dev machine path (Windows-only `os.name=="nt"` branch; non-Windows gets `/usr/src` which needs root to write), and `SourceTreeManager.__init__` → `_ensure_root()` does `mkdir(parents=True, exist_ok=True)` — so merely constructing `SourcesManager()` silently creates the tree under the wrong path on any other machine, and raises `PermissionError` on a non-root Linux `/usr/src`. Side effect at construction time (not behind an explicit action).
- **💭 H263:** `source_tree.py:17` `import shutil` is never referenced (module uses `os`/`Path`/`open`/`os.walk` only) — dead import.
- **💭 H264:** `signals.py` + `README.md` advertise "inter-process dispatching", but `SignalDispatcher` is an in-process, in-memory simulated callback registry (`send_signal` invokes the handler synchronously in the same process). Wording overclaims real cross-process IPC.
- **Standard bumped to v1.38** (header + `sources/`, §4.4 `sources/` bullet L177, §9 H259–H264 L568–573, §11 `sources/` quick-ref L678; 681 lines). Total hotspots now **H1–H264** (4 🟡 + 2 💭 for sources/, 0 🔴). MEMORY.md updated: `sources/` bullet + Total H1–H264 + remediation 21×→22×. Cross-cutting remediation pass re-offered (**22nd**).

## srv/ folder study (v1.39 of the standard)
- User directed study of `srv/` — the **FHS `/srv` site-specific service-data hierarchy manager**: **10 Python modules (~2,352 LOC)** (`__init__`, `service`, `permissions`, `protocols`, `fhs`, `backup`, `hierarchy`, `manager`, `cli`, `test_srv`) + `README.md` + `registry.json` (empty `{}`). Scope: per-service data trees under `/srv/<service>`, POSIX permission profiles (`SecurityProfile` w/ sticky bits), `www`/`ftp`/`git`/`rsync`/`tftp`/`samba`/`nfs` protocol handlers, tar/zip backup + restore, registry-driven management, `srv_ctl` argparse CLI (~214 LOC, safe no-shell).
- **One of the few folders with LIVE dangerous filesystem calls** (unlike the mostly-simulated/clean subsystems studied recently — home/packages/opt/media/mnt/installer are the others). grep across all 10 files found **🔴 `tarfile.extractall` ×1 (no `filter=`), 🔴 `zipfile.extractall` ×1 (no `filter=`), 🔴 `shutil.rmtree` ×3 (destructive), `os.chmod` ×2 (POSIX-gated to `os.name=="posix"`)**, plus hardcoded Windows paths ×3 and `sys.path.insert` ×3.
- **🔴 H265 (CVE-2007-4559):** `backup.py:153-154` `tarfile.open(archive_path,"r:*")` → `tar.extractall(temp_dir)` with no `filter=` → tar path-traversal on hostile archive during restore. Fix: `filter="data"`.
- **🔴 H266 (zip-slip):** `backup.py:157` `zipf.extractall(temp_dir)` with no `filter=` → crafted `../../` entry escapes `temp_dir`. Fix: `filter="data"` (or member-containment check).
- **🔴 H267 (cap-gate):** `backup.py:181` `restore_backup` does `shutil.rmtree(dest_folder)` when `overwrite=True` — destructive delete with no `CapabilityManager`/`root-uid` check. Fix: capability-gate the destructive delete.
- **🔴 H268 (cap-gate):** `hierarchy.py:275-290` `delete_service_tree` → `if not force: raise PermissionError(...)` then `shutil.rmtree(target)`; `force` is a plain CLI boolean, not a capability, so `srv_ctl remove --force` deletes an arbitrary resolved tree with no zero-trust gate (H27/H92/H110/H205 family). Fix: require a real capability, not `force`.
- **🟡 H269 (H7):** 9 of 10 modules declare docstring `GPL-3.0 (GNU General Public License Version 3)` (`__init__`,`service`,`permissions`,`protocols`,`fhs`,`backup`,`hierarchy`,`manager`,`cli`) — Apache-stray cluster; `test_srv.py` has no licence (H275).
- **🟡 H270 (H262 family):** hardcoded Windows dev paths — `fhs.py:51 DEFAULT_SRV_ROOT = Path("F:/Pension Person Details/UmerOS/srv")`, `backup.py:37 DEFAULT_BACKUP_DIR = Path("F:/Pension Person Details/UmerOS/var/backups/srv")`, `manager.py:49 DEFAULT_REGISTRY_PATH = Path("F:/Pension Person Details/UmerOS/srv/registry.json")` (Windows-only `os.name=="nt"` branch) — + `__init__`-time mkdir side effects (`hierarchy.py:80` mkdir; `backup.py:65` mkdir; `manager.py:57-71` SrvHierarchy+SrvBackupManager+load_registry+auto_discover). Fix: runtime/arg resolve, move mkdir out of `__init__`.
- **🟡 H271 (H261 family):** fragile absolute intra-package imports (`from fhs import ...`) + `sys.path.insert` hacks (`__init__.py:30-32`, `test_srv.py:16-21`). Fix: relative imports + drop sys.path hacks.
- **🟡 H272:** `protocols.py:211-226` `generate_nfs_export_line`/`generate_samba_share_section` interpolate unvalidated `client`/`options`/`share_name`/`comment` into `/etc/exports`/`smb.conf` → config-injection (e.g. `client="*(rw,no_root_squash)"`, newline in `comment` adds a share). Fix: IP/CIDR allowlist + quote/escape + options allowlist.
- **🟡 H273 (cap-gate family):** `permissions.py:139-171` `apply_profile` does real `os.chmod` (POSIX-gated) to set per-protocol modes; advisory, no `CapabilityManager` gate (cap family H227/H233). `audit_service` (173-225) walks trees flagging world-writable/no-sticky + world-readable-secret — read-only, keep that way; gate the chmod when real-wired.
- **💭 H274:** `protocols.py:64-78` `WWWServiceHandler.start_test_server` binds `socketserver.TCPServer(("127.0.0.1", port), ...)` (good, localhost-only) but serves arbitrary `doc_root` with no auth — dev test server, low risk.
- **💭 H275 (H7 missing-header):** `test_srv.py` has ZERO licence strings AND no `from __future__` (like sbin H232 / scripts H236 / sdk H240 / sources H260).
- **💭 H276:** `service.py:15` unused `import json` (dead import).
- **💭 H277:** `permissions.py:38` unused `from typing import ... Tuple` (dead import).
- **Standard bumped to v1.39** (header + `srv/`, §4.4 `srv/` study-notes bullet L178, §9 H265–H277 L575–587, §11 `srv/` quick-ref L693; 694 lines). Total hotspots now **H1–H277** (4 🔴 + 5 🟡 + 4 💭 for srv/, the 4 🔴 being live extract/rmtree calls — the first new 🔴 since `security/` H244–H246). MEMORY.md updated: `srv/` bullet + Known-🔴 add H265/H266/H267/H268 + Total H1–H277 + remediation 22×→23×. Cross-cutting remediation pass re-offered (**23rd**).

## tmp/ folder study (v1.40 of the standard)
- User directed study of `tmp/` — the **FHS `/tmp` transient-files manager**: **11 Python modules (~65 KB / ~2,000 LOC)** (`__init__`, `fhs`, `hierarchy`, `secure_io`, `lockfile`, `reaper`, `permissions`, `tmpfs`, `manager`, `cli`, `test_tmp`) + `README.md`. Runtime artifacts present (`.font-unix`/`.ICE-unix`/`.rpc-unix`/`.systemd-private`/`.Test-unix`/`.X11-unix` + `user/`) treated as generated, not source. Scope: FHS `/tmp` (transient files, protected UNIX socket dirs, per-user `/tmp/user/<uid>` runtimes), race-free process locks, tmpwatch/systemd-tmpfiles reaper, in-memory TmpFS (RAM-disk w/ quotas), `tmp_ctl` CLI.
- **Best-engineered folder studied recently for secure-temp discipline.** grep across all 11 files found **ZERO** `subprocess`/`eval`/`exec`/`os.system`/`pickle`/`__import__`/`tarfile`/`zipfile`/`CapabilityManager`; the only `shutil.rmtree` is `secure_io.SecureTempDir.__exit__` cleaning up its OWN just-created temp dir (safe, self-scoped). `secure_io` uses `secrets` + `os.O_CREAT|os.O_EXCL|os.O_NOFOLLOW` (race-free, symlink-proof) at 0600/0700 modes — a model to copy. Reaper correctly excludes `PROTECTED_SOCKET_DIRS`; `permissions.audit_security` checks sticky-bit + world-writable; `tmpfs` enforces a real byte quota (contrast H252 heuristics whole-file read). `test_tmp.py` uses proper `from tmp.X import ...` + `tempfile.TemporaryDirectory`.
- **0 🔴 + 6 🟡 + 4 💭** (NO new 🔴 — unlike `srv/`):
  - **🟡 H278 (H7):** 10 of 11 modules declare `GPL-3.0 (GNU General Public License Version 3)` (`__init__`,`fhs`,`hierarchy`,`secure_io`,`lockfile`,`reaper`,`permissions`,`tmpfs`,`manager`,`cli`); `test_tmp.py` has none (H284).
  - **🟡 H279 (H262 family):** hardcoded Windows `fhs.py:50 DEFAULT_TMP_ROOT = Path("F:/Pension Person Details/UmerOS/tmp")` (centralized in `fhs`, better than `srv`'s 3 spots) + `__init__`-time mkdir: `TmpHierarchy.__init__`→`_ensure_root()` mkdir+chmod 1777, `TmpManager.__init__`→`bootstrap()` mkdirs, `get_default_tmp_manager()` triggers bootstrap on first call.
  - **🟡 H280 (H261 family):** fragile absolute intra-package imports (`from fhs import ...`/`from manager import ...`) + `sys.path.insert` hacks (`__init__.py:34-35`, `test_tmp.py:19-22`).
  - **🟡 H281 (cap-gate family, 🟡 not 🔴):** `reaper.py:86-202` `clean_by_age`/`clean_on_boot`/`clean_by_quota` delete files by age/quota (legitimate tmpwatch job) and exclude protected socket dirs, but `tmp_root` is any caller path with **no validation it is real `/tmp`** and **no `CapabilityManager` gate**; misconfigured/attacker-influenced `tmp_root` reaps arbitrary files. Milder than `srv/` whole-tree `rmtree` 🔴 (bounded by age/quota + excludes protected dirs) → 🟡.
  - **🟡 H282 (H241 family, CWE-22):** `tmpfs.py:129-138` `sync_to_disk` writes `dest = target_path / name` with no validation; a node named `../../etc/cron.d/x` escapes `target_dir` → arbitrary file write.
  - **🟡 H283 (cap-gate family):** `permissions.py:116-142` `enforce_permissions` does `os.chmod(tmp_root, 0o1777)`+socket dirs (correct secure mode) but **no `CapabilityManager` gate** (advisory, privileged op).
  - **💭 H284 (H7 missing-header):** `test_tmp.py` ZERO licence strings + no `from __future__` (like sbin H232/scripts H236/sdk H240/sources H260/srv H275); but tests correctly use `from tmp.X import`.
  - **💭 H285:** dead imports — `import shutil` unused in `hierarchy.py`/`reaper.py`/`manager.py`/`test_tmp.py` (only `secure_io` uses it), `import json` unused in `test_tmp.py`, `from typing import ... Tuple` unused in `secure_io.py`/`hierarchy.py`/`fhs.py` (PEP-604 unions).
  - **💭 H286:** `lockfile.py:59-63` lazy `import ctypes` + `ctypes.windll.kernel32.OpenProcess/CloseHandle` (Windows PID check) — works, couples to Win32, low risk (no code load).
  - **💭 H287:** `manager.py:167-174` global singleton `get_default_tmp_manager` triggers `bootstrap()` mkdir/chmod as hidden init side effect (related to H279, global-API layer).
- **Standard bumped to v1.40** (header + `tmp/`, §4.4 `tmp/` study-notes bullet, §9 H278–H287, §11 `tmp/` quick-ref). Total hotspots now **H1–H287** (0 🔴 + 6 🟡 + 4 💭 for tmp/, no new 🔴). MEMORY.md updated: `tmp/` bullet + Total H1–H287 + remediation 23×→24×. Cross-cutting remediation pass re-offered (**24th**).

## tools/ folder study (v1.41 of the standard)
- User directed study of `tools/` — a **standalone OS installer script**: **1 Python module (~967 B / 28 LOC)** (`installer.py`). NOT a package (no `__init__.py`). Scope: `show_license()` prints an EULA then blocks on `input("Press ENTER to accept...")`; `install_umer_os(target_dir="/umer_os")` does `os.makedirs(target_dir, exist_ok=True)` and copies a fixed allowlist of source subtrees (`kernel`/`quantum`/`ai`/`security`/`ui`/`compatibility`/`filesystem`/`update`/`drivers`/`bootloader`) into `target_dir` via `shutil.copytree` (pre-clearing an existing `dst` with `shutil.rmtree(dst)`).
- **Clean dangerous-call surface** — grep found **ZERO** `subprocess`/`eval`/`exec`/`os.system`/`pickle`/`ctypes`/`__import__`/`tarfile`/`zipfile`/`os.chmod`/`CapabilityManager`/`sys.path`/Windows paths. The only FS mutations are `shutil.rmtree(dst)` (pre-copy cleanup of `target_dir/<allowlisted-name>`) + `shutil.copytree(src, dst)`. No live extract/SSRF/secret calls.
- **0 🔴 + 5 🟡 + 3 💭** (no new 🔴 — the one destructive call is bounded to allowlisted subdirs of `target_dir`):
  - **🟡 H288 (cap-gate family, 🟡 not 🔴):** `installer.py:21` `shutil.rmtree(dst)` recursive delete of `target_dir/<allowlisted-name>` with **no `CapabilityManager` gate** (cap family H227/H233/H267/H273); bounded to the fixed source-name allowlist under `target_dir`, so milder than `srv/`'s whole-tree 🔴 rmtree.
  - **🟡 H289 (H262 family):** `installer.py:13` hardcoded default `target_dir="/umer_os"` — a Unix absolute root baked into the signature that ignores env/arg and would clobber `/umer_os` on any Linux host unless overridden (project path, not a dev-machine path, contrast srv/tmp H270/H279).
  - **🟡 H290:** `installer.py:16-22` `src` names resolved **CWD-relative** (`os.path.exists(src)` + `shutil.copytree(src, dst)`) instead of `os.path.dirname(__file__)`/explicit source root — run from the wrong directory it copies whatever is nearby or silently skips (fail-open, wrong-dir copy risk).
  - **🟡 H291 (H7 missing-header):** `installer.py:1-3` opens with only `import os`/`import shutil`/`import logging` — grep ZERO `GPL`/`Apache`/`Copyright`/`Licence`/`License`/`SPDX` AND no `from __future__ import annotations` (like scripts H236 / sbin H232 / sdk H240 / sources H260 / srv H275 / tmp H284).
  - **🟡 H292:** `installer.py:10` `input()` EULA block is interactive/non-automation-friendly and the EULA text is "AS IS" with no GPL-3.0 reference and no real consent capture (per authoritative design source the installer EULA is required; this one is minimal + not GPL-aligned, blocks headless/CI).
  - **💭 H293:** `installer.py:22` `copytree` **fails open** — a missing source subtree is skipped by the `if os.path.exists(src):` guard with no error/warning, so the install can complete with entire components absent (silent partial install).
  - **💭 H294:** `installer.py:12,17` `logging.info` used with **no `logging.basicConfig`/handler** — all log records silently dropped (observability gap).
  - **💭 H295:** whole module has no docstring/`__all__`/type hints on `install_umer_os(target_dir: str = ...)` (per-file baseline gap).
- **Standard bumped to v1.41** (header + `tools/`, §4.4 `tools/` study-notes bullet L180, §9 H288–H295 L599–606, §11 `tools/` quick-ref L715). Total hotspots now **H1–H295** (0 🔴 + 5 🟡 + 3 💭 for tools/, no new 🔴). MEMORY.md updated: `tools/` bullet + Total H1–H295 + remediation 24×→25×. Cross-cutting remediation pass re-offered (**25th**).

## usr/ folder study (v1.42 of the standard)
- User directed study of `usr/` — the **FHS `/usr` hierarchy manager**: **61 Python modules (~22,907 LOC)** + `src/`. Scope: binaries, libs (`lib`/`lib64`/`libexec`), share data (dict/games/terminfo/ICC/PPD/dconf/NLS/XML/XDG/Spool/misc), locales/man, X11/Wayland, sendmail, BSD-compat, user-help, plus privileged FHS managers (`lib`/`sbin`/`rpm`/`gcc`/`x11`) + kernel-interface modules (`io_uring`/`landlock`/`seccomp`/`ntsync`/`iommufd`/`dmabuf`/`vduse`/`tee`/`netlink`/`procfs`/`sysfs`/`syscalls`).
- **MIXED folder** — ~20 LIVE modules perform REAL filesystem writes at system paths; ~41 are SIMULATED (in-memory dicts/prints). LIVE hygiene is mostly clean: grep found **ZERO** `subprocess`/`eval`/`exec`/`os.system`/`pickle`/`ctypes`/`__import__`/`tarfile`/`zipfile` (NO srv-style archive-extraction 🔴); the only destructive call is `shutil.rmtree` in **3** modules, all scoped to a manager-owned `entry.path` under its `BASE_DIR`: `bsd_compat_manager.py:230`, `games_data_manager.py:239`, `xml_manager.py:223`. No `sys.path` hacks; `usr/` ships **no `_selftest()`/tests**.
- **Read ground-truth on 2 LIVE modules:** `sendmail_manager.py` does `os.symlink(target, /usr/lib/sendmail)` + `os.unlink(/usr/sbin/sendmail)` with NO `CapabilityManager` gate, NO target validation, all in broad `except Exception: return False` (fail-open), NO GPL header; `misc_data_manager.py` `__init__`→`_refresh` does `mkdir(/usr/share/misc)` (construction-time side effect) + `open('w')` writes + `os.symlink` for `/usr/share/misc/magic`→`/usr/share/file/magic`. `io_uring.py`/`landlock.py` are in-memory enums (NO real syscall wiring).
- **0 🔴 + 5 🟡 + 2 💭** (no new 🔴):
  - **🟡 H296 (cap-gate family):** privileged FS ops (`os.symlink`/`os.unlink`/`open('w')`/`shutil.rmtree`) with **no `CapabilityManager` gate** — `sendmail_manager.py:185,195`, `misc_data_manager.py:672,686-739`, `bsd_compat_manager.py:230`, `games_data_manager.py:239`, `xml_manager.py:223` (cap family H227/H233/H267/H273/H281/H283).
  - **🟡 H297 (H262 family):** construction-time `mkdir` side effects — managers create system dirs (`/usr/share/misc`,`/usr/share/dict`,`/usr/share/games`,`/usr/share/dconf`,...) at `__init__`/`_refresh` with no cap gate/opt-in (`misc_data_manager.py:589`, `bsd_compat:101`, `dict:171`, `games_data:120-121`, `dconf:73-75`, `icc_profile:111-112`, `nls:86`, `ppd:110`, `tmac:106`, `user_help:127`, `terminfo:111`, `wayland:102-103`, `xdg:73-74`, `xml:93`). Hidden side effect; raises off non-root Linux; wrong-place trees off Linux.
  - **🟡 H298:** fail-open broad `except Exception: return False` around privileged ops (`sendmail_manager`, `misc_data_manager`, `bsd_compat:220,235`, `games_data:229,244`, `xml:213,228`) — security-relevant failures silently swallowed.
  - **🟡 H299 (H7, largest missing-header cluster in tree):** only 3/61 modules (`kernel_source_manager`, `rpm_manager`, `source_manager`) carry any `GPL`/`Copyright`/`SPDX`/`Licence`/`License` string; **58/61 ship no licence at all** (like sbin H232 / scripts H236 / sdk H240 / sources H260 / srv H275 / tmp H284 / tools H291).
  - **🟡 H300 (H262/H270/H279 family):** hardcoded absolute `/usr/...` system paths baked into class constants (`/usr/lib/sendmail`, `/usr/sbin/sendmail`, `/usr/share/misc`, `/usr/share/file/magic`, ...) — not runtime/arg-resolved; combined with import-time `mkdir` (H297) = non-portable + wrong-place creation.
  - **💭 H301 (like sbin H235):** SIMULATED privileged/kernel-surface modules masquerade as functional — `lib`/`sbin`/`rpm`/`gcc`/`x11` managers are in-memory registries (no real syscalls); `io_uring`/`landlock`/`seccomp`/`ntsync`/`iommufd`/`dmabuf`/`vduse`/`tee`/`netlink`/`procfs`/`sysfs`/`syscalls` define constants/enums but have NO real syscall wiring — they imply OS access that does not exist (false-state risk).
  - **💭 H302:** per-file baseline gaps — many modules lack `from __future__`/docstring/`__all__`/type hints, use lazy `import shutil` inside functions; `usr/` ships **no `_selftest()`/test module at all** (contrast srv/tmp `test_*.py`).
- **Standard bumped to v1.42** (header + `usr/`, §4.4 `usr/` study-notes bullet L181, §9 H296–H302 L609–615, §11 `usr/` quick-ref L724). Total hotspots now **H1–H302** (0 🔴 + 5 🟡 + 2 💭 for usr/, no new 🔴). MEMORY.md updated: `usr/` bullet + Total H1–H302 + Known-🔴 unchanged (usr/ adds 0 🔴) + remediation 25×→26×. Cross-cutting remediation pass re-offered (**26th**).

## var/ folder study (v1.43 of the standard)
- User directed study of `var/` — the **FHS `/var` variable-data hierarchy manager** (`LogManager` @ `/var/log`, `SpoolManager` @ `/var/spool`, `VarDirectoryManager` @ `/var/local,/var/lock,/var/opt,/var/run,/var/tmp`).
- **Scope:** 4 Python modules (~706 LOC) + 2 data files (`lib/umeros/consent_ledger.json`, `log/umeros_initrd.log`). **Smallest AND only 100% LIVE folder studied** — every manager does real FS writes; no SIMULATED modules (contrast `usr/` which was MIXED ~20 LIVE + ~41 SIMULATED).
- **Dangerous-call surface is otherwise clean:** grep found ZERO `subprocess`/`eval`/`exec`/`os.system`/`pickle`/`ctypes`/`__import__`/`tarfile`/`zipfile`/`CapabilityManager`/`sys.path`/Windows paths; only `shutil.rmtree`×3, all scoped to manager-owned children (`cleanup_tmp`, `cleanup_old_items`). NO construction-time `mkdir` (paths are `Path` objects; mkdir on-demand/scoped — better than `usr/` H297). Baseline otherwise good (`from __future__`, type hints, docstrings, logging).
- **Headline 🔴 (H303):** path-traversal across ALL three Var managers. `name`/`username`/`directory`/`filename` joined as `self.<x>_path / param` with no scoping or `realpath`-under-root check; `Path` collapses `..`, so `remove_local_item("../../etc/passwd")`→`unlink("/etc/passwd")`, `VarDirectoryManager.remove_local_item("../etc")`→`shutil.rmtree("/etc")`, and `SpoolManager.set_cron_user("../../etc/cron.d/x", jobs)`→writes `/etc/cron.d/x` = **cron RCE as root**; `LogManager.write_log("../../etc/cron.d/x", …)` = arbitrary append. Read/list variants expose arbitrary content (`read_mailbox("../../etc/shadow")`). CWE-22.
- **🟡 H304:** privileged FS ops (`write_text`/`append`/`unlink`/`rename`/`rmtree`) across all 3 managers with **no `CapabilityManager` gate** (cap-gate family H227/H233/H267/H273/H281/H283/H296). **🟡 H305:** fail-open broad `except Exception: return False` (bare `except Exception: pass` at `directory_manager.py:154`) swallows security-relevant failures. **🟡 H306:** H7 Apache-2.0 strays — `directory_manager.py:14`/`spool_manager.py:13`/`log_manager.py:13` declare `GPL-3.0 (GNU General Public License Version 3)` (conflicts GPL-3.0 canonical; `__init__.py` unlicensed — opposite of `usr/`'s missing-header H299).
- **💭 H307:** `var/` ships **no `_selftest()`/test module**; minor lazy in-function `import shutil` (`spool_manager.py:162`) / `import gzip` (`log_manager.py:150`). Note: `var/lib/umeros/consent_ledger.json` is a real persistence artifact linking to the `legal/` consent ledger (H128–H141) — worth a cross-folder check it's written/read under a capability + not world-readable.
- **Standard bumped to v1.43** (header + `var/`, §4.4 `var/` study-notes bullet L181, §9 H303–H307 L617–622, §11 `var/` quick-ref L731). Total hotspots now **H1–H307** (1 🔴 + 3 🟡 + 1 💭 for var/; H303 is the only new 🔴). MEMORY.md updated: `var/` folder bullet + Total H1–H307 + Known-🔴 adds H303 + remediation 26×→27×. Cross-cutting remediation pass re-offered (**27th**).


## Remediation loop started — var/ cluster fixed (2026-08-22, session 1)
- User directive: fix ALL hotspots (H1-H307) from the standard + MEMORY, tests in `tests/`, `# [FIX Hxxx]` comments, resumable across sessions.
- Built resumable checkpoint `.workbuddy-ai/memory/remediation_progress.md` (307-item checklist from standard §9; 66 RED / 152 YELLOW / 89 BLUE).
- Created isolated pytest venv at `C:/Users/MC Raja Jang/.workbuddy-ai/binaries/python/envs/default`.
- var/ cluster DONE:
  - H303 (RED path-traversal -> cron RCE, CWE-22): new `var/_path_guard.py` (`safe_child` + `PathTraversalError`) wired into `VarDirectoryManager`/`SpoolManager`/`LogManager`; every manager path now contained to its owned root, traversal refused (fail-closed). Headline: `set_cron_user("../../etc/cron.d/x", jobs)` no longer writes outside /var/spool.
  - H305 (YELLOW fail-open): `directory_manager.list_locks` silent `except Exception: pass` -> `log.debug`.
  - H306 (YELLOW H7 licence): `GPL-3.0 (GNU General Public License Version 3)` -> `License: GPL-3.0` in 3 modules + GPL header added to `var/__init__.py`.
  - H307 (BLUE no tests): added `tests/test_var.py` (13 tests pass) incl. security regressions (cron-RCE / arbitrary-append / traversal refusal).
  - H304 (YELLOW cap-gate) DEFERRED to cap-gate family pass (needs CapabilityManager wiring).
- NEXT (per tracker): path-traversal cluster H185/H186/H195/H265/H266/H282 with the same `safe_child` pattern.

## Path-traversal cluster fixed (2026-08-22, session 2)
- Continued the resumable remediation loop beyond var/. Applied the proven `safe_child` CWE-22 guard pattern across the remaining path-traversal RED cluster.
- New **shared** guard `core/path_guard.py`: `safe_child(root, name)` (single-segment strict containment), `safe_join(root, *names)` (multi-segment; splits on `/`+`\`; rejects `..`/stray separators), `PathTraversalError(ValueError)` fail-closed; GPL-3.0 header. (Promoted from `var/_path_guard.py` so every folder shares one canonical guard.)
- Fixes (each carries `# [FIX Hxxx]` comments):
  - H185 (opt/var.py, opt/config.py): file/config names now contained via `safe_child`/`safe_join`; install/get/remove refuse traversal.
  - H186 (opt/manager.py, opt/package.py): `remove()`/`remove_package()` rmtree targets built via contained `_scoped_path`/`safe_child`; traversal names recorded as "Refusing" and the path is skipped (no rmtree).
  - H194/H195 (packages/umer_pkg.py): `dest = safe_child(install_dir, name)`; `tar.extractall(path=dest, members=…, **_FILTER_KW)` with `_FILTER_KW={"filter":"data"}` on Py≥3.12 (none on <3.12); `remove()` only rmtrees paths resolving inside install_dir; `build()` pkg path contained; JSON stores `str(dest)`.
  - H265/H266 (srv/backup.py): tar/zip extraction gets `filter="data"`; `dest_folder = safe_join(target_root, service_name)` refuses traversal.
  - H282 (tmp/tmpfs.py): `sync_to_disk` `dest = safe_join(target_path, name)`, `continue` on `PathTraversalError`.
- Unblocked the test harness (pre-existing blockers that broke collection of the new tests):
  - `packages/__init__.py` referenced non-existent `PackageManager` → `UmerPackageManager`.
  - `tmp/__init__.py` + 7 `tmp/*.py` used top-level `from fhs import`/`from hierarchy import` (fragile absolute imports colliding with `srv`'s same-named modules) → all converted to relative `.fhs`/`.hierarchy`/… (resolves documented H271/H280 smell).
- Tests: new `tests/test_opt.py`; extended `tests/test_packages.py`/`test_srv.py`/`test_tmp.py`. **33 pass** across the 4 files; `test_var.py` (13, session 1) still green.
- Checkpoint `remediation_progress.md`: flipped H185/H186/H194/H195/H265/H266/H282 → `[x]`; NEXT now points to the fail-open + dummy-crypto cluster (H129/H146/H196/H111/H152/H154).
- NEXT cluster: **fail-open + dummy-crypto** — H129 (legal audit fail-open), H146 (lib ssl trust fail-open), H196 (packages "Signed" overstated + verifies fail-OPEN), H111 (kernel CryptoEngine `verify`→`True` dummy), H152 (quantum silent classical downgrade), H154 (OTA hardcoded fake PQC sig).

## Fail-open + dummy-crypto cluster fixed (2026-08-22, session 3)
- Continued the resumable loop. Applied fail-closed + real-crypto fixes across the fail-open / dummy-crypto RED cluster; all source changes carry `# [FIX Hxxx]` comments.
- Source fixes:
  - H111 (kernel/umer_kernel.py `CryptoEngine`): replaced all-dummy crypto with real `AESGCM` encrypt/decrypt + `hmac` SHA-256 `sign`/`verify` (constant-time `compare_digest`); `verify` no longer returns `True` unconditionally. `import hashlib`/`hmac` added.
  - H152 (quantum/crypto_pqc.py `PostQuantumCrypto`): added honest `self.is_post_quantum` (False when `liboqs` missing), `log.warning` on classical fallback, and `assert_post_quantum()` raising `RuntimeError` if not PQC — no silent downgrade.
  - H154 (cloud/ota_updater/update_system.py `UpdateManager`): `__init__(crypto_engine=None, trusted_public_key=None)`; removed hardcoded `"simulated_dilithium_sig_abc123"`; `verify_and_apply` now fail-closed — requires a real `crypto`, `trusted_public_key`, and manifest signature, verifies via `crypto.verify`, otherwise refuses.
  - H196/H197 (packages/umer_pkg.py): new `_package_integrity_hash(manifest_bytes, file_items)` — SHA3-256 over manifest + every `files/` member (sorted, fail-closed); `_verify_hash` now fails CLOSED on missing HASH/manifest and mismatched hash; `build()` computes the full-payload hash and embeds it. `import hmac` added.
  - H146 (lib/ssl_libs.py `_check_is_trusted`): fail-closed CA trust — a cert is trusted ONLY if its fingerprint appears among the certs in a configured CA bundle. Added `_fingerprint_pem_cert` (X.509 DER fingerprint via `cryptography`, whitespace-normalised PEM-block fallback) so candidate and bundle fingerprints compare on identical bytes; `_bundle_fingerprints` uses the same helper; `_inspect_cert.fingerprint_sha256` now uses the canonical fingerprint. (Bug-caught: old code hashed the whole file for the candidate but only the BEGIN→END block for the bundle, and a trailing newline made them diverge — now consistent.)
  - H129 (legal/licenses.py `scan_directory`): fail-closed — a file is compliant only with an explicit SPDX/grant declaration; generic "License"/"Copyright" mentions no longer count; non-compliant files land in `missing_license_files`.
- Harness unblock: `srv/*` (8 modules) fragile top-level sibling imports (`from fhs import`/`from manager import`/…) → relative (`.fhs`, `.manager`, …). This fixed `test_srv.py` collection `ImportError: cannot import name 'SrvManager' from 'manager'` (collision with `legal/manager.py`) — same H271 smell as the `tmp` package fixed last session.
- Venv: `cryptography` (50.x) + `numpy` (2.5.2) were missing from the isolated test venv → installed so kernel/lib/quantum crypto tests can import/run.
- Tests: extended `tests/test_packages.py` (legit-build verify, missing-HASH refusal, tamper detection); NEW `tests/test_ssl.py` (fail-closed trust + no-bundle→False, real self-signed certs); NEW `tests/test_legal_scan.py` (generic-mention fails closed, declaration compliant, missing-count); NEW `tests/test_crypto_engine.py` (sign/verify roundtrip, key-bound, OTA fail-closed); NEW `tests/test_pqc.py` (honest fallback + sign/verify roundtrip). Test bugs fixed: packages `build()` arcnames normalised to POSIX `/` (Windows `os.path.join` emitted `\`, which tarfile reads back as `/` → hash mismatch); `test_ssl.py` cert builder given validity dates (older `cryptography` lacks `_utc` → used naive `not_valid_before`/`not_valid_after`); `test_legal_scan.py` assertions compare on `os.path.basename` (source stores absolute paths).
- Result: **59 passed** (full set: test_packages/test_ssl/test_legal_scan/test_crypto_engine/test_pqc/test_opt/test_srv/test_tmp/test_var).
- Checkpoint `remediation_progress.md`: flipped H129/H146/H111/H152/H154/H196/H197 → `[x]`; NEXT now points to the **cap-gate (wire CapabilityManager)** family — H227,H233,H267,H273,H281,H283,H296,H304.
- MEMORY.md folder map updated: cloud H154(FIXED), kernel H111(FIXED), legal H129(FIXED), lib H146(FIXED), packages H196/H197(FIXED), quantum H152(FIXED); srv sibling imports now relative (H271).
- NEXT cluster: **cap-gate** — wire a shared `CapabilityManager` and gate the ungated privileged entry points: H227 (root passwd write), H233 (sbin `execute()`), H267/H273 (srv backup/permissions), H281/H283 (tmp reaper/permissions), H296 (usr privileged FS), H304 (var deferred). Then full YELLOW/BLUE sweep; outstanding H7 licence sweep (`Licence: GPL-3.0 (GNU General Public License Version 3)`→`License: GPL-3.0`) in opt/config.py, opt/var.py, opt/package.py, srv/backup.py, packages/umer_pkg.py, tmp/tmpfs.py headers.

## Cap-gate cluster CLOSED (2026-08-22, session 5/6)
- Continued the resumable loop; finished the **cap-gate** family (wire `CapabilityManager` to the zero-trust bridge). All 8 hotspots now gated behind `core/capability_gate.py` (`gate` + `gate.require(cap)`), which enforces **fail-closed when a real `CapabilityManager` is wired** (the running kernel) and stays **permissive-with-warning when standalone** (CLI/tests) so nothing regresses; `set_strict(True)` denies with no trust source.
- Source gates (each carries a `# [FIX Hxxx]` comment, mirroring the `srv/backup.py`/`tmp/reaper.py` import+gate pattern):
  - **H296 (usr privileged FS)** — gated in `usr/sendmail_manager.py`, `usr/misc_data_manager.py`, `usr/bsd_compat_manager.py`, `usr/games_data_manager.py`, `usr/xml_manager.py`. All privileged `os.symlink`/`os.unlink`/`open('w')`/`shutil.rmtree` entry points now `gate.require(CAP_FS_ADMIN)` as their first statement.
  - **H304 (var managers)** — gated in `var/directory_manager.py` (10 methods), `var/spool_manager.py` (4: write/clear mailbox, set cron user, cleanup), `var/log_manager.py` (3: write/rotate/compress).
- Two genuine bugs surfaced and fixed while landing H296:
  - `usr/man_page.py` referenced an **undefined `ManPageStatus` enum** → `NameError` crashed `import usr`, blocking the H296 integration tests. Added the missing `IntEnum` (MISSING/PARSED) with a `# [FIX …]` note.
  - `GamesDataManager.add_game_data` was **never gated** (only `remove_game_data` was, from the prior pass). Added the `# [FIX H296]` `gate.require(CAP_FS_ADMIN)` as its first statement.
- Tests: extended `tests/test_cap_gate.py` — gate-level allow/deny for `CAP_FS_ADMIN`, strict-mode deny, a parametrised **per-module fail-closed integration** across all 8 modules (usr managers built via `__new__` to avoid their real `/usr` BASE_DIR `mkdir` side effect; var managers built with a temp `var_path`), and a positive **allow-when-held integration** (var spool actually writes under a temp dir). `test_cap_gate.py` + `test_var.py` = **32 passed**.
- Regression check: ran the full `pytest tests/` — **0 regressions from this work**. However the full suite is currently blocked by **6 PRE-EXISTING collection errors** (unrelated to cap-gate): `tests/quantum/test_circuit*.py`/`test_gates.py` (quantum test modules), `tests/test_dc_v2.py` (`FileNotFoundError: UmerOS/quantum/dynamic_circuits_v2.py` missing source), `tests/test_sbin.py` (`ImportError: cannot import 'IfconfigCommand' from 'network'`), `tests/test_sources.py` (`ImportError: cannot import 'SourcesManager' from 'manager'` — fragile sys.path hack, H261 family).
- Checkpoint `remediation_progress.md`: flipped H296/H304 → `[x]`; NEXT now points at the 6 collection errors, then the full YELLOW/BLUE sweep + outstanding H7 licence sweep (note `srv/backup.py` still carries `Licence: GPL-3.0 (GNU General Public License Version 3)` at line 16 — not yet fixed).
- MEMORY.md folder map: usr/ (H296 FIXED), var/ (H304 FIXED) annotations added; DONE/NEXT updated.
- NEXT cluster: **fix the 6 pre-existing collection errors** so `pytest tests/` collects, then the full YELLOW/BLUE sweep; outstanding H7 licence sweep (`Licence: GPL-3.0 (GNU General Public License Version 3)`→`License: GPL-3.0`) in opt/config.py, opt/var.py, opt/package.py, srv/backup.py, packages/umer_pkg.py, tmp/tmpfs.py headers.

## Collection-error cluster CLOSED (2026-08-22, session 7)
- Continued the resumable loop; closed the **6-collection-error cluster** that was blocking `pytest tests/`
  from collecting. All fixes carry `# [FIX Hxxx]` comments. Ground-truth: the "6 collection errors" were
  actually **4 directly-broken modules** (test_gates/test_circuit/test_circuit_library/test_dc_v2) + **2 that
  only failed in full-suite context** (test_sbin/test_sources) because top-level `network/` and `legal/manager.py`
  were already cached in `sys.modules`.
- Source/test fixes:
  - `quantum/gates.py` (H261): `get_gate` now raises `KeyError` (test `test_get_unknown_raises` expects it);
    added class-style aliases `I,X,Y,Z,H,S,T,CX,CZ,CCX,SWAP` + parametric `RX,RY,RZ,PhaseGate`.
  - `quantum/circuit_library.py` (H261): added `inverse_qft_circuit = qft_inverse_circuit` alias +
    `grover_circuit(num_qubits)` with `_multi_controlled_z` helper (exact 1–3q; >3q via H+CCX+H decomposition
    matching the existing `grover_diffusion_circuit` style); returns a circuit with `size() > 0`.
  - `tests/test_dc_v2.py` (H262): replaced hardcoded `spec_from_file_location('umerOS.quantum.dynamic_circuits_v2',
    r'UmerOS\quantum\dynamic_circuits_v2.py')` (doubled to `UmerOS\UmerOS\quantum\...`) with
    project-root-relative resolution via `os.path.dirname(__file__)`.
  - `sources/__init__.py` + `sources/manager.py` + `sources/cli.py` (H261): removed `sys.path` self-injection;
    converted bare sibling imports to relative (`.bibliography`, `.glossary`, `.signals`, `.specs_parser`,
    `.source_tree`, `.manager`). `# [FIX H261]` explains `from manager import` resolved to `legal/manager.py`.
  - `sbin/__init__.py` + `sbin/sbin_manager.py` (H261): removed `sys.path` self-injection; converted bare
    sibling imports to relative (`.boot`, `.filesystem`, `.modules`, `.network`, `.system`, `.mount`,
    `.maintenance`, `.sbin_manager`). `# [FIX H261]` notes `boot`/`network` collide with top-level packages.
  - `tests/test_sbin.py` (H261): dropped its own `sys.path.insert` injection (would shadow top-level `boot/`/
    `network/` for the rest of the suite) and switched every bare import to qualified `from sbin.X import …`
    (incl. the six `_selftest` imports inside `TestSelftest`).
- Verification:
  - Full `pytest tests/ --collect-only` → **1742 tests collected, 0 errors, exit 0** (was blocked before).
  - The 4 directly-repaired modules PASS; `grover_circuit` / `inverse_qft_circuit` tests pass.
  - `test_cap_gate.py` = 19/19 in isolation (the 2 failures seen in the full run are pre-existing cross-test
    global-state pollution of the capability-gate singleton, NOT a regression).
  - Full run = 1613 passed, 48 skipped, **81 failing** — all 81 are PRE-EXISTING API drift unrelated to these
    edits (quantum Statevector/Instruction drift in test_simulator/operators/primitives/transpiler/circuit/
    circuit_library; test_bin/test_proc/test_legal_scan; + the 2 cap-gate pollution). **Zero regressions.**
- Checkpoint `remediation_progress.md`: NEXT rewritten — collection cluster marked CLOSED, new API-drift
  cluster (81 failures) documented as the next sweep.
- MEMORY.md: collection cluster folded into DONE line; NEXT repointed to the 81-failure API-drift sweep + H7
  licence sweep + full YELLOW/BLUE sweep.
- NEXT cluster: **reconcile the 81 failing tests** (quantum/bin/proc/legal test↔library API drift) — this is a
  large, separate sweep. Then the outstanding H7 licence sweep and the full YELLOW/BLUE sweep.

## 80-failure cluster — reconnaissance (2026-08-22, session 7 cont.)
- Ran the full suite AFTER closing collection: **80 failed, 1614 passed, 48 skipped** (was 81; the
  `test_legal_scan` typo fix flipped 1 → pass). `cap_gate` = 0 failures (the `finally: mod.gate = prev`
  restoration added in session 6 already resolved the prior singleton pollution; the "2 cap_gate failures"
  in the carried summary was from an earlier run).
- Fixed one genuinely-owned test bug: `tests/test_legal_scan.py::test_scan_directory_compliant_with_declaration`
  asserted `compliant_files == 2` but only 1 file is written → corrected to `== 1` (scan_directory logic is
  correct; the assertion was a session-3 typo). 3/3 pass now.
- Taxonomy of the remaining 80:
  - **~68 quantum** (tests/quantum/*): pure API drift. Tests were written against a Qiskit-style API
    (`Statevector.probabilities_dict()`, `SparsePauliOp.from_list()`, `SamplerV2`/`EstimatorV2`/`PrimitiveJob`,
    transpiler `CouplingMap`, circuit `Instruction`/`QuantumCircuit` signatures) but the bespoke UmerOS quantum
    library implements a different, partial API (`probabilities` not `probabilities_dict`, no `from_list`, etc.).
    NOT H-targeted. Reconciling = either (a) rewrite ~68 tests to match the shipped library, or
    (b) re-architect the quantum library to be Qiskit-compatible. Architectural fork — needs a user decision.
  - **~12 test_bin/test_proc** (mixed, NOT all platform):
    - POSIX-only by design (skip on non-POSIX is the honest fix, aligns with existing `os.name=="posix"`
      guards): `bin/system_info.py DfCommand._get_filesystems` uses `os.statvfs("/")` (Windows has no
      `statvfs`); `proc` reads real `/proc` (synthetic Windows fallback differs).
    - Genuine source↔test drift (real bugs / stale tests): `test_proc::test_filesystems` expects bare `'proc'`
      but `proc.filesystems.get()` returns `'nodev/proc'` (actual `/proc/filesystems` format); `test_bin::
      TestDateCommand` is a `strftime` `TypeError` (unsupported directive); `TestSelfTest`/`TestCpioCommand`/
      `TestDdCommand`/`TestGunzipCommand`/`TestArchiveTarCommand` tar/cpio/dd return-code assertions (e.g.
      `tar.execute(["cf"])` return-code logic). Need per-test triage + source/test fixes.
- Decision pending from user: quantum direction (rewrite tests vs Qiskit-compat lib). `test_bin`/`test_proc`
  can be triaged independently (skip the POSIX-only ones, fix the genuine drift).
- NEXT: confirm quantum direction with user; meanwhile begin the `test_bin`/`test_proc` 12-test triage (skip
  the POSIX-only ones, fix the genuine drift). Then H7 licence sweep + full YELLOW/BLUE sweep.

## bin/proc test cluster CLOSED (session 8) — final failing-test cluster reconciled
- Picked up the carried `test_bin`/`test_proc` triage (the last of the 80 pre-existing failures). Ran both
  suites → 17 failures, triaged each to root cause, fixed 5 genuine library bugs + test↔library drift, added
  POSIX-only skips. Result: **408 passed, 43 skipped, 0 failures**.
- Genuine source fixes (all carry `# [RECONCILE]` / `# [FIX]` comments so the user can see what changed):
  - `bin/boolean_ops.py` `BracketTestCommand.execute`: bare `[` (no args / no `]`) must be **exit 2** (syntax
    error, POSIX + the module's own `_selftest` asserted 2); was returning 1. Also corrected the unit test
    `TestBracketTestCommand.test_bracket_no_args` (it expected 1, conflating "no args" with a closed `[]`
    empty-expression which is exit 1).
  - `bin/system_info.py` `DateCommand.execute`: rewrote to parse **argv** (`--help/--version/+FORMAT/-u/
    -I[timespec]`) instead of structured kwargs. Every sibling coreutils command and the test suite drive it
    with `execute(["--help"])`; the old kwargs signature crashed with `strftime(list)` `TypeError`.
  - `bin/shell.py` `TarCommand`: "archive file required" returns **1** (matches module `_selftest`
    `tc.execute(["cf"]) == 1`); was 2.
  - `bin/shell.py` `GunzipCommand.execute`: no operands + no stdin → **0** (no-op, matches `GzipCommand` and
    the module `_selftest`); was delegating to `gzip -d` with no stdin → "gzip: no input", rc 1.
  - `bin/shell.py` `CpioCommand._create`: copy-out now **reads the stdin pathname list**, validates, returns
    0 only when ≥1 named file exists (1 otherwise) — honest instead of a silent no-op stub. Test updated to
    assert the success contract.
  - `proc/procfs.py` `_resolve`: now also strips a bare leading `proc`/`proc/...` (the VFS bridge
    `_is_proc_path` already treats `proc` as the root) so `fs.list("proc")` / `fs.read("proc/cpuinfo")` work.
  - `proc/filesystems.py` `get()`: fallback list returns **bare names** (`proc`,`sysfs`,…) to match the parse
    branch (`line.split()[-1]`); was `nodev/proc` so `test_filesystems` couldn't find `'proc'`.
  - `proc/kernel_adapter.py` `LoadAvgTracker.update`: **seeds `_ema` from the first sample** — near-zero `dt`
    on first call made the EMA stay at 0.0 and fail `test_update_increases`.
- Test-only drift fixed: `test_proc::test_loadavg` asserted `parts[-1]` (last PID) held `/` — the `/` is in
  `parts[-2]` (running/total); `test_bin::TestArchiveTarCommand.test_archive_tar_no_args` expected rc 0 but
  `archive.TarCommand` correctly returns 1 (missing operand, asserted by its own `_selftest`) → `assertNotEqual`.
- POSIX-only skips (Windows runner): `@skipUnless(os.name=="posix")` on `TestDfCommand` (whole class — `os.statvfs`)
  and `TestDdCommand.test_dd_basic` (`/dev/null`).
- **Full `pytest tests/` now = 1688 passed, 54 skipped, 0 failures, 0 errors** (was 80 failing). All
  pre-existing failures reconciled (quantum + bin/proc). Bookkeeping updated: `remediation_progress.md`
  (new BIN/PROC CLOSED block), `MEMORY.md` NEXT (all-green), this daily log.
- NEXT: **H7 licence sweep** (Apache-2.0/`Licence: GPL-3.0 (...)` strays → `License: GPL-3.0`): opt/config.py,
  opt/var.py, opt/package.py, srv/backup.py, packages/umer_pkg.py, tmp/tmpfs.py headers. Then **full YELLOW/BLUE
  sweep** (H1–H307 remaining non-test gaps — un-gated privileged paths, missing license headers, baseline smells).
