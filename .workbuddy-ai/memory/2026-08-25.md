# 2026-08-25 — UmerOS remediation session 19

## H73 — Gate etc/ privileged writers (cap-gate cluster)
- Scope: the 3 highest-risk privileged `/etc` writers in `etc/`.
- Fixes:
  - `etc/sudoers.py`: `SudoersManager` now requires `CAP_FS_ADMIN` (`core/capability_gate.py`) in `add_rule`/`write_include_file`/`_write_sudoers`; `_validate_sudoers_rule` rejects blanket NOPASSWD (`user=="ALL"` or `command=="ALL"`); `__init__(sudoers_path, allow_host_etc=False)` + cross-platform `_is_host_etc` host guard blocks real host `/etc` writes. `# [FIX H73]`.
  - `etc/critical_files.py`: `initialize()` requires `CAP_FS_ADMIN` + host guard; removed hardcoded `umer ALL=(ALL) NOPASSWD: ALL` escalation → `umer ALL=(ALL:ALL) ALL`. `# [FIX H73]`.
  - `etc/passwd_group.py`: `_write_passwd`/`_write_group` require `CAP_FS_ADMIN` + host guard. `# [FIX H73]`.
  - Incidental: stripped stray `U+FEFF` BOM in `etc/pam_config.py` (line 14) that broke `etc` package import (only file affected). No `# [FIX]` (outside H73 scope but unblocks import).
- Tests: `tests/test_etc_sudoers.py` (12 tests, all green) — loads the 3 modules by file path under unique names to avoid `etc` package import + sibling shadowing; `autouse` fixture restores `gate.strict`/`unwire()` after each test.
- Verification: `tests/test_etc_sudoers.py` = 12 passed; full `pytest tests/` (excl. pre-existing-broken `tests/test_ai.py`) = 1743 passed, 54 skipped, 43 warnings, 0 failures, 0 errors.
- Bookkeeping: remediation_progress.md H73 → `[x]`; standard §9 H73 → 🟢 FIXED; MEMORY.md `etc/` → 🟢 + DONE bullet; this log.
- Scope note / follow-up: only the 3 security-critical writers gated. Broader `etc/` config-writer gating (~92 other modules) remains; H71 (license) + H72 (PAM fail-open) tracked separately.

## Session 20 — installer/ privileged-write + fail-open + data-loss cluster (H98/H99/H100/H101/H102/H103)
- Fixes (`installer/__init__.py`, `installer/installer.py`; `install.py` deleted):
  - H98/H106: `__init__.py` now re-exports the real `installer.py` `UmerInstaller`; dead non-functional stub `install.py` removed. `# [FIX H98]`
  - H99: the stub's fail-open `display_waiver` (auto-accepts in dry-run/non-TTY) is gone with the stub; canonical `show_eula` is already fail-closed (non-interactive -> denied). `# [FIX H99]`
  - H100: `run(consent_override=True)` programmatic EULA bypass now requires `CAP_INSTALL`. `# [FIX H100]`
  - H101: `rollback()` refuses to `rmtree` an unsafe root via cross-platform `_is_safe_install_root` (rejects `/`, `/opt`, and system dirs incl. `/etc`; matches on Windows where `abspath('/etc')` -> `F:\etc`); also requires `CAP_FS_ADMIN`. `# [FIX H101]`
  - H102: the whole privileged install pipeline (`run`) + `backup_bootloader`/`copy_os_files`/`install_bootloader`/`configure_first_boot`/`rollback` now require `CAP_FS_ADMIN` (fail-closed when wired, permissive-with-warning standalone). `# [FIX H102]`
  - H103: `copy_os_files` uses `_safe_join` (rejects destinations escaping the install root) and skips dotfiles. `# [FIX H103]`
- Tests: `tests/test_installer_security.py` (17 tests) + existing `tests/test_installer.py` (29) all green. Full `pytest tests/` = 1744 passed, 54 skipped, 43 warnings, 0 failures, 0 errors (excl. pre-existing-broken `tests/test_ai.py`).
- Bookkeeping: checkpoint H98/H99/H100/H101/H102/H103 -> `[x]`; standard §9 rows -> 🟢; MEMORY `installer/` -> 🟢 + DONE bullet; this log.
- Next open RED hotspots (per §9): kernel/ H110,H112 (H111 dummy-crypto residual); legal/ H128,H130,H131,H135; lib/ H3,H147; initrd/ H91,H92,H93.
