# 2026-08-20

## Code Review standard established for UmerOS
- Deliverable: `MainTask/Raw Data/Code Review Standards and Process.md` (v1.1).
- Grounded in codebase study + design sources (`MainTask/prompt/*.md`, `MainTask/Raw Data/*.docx` master prompts).
- Key design mandates encoded as review gates: mandatory tier labels `[TODAY]/[EXPERIMENTAL]/[FUTURE]`; microkernel isolation (kernel core = IPC/sched/mem/cap only); zero-trust (code-signing, HMAC IPC, capability gating); per-file baseline (try/except + logging + type hints + Google docstrings); AI self-healing patches must be sandbox+audit+rollback-gated; opt-in privacy; signed `.umerpkg`.
- Open inconsistencies to resolve: H7 license (GPL-3.0 actual vs Apache-2.0 in prompts/dev-guide); H11 UI (Kivy in code vs Flutter in blueprint).
- Known 🔴 hotspots: `settings.local.json` live OpenRouter key; `initrd/ai_helper.py:166` eval(); `lib/security.py:72` PASSWORD="password".

## requirements.txt + README.md study (v1.2 of the standard)
- **requirements.txt** (91 lines): ~32 active deps (qiskit, cirq, pennylane, torch, transformers, liboqs, cryptography…) + 4 commented dev tools (black/flake8/mypy/pylint). Risks: `g4f>=0.4.0` (free GPT-4o via reverse-engineered endpoints — supply-chain + ToS risk); floating `>=` pins and unbounded `>` on fastapi/uvicorn/httpx; `kivy` commented out though README treats Kivy as a core UI pillar.
- **README.md** (917 lines): confirms GPL-3.0 (badge + License section) → canonical license; confirms Kivy UI → canonical UI. But "Project Structure" + code examples document a DIFFERENT, aspirational layout (`kernel/ipc.py`, `quantum/simulator.py`, `security/crypto.py`, `fs/quantum_fs.py`, `ui/shell.py`) that does NOT exist; examples import phantom modules; stats claim ~3,500 LOC / 12 core modules vs actual ~735 active modules. Contributing tells users to install black/flake8/mypy/pylint though those are commented out.
- Standard bumped to **v1.2**: H7 + H11 now "resolved in direction" (GPL-3.0 / Kivy canonical, fix the strays); added **H14** (README drift — don't review against it) and **H15** (dependency hygiene: pin, isolate g4f, restore kivy, align README contributing with Ruff+Mypy).

## tests/ + Skills/Context File/ study (v1.3 of the standard)
- User created `tests/` (39 test files + `tests/quantum/`) and `Skills/Context File/` (3 design-context files).
- `Skills/Context File/umer_os_skills.json` = CANONICAL design contract: tier labels, zero-trust, AI governance, per-file baseline, installer EULA ("I AGREE"), crypto (HMAC-SHA256 IPC, SHA3-512, Kyber/Dilithium, AES-256-GCM), >80% coverage target, max 50-line functions. `skills_gemini-code.json` + `umer_skills_deepseek.md` are lighter variants. Added to standard's authoritative-design-sources.
- `tests/` framework SPLIT: top-level `test_*.py` + `run_initrd/lib/root_tests.py` use **unittest**; `tests/quantum/` uses **pytest** (fixtures + bare assert). Two quantum namespaces: `quantum.quantum_sim` (UmerOS) vs `quantum.circuit`/`quantum.simulator` (qiskit-like) — both exist.
- **No CI test step** (only `security_scan.yml`, which just does `pip install`). The 3 `run_*.py` scripts cover only 4 of 39 modules → §8 "full suite green" gate unenforceable. Bundled pytest imports removed `imp` (broken on 3.12+).
- `test_security.py` is strong (sandbox capability model, SecureBoot, IPC HMAC-SHA256, PQC) but **`SecureBoot.verify_image` is fail-open** (no trust entry / missing file → True). Flagged **H16** (test harness/CI gap) and **H17** (fail-closed SecureBoot) in the standard.

## ai/ folder study (v1.4 of the standard)
- User created `ai/` (6 `.py`: `__init__.py`, `assistant.py`, `self_healing.py`, `providers.py`, `resource_predictor.py`, `umer_ai.py`) and mandated: **backend = Python only; frontend = Flutter**.
- **Language-policy conflict surfaced:** the latest instruction mandates Flutter, REOPENING the prior H11 "Kivy canonical" decision. Also "Flutter with java" is technically wrong — Flutter = **Dart** (Java/Kotlin only for native platform channels). No Flutter/Dart source exists yet → large migration if adopted. Recorded as UNRESOLVED in H11 + new §1.1.
- `umer_ai.py` (1416 lines, good quality): `NullAIResourceManager`, `AIResourceManager` (EWMA predictor + JSON persistence), `LocalAIAssistant` (multi-provider + `_FALLBACK_RULES`/`_ERROR_PATTERNS`), `SelfHealingEngine` (records crashes, generates patch *stubs*, rollback), `AIFirewall` (anomaly scoring), `AIGovernance` (consent). Header says `Licence: Apache 2.0` (→ H20).
- **🔴 H18:** `LocalAIAssistant.query` delegates to `OnlineProvider` (POSTs prompt to external API) WITHOUT an `AIGovernance.check_consent()` gate → breaks the module's own "no data leaves device" guarantee (§4.3).
- **🟡 H19:** `ai/` has DUPLICATED stacks — `assistant.py:AIAssistant`≈`LocalAIAssistant`, `self_healing.py:SelfHealingService`≈`SelfHealingEngine`, `resource_predictor.py:ResourcePredictor`≈`AIResourceManager` (two EWMA predictors, `SPIKE_Z_THRESHOLD` 2.0 vs 2.5). The two stubs also violate the per-file baseline (no hints/docstrings/logging/tier labels, use `print`).
- **🟡→🔴 H21:** self-healing patch `generate_patch` returns code strings; TODAY stubs are comment-only (safe) but FUTURE LLM auto-apply must stay under the H12 gate; `SelfHealingService.mitigate` has no sandbox/audit/rollback.
- Standard bumped to **v1.4**: added §1.1 (language/tech-stack policy), §4.2/§4.3/§4.4 ai/-specific items, H18–H21 hotspots, updated H11, refreshed §11 quick-ref.
- **H11 finalized (2026-08-20): Flutter (Dart) canonical** — user confirmed "Switch to Flutter (Dart)", overriding the earlier Kivy decision. Backend = Python only; "Flutter with java" corrected (Dart). Kivy becomes legacy drift: drop `kivy` from `requirements.txt`, refresh Kivy refs in README/`setup.py`. Updated H11 row, §1.1, §4.7, H15, §11 quick-ref, and MEMORY.md accordingly.

## ui/ folder study (v1.5 of the standard)
- User pointed UX/UI design folder = `ui/`; **mandate: frontend in Flutter (Dart) ONLY, following HCI (Human-Computer Interaction) principles.**
- **Canonical frontend already exists:** `ui/flutter_ui/` is a real Flutter/Dart app — `pubspec.yaml` (`sdk: ^3.12.2`, deps `provider`, `flex_color_scheme`, `google_fonts`, `flutter_animate`, `http`, `shared_preferences`, `webview_flutter_windows`; linted via `flutter_lints`), `lib/main.dart` (MultiProvider + Material 3 light/dark via `FlexThemeData`, `debugShowCheckedModeBanner:false`), `theme_provider.dart` (wallpaper presets, glassmorphism, uiScale), `app_state.dart` (windowing model: drag/resize/snap `WindowSnapMode`, notifications, control-center toggles), `desktop_shell.dart` (DesktopShell + clock Timer + launchpad + spotlight search), 18 app screens in `lib/src/apps/`.
- Good HCI baseline already present: M3 theming, `tooltip:` on icon buttons, keyboard shortcuts (Ctrl+K search, Esc dismiss), responsive windowing, spotlight search, dock.
- **Legacy frontend:** `ui/*.py` (`fluidic_ui.py` CLI shell, `umer_de.py` Tkinter DE, `theme.py`, `launch_gui.py`, `umeros_gui.py`) — Python/Tkinter, superseded by Flutter.
- Confirmed Kivy contradiction for H22: `setup.py:15` lists `kivy>=2.3.0` in `install_requires` (active dep); `README.md` has ~7 Kivy refs (L146/167/206/262/362/748/855); `requirements.txt:52` already comments kivy out.
- Confirmed HCI #2 jargon for H24: `desktop_shell.dart:287` "CPUIdle & Governor", `:448` "Power & Idle", `power_governor_app.dart:104` "CPUIdle & Power Governor Framework".
- Standard bumped to **v1.5**: §1 UI row → `ui/flutter_ui/` canonical + `ui/*.py` legacy (H15/H25); §1.1 reviewer action → Dart-only frontend + every frontend PR must satisfy §4.8 HCI checklist; **new §4.8 "Frontend — Flutter/Dart & HCI principles 🟡"** (10 Nielsen heuristics + 2 UmerOS items); added **H22** (Kivy in `setup.py`/README), **H23** (`desktop_shell.dart` duplicated app registry → `AppRegistry`), **H24** (jargon labels → plain language), **H25** (legacy `ui/*.py`), **H26** (Flutter a11y: missing `Semantics` + smoke-only test); refreshed §11 quick-ref + fixed §7 "add kivy back" → drop kivy.

## boot/ folder study (v1.6 of the standard)
- User directed study of `boot/` (20 .py modules ~9.8k LOC + 1 C stub `uefi_stub.c` + `boot_samples/` fixtures: config/System.map/vmlinuz-6.1.0-umeros binary).
- **Scope:** `boot/` is a `/boot` **management toolkit** for a Linux-hosted boot env (FHS 3.0 audit, GRUB2, systemd-boot/BLS, EFI+Secure Boot, UKI, bzImage header parser, kernel cmdline+sysctl, CPU microcode, boot splash/framebuffer, kdump crash kernel, initrd). It is NOT the UmerOS microkernel entry — that's `kernel/umer_kernel.py` (imported by `bootloader.py`/`init.py`). Consistent with "runs atop an existing kernel (Linux userspace)".
- Overall quality is HIGH: 19/20 files have `from __future__ import annotations`, type hints, Google docstrings, `_selftest()`; `subprocess` calls are all arg-list + `timeout` (no `shell=True`).
- **🔴 H27:** `bootloader.py:147` `verify_kernel` returns `True` on missing image ("Permissive during prototype phase") + when `expected_hash is None` → fail-open kernel integrity (same class as H17).
- **🔴 H28:** `efi_system.py:524-533` `is_binary_trusted` returns `True` for `DISABLED`/`SETUP_MODE` and never consults `dbx` → fail-open Secure Boot (extends H17).
- **🟡→🔴 H29:** `init.py:27` auto-accepts the "I AGREE" liability waiver in non-interactive mode → bypasses §4.2 installer EULA mandate.
- **🟡 H30:** 10 `boot/` files declare `Licence: Apache 2.0` headers (extends H7/H20). **🟡 H31:** only `bootloader.py` has a tier label (19/20 missing). **🟡 H32:** `uefi_stub.c` is a 33-line `printf` placeholder, no real UEFI/ctypes binding (despite `init.py:33` claiming ctypes sim). **🟡 H33:** hash inconsistency (SHA3-256 kernel vs SHA-256 EFI vs mandated SHA3-512). **🟡 H34:** `boot/` CLI skips `core/command` contract; `demo_boot.py` uses unprefixed sibling imports; `init.py` skips per-file baseline.
- Standard bumped to **v1.6**: §1 status → +`boot/`; §4.2 added fail-closed boot-path integrity item (H27/H28/H29); §4.4 added `boot/` tier-label + `init.py` baseline note (H31/H32); §9 added **H27–H34**; §11 quick-ref added `boot/` study line.

## bin/ folder study (v1.7 of the standard)
- User directed study of `bin/` (44 `.py` modules ≈19k LOC — FHS `/bin` + `/usr/*` user commands: `usr_cmds.py` 3222, `usr_commands.py` 2132, `essential_commands.py` 1725, `system_info.py` 1288, `permissions.py` 1219, `bin_manager.py` 1114, `process.py` 834, `shell.py` 751, `usr_share.py` 722, plus ~36 per-FHS-dir modules).
- **Scope:** `bin/` is the user-facing shell/command surface (FHS `/bin` + `/usr/*`), separate from the `boot/` toolkit and the `kernel/` microkernel. It is the dominant H6 offender.
- **🔴 H37:** `user_commands.py` `LoginCommand` — `-f`/`-F` set `opts["skip_auth"]=True` (L268-273); `if not skip_auth:` (L245) skips `_authenticate`, so password (`crypt.crypt` L333) is never verified → auth bypass.
- **🟡 H36:** `SuCommand` `_exec_command` (L169-185) runs `subprocess.run([sh,"-c",command], … user=pw_uid, group=pw_gid)` as another user; sibling `_exec_shell` (L187-199) is a stub that `print`s "Interactive shell not available" and `return 0` (fail-open silence). Arg-list (no `shell=True`).
- **🟡 H35:** **27/44** command classes use wrong `def execute(self, *args)` (190 occurrences) vs adopted `def execute(self, args=None)->int`; only 12 modules use the correct signature (incl. `user_commands.py`, `usr_cmds.py`, `archive.py`). Converge + fix `core/command.py` base (H6).
- **🟡 H38:** `ChageCommand` defined twice — `usr_cmds.py:3045` and `usr_commands.py:1938` (likely `whoami`/`id`/`groups` too). Consolidate.
- **🟡 H39/H8:** ~30+ broad `except` mask errors. **🟡 H40:** **0/44** modules carry a tier label (worse than `boot/` 1/20).
- **Good:** NO Apache-2.0 headers in `bin/` (unlike `boot/`); all `subprocess` calls are arg-list + `timeout` (no `shell=True`); NO `eval`/`exec`/`os.system`/`pickle`/`yaml.load`.
- Standard bumped to **v1.7**: header → +`bin/`; §4.4 added `bin/` study bullet (tier labels/su/login/dupe/broad-except + safe subprocess); §9 added **H35–H40**; §11 quick-ref added `bin/` study line.

## build/ folder study (v1.8 of the standard)
- User directed study of `build/` (16 files ~42MB). **It is PyInstaller output** for the `UmerOS-GUI` freeze — only 2 authored files: `build/UmerOS-GUI.spec` (PyInstaller spec) + `build/__init__.py` (0-byte marker). Everything under `build/UmerOS-GUI/` (`*.toc`, `PYZ-00.pyz`, `UmerOS-GUI.pkg` 38MB, `base_library.zip`, `localpycs/*.pyc`, `warn-UmerOS-GUI.txt`, `xref-UmerOS-GUI.html`) is **generated → out of review scope per §0**.
- **🟡 H41:** Spec `Analysis(['F:\...\UmerOS\ui\umeros_gui.py'])` freezes the **legacy Tkinter GUI** (`ui/umeros_gui.py`, 84KB, retired per H25) — not the canonical Flutter app (`ui/flutter_ui/pubspec.yaml` present, unbuilt). Ships the wrong frontend.
- **🟡→🔴 H42:** `codesign_identity=None` (unsigned distributable — breaks zero-trust signed-artifact mandate, H17/H27/H28) + `upx=True` (packs/obscures, trips AV).
- **🟡 H43:** hardcoded absolute Windows path `F:\Pension Person Details\UmerOS\ui\umeros_gui.py` (non-portable).
- **🟡 H44:** `datas=[]`/`binaries=[]`/`hiddenimports=[]`/`excludes=[]` + `optimize=0`; no `Makefile`/`build.py`/CI build step in-repo (extends H16).
- **💭 H45:** spec has no license/tier label (`build/__init__.py` is harmless empty). `warn-UmerOS-GUI.txt` = benign Windows posix-module noise (pwd/grp/fcntl/posix/resource/_posixsubprocess) — no real missing imports.
- Standard bumped to **v1.8**: header → +`build/`; new **§4.9 "Build & release integrity"** (6 bullets: target canonical frontend / sign artifacts / portable path / complete+optimized freeze / codify build / spec header); §9 added **H41–H45**; §11 quick-ref added `build/` study line.

## cloud/ folder study (v1.9 of the standard)
- User directed study of `cloud/` (3 files: `cloud/__init__.py` empty, `cloud/ota_updater/__init__.py` re-exports `UpdateManager`, `cloud/ota_updater/update_system.py` ~67 LOC). `cloud/` = the **OTA (over-the-air) update subsystem** — the zero-trust update path.
- **🔴 H46:** `verify_and_apply` (L48-60) is **fail-open** — returns `True` and applies the update even when `self.crypto is None` ("No crypto engine — skipping signature check"); when crypto present it only `self.crypto.sign(payload)` (self-signs) + prints "PASS (simulated)" — never verifies the manifest signature. A real PQC `CryptoEngine.verify` exists in `security/crypto_engine.py`/`quantum/crypto_pqc.py` (dilithium/ml_dsa) but is **unused**. So unsigned/unverified updates get applied — same class as H17/H27/H28, breaks signed-`.umerpkg` mandate (§4.2). Added a parallel §4.2 "OTA/update integrity is fail-closed" bullet.
- **🟡 H47:** module skips baseline (`print` not `logging`, no `from __future__`, no tier label → should be `[EXPERIMENTAL]` since simulated); **🟡 H48:** hardcoded `update_url="https://updates.umeros.dev/latest"` (no pinned source/key); **🟡 H49:** docstrings overstate security ("Verify cryptographic signature") while faking it; no rollback/audit.
- No license headers in `cloud/` (clean, like `bin/`, unlike `boot/`). No real network/crypto calls — fully simulated.
- Standard bumped to **v1.9**: header → +`cloud/`; §4.2 added **OTA/update integrity fail-closed** bullet (H46); §9 added **H46–H49**; §11 quick-ref added `cloud/` study line.

## compatibility/ folder study (v1.10 of the standard)
- User directed study of `compatibility/` (4 files, ~645 LOC) — the **Universal Compatibility Layer** (runs foreign-OS apps: Linux ELF, Windows .exe via Wine, Android APK via ADB) + a syscall translator.
- **Best tier-label discipline so far:** `container_engine.py` uses `[EXPERIMENTAL]`/TODAY/FUTURE throughout (good counterexample to `bin/` 0/44, `boot/` 1/20). Subprocess launches are arg-list, no `shell=True` (injection-safe).
- **🟡 H50:** `container_engine.py:30` `Licence: Apache 2.0` (H7/H30); other 3 files have no header.
- **🔴 H51:** `container.py:ZeroTrustContainer.execute_binary` (L12-23) calls `capabilities.check(...)` but only prints + still runs the binary — **fail-open zero-trust gate** (same class as H17/H27/H28/H46). Class is unused (not imported by container_engine.py/`__init__.py`).
- **🟡 H52:** `container_engine.py:269/348/435` launch foreign ELF/`.exe`/APK **unsandboxed** (no chroot/namespace/quotas, full env inherited) — must sandbox before `[TODAY]`.
- **🟡 H53:** `container.py` + `syscall_shim.py` skip baseline (`print` not logging, no `from __future__`, no label); `syscall_shim.py:13` dynamic `self.syscall_table[name](*args)` → keep table allow-listed.
- **💭 H54:** two divergent container models (`ZeroTrustContainer` vs `ContainerEngine`/`ContainerInstance`) — consolidate.
- Standard bumped to **v1.10**: header → +`compatibility/`; §4.2 added **compatibility-launched foreign binaries sandboxed + capability-gated** bullet (H51); §9 added **H50–H54**; §11 quick-ref added `compatibility/` study line.

## core/ folder study (v1.11 of the standard)
- User directed study of `core/` (2 files): `core/command.py` (32 LOC — base `Command` class every `bin/` command inherits) + `core/__init__.py` (1 line).
- **`core/command.py:30` is the ROOT CAUSE of H6/H35:** base `execute(self, *args: Any) -> Any` (returns `Any`) contradicts the adopted `execute(args=None) -> int`. So the 27/44 `bin/` modules using `def execute(self, *args)` just inherit the wrong base signature. Fix base class first.
- **🟡 H55:** fix base `Command.execute` to `execute(args: Optional[List[str]] = None) -> int` to resolve H35 at source.
- **🟡 H56:** base `Command` declares `privileges: List[str]` field (L28) but never enforces it — zero-trust commands must gate execution on declared privileges via `CapabilityManager` (cf. H37/H4).
- **💭 H57:** `command.py` has no tier label (should be `[TODAY]`) / no license header (clean, like `bin/`). Tiny, foundational, low-risk otherwise.
- Strengthened §4.4 command-interface bullet to name `core/command.py` as root cause + require privilege enforcement.
- Standard bumped to **v1.11**: header → +`core/`; §4.4 command-interface bullet notes root cause (H55/H56); §9 added **H55–H57**; §11 quick-ref added `core/` study line.

## dev/ folder study (v1.12 of the standard)
- User directed study of `dev/` (43 `.py` modules ≈6,800 LOC) — a virtual FHS `/dev` **device-node filesystem**: central `DeviceManager`/`DeviceNode`/`DeviceType` (`dev/core.py`, 245 LOC) registry + ~40 pseudo-device / subsystem / command modules (null/zero/random/full/tty/ptmx, input, shm, net/tun, fuse, memory `/dev/mem`+`/dev/kmem`+`/dev/port`, SCSI, NVMe, KVM, dri, snd, udevadm/losetup/makedev/mknod commands).
- **Cleanest folder security-wise so far:** NO `eval`/`exec`/`os.system`/`subprocess`/`shell=True`/`pickle`/`yaml.load`/`ctypes` anywhere; licensing is **clean GPLv3** (headers say `Licence: GPLv3`; ZERO Apache-2.0 strays — a good counterexample to `boot/`). 42/43 files have `from __future__`; all have module docstrings.
- **💭 H58:** **0/43** files carry a tier label (same gap as `bin/` 0/44, `boot/` 19/20).
- **🟡 H59:** base `DeviceNode` (dev/core.py) defaults to `mode=0o666` (world-writable); many nodes explicitly `0o666` (null/zero/full/tty/ptmx/shm/tun/fuse/i2c/misc-char/misc-system). Only the security-sensitive memory devices (`memory_devices.py`) correctly use `0o640`. World-writable device nodes = privilege-escalation vector on a real `/dev`.
- **🟡 H60:** `DeviceManager.sync_to_filesystem()` (dev/core.py:211) calls `os.mknod`/`os.mkfifo`/`os.symlink_to` to materialize **real** device nodes into the VFS with **no capability/privilege gate** — on a real `/dev` with host privileges this creates device special files (incl. world-writable from `0o666`), incl. `/dev/mem`/`/dev/kmem`/`/dev/port`. Same "privileged op / no gate" family as H27/H28/H46/H51.
- **💭 H61:** `DeviceManager` singleton registry (`self._nodes`) has no `threading.Lock` — concurrent registration/hotplug could race (low risk in single-threaded boot).
- `memory_devices.py` is the model to copy: `STRICT_DEVMEM` restricts `/dev/mem` to `ALLOWED_RANGES`, `/dev/kmem` `AVAILABLE=False` (returns zeros), `_check_access` logs warnings before denying.
- Standard bumped to **v1.12**: header → +`dev/`; §4.4 added `dev/` study bullet (tier labels/perm default/capability gate/lock + memory_devices model); §9 added **H58–H61**; §11 quick-ref added `dev/` study line. Total hotspots now H1–H61.

## drivers/ folder study (v1.13 of the standard)
- User directed study of `drivers/` — **75 Python modules (~34,853 LOC)**, the **kernel driver subsystem** (bus/device/driver framework: `device.py`/`bus.py`/`platform_driver.py`/`device_registry.py` + ~70 subsystem drivers: irq, usb, crypto, pci, input, mtd, dmabuf, tty, i2c, gpio, cpuidle, device_io, clock, thermal, reset, spi, iio, pinctrl, console, edac, nvmem, regulator, nfc, mailbox, nvme, rtc, devfreq, pwm, dmaengine, scsi, ata, counter, acpi, virtio, soundwire, tee, phy, ntb, remoteproc, fpga, pwrseq, rpmsg, framebuffer, interconnect, led, extcon, rfkill, backlight, `pci_endpoint`, `example_*`/`hello_char_driver`). A large **simulation of the Linux driver model** (no real HW access; `ctypes` is imported in `device_io.py`/`irq.py` but never called).
- **Clean:** no `eval`/`exec`/`os.system`/`subprocess`/`shell=True`/`pickle`/`yaml.load` anywhere; `crypto.py` uses `os.urandom` (CSPRNG) for every nonce/key (good RNG hygiene); `device_io.py` correctly locks its registries (`_lock`), claims regions (duplicate-key guard), tracks owner, protects read-only ports (good model). `driver_service.py` has good security middleware (HSTS, X-Content-Type-Options, X-Frame-Options, Referrer-Policy, Permissions-Policy, CORS restricted to a placeholder origin, in-memory rate limiter, binds 127.0.0.1, generic 500 handler with no stack-trace leak).
- **🔴 H64:** `driver_service.py` (`/proc` emulation API) falls back to a **hardcoded static JWT secret `"test-secret"`** (HS256) when OIDC env vars are unset and serves every endpoint — incl. `/pid/{pid}/environ` (process env/secrets) and an **unauthenticated `/metrics`**. Weak-default/static-secret auth gap (same family as H17/H27/H28/H46/H51). Must fail-closed (refuse to serve without OIDC, or explicit dev mode) + gate environ/metrics.
- **🟡 H63:** **0/75** carry a GPL license header (should be GPLv3 per H7; unlike `dev/`, none at all). **💭 H62:** **0/75** tier labels (worst raw count). **🟡 H66:** whole subsystem has **no capability gating** (MMIO/port/DMA `device_io.py`, PCI claim `pci.py`, crypto key-gen/RNG `crypto.py` callable by any code; only `driver_service.py` imports `security`, and only `tls_utils`). **💭 H65:** `ctypes` imported but never called (`device_io.py`/`irq.py`) — dead import / latent HW surface. **💭 H67:** `crypto.py` registers weak algos (DES/MD5) + pure-Python non-constant-time ciphers. **💭 H68:** `device_registry.py` + driver registries have no lock (`device_io.py` is the locked counterexample). **🟡 H69:** **19/75** lack `from __future__` (incl. `driver_service.py`, `device.py`, `bus.py`, `platform_driver.py`, all `example_*`) and `driver_service.py` uses `loguru` not `logging` (H8).
- Standard bumped to **v1.13**: header → +`drivers/`; §4.2 added fail-closed-auth/static-secret bullet (H64); §4.4 added `drivers/` study bullet; §9 added **H62–H69**; §11 quick-ref added `drivers/` study line. Total hotspots now H1–H69.

## etc/ folder study (v1.14 of the standard)
- User directed study of `etc/` (81 `.py` modules ~24,419 LOC) — the FHS `/etc` **configuration-management layer**: managers for PAM (`pam_config.py` 1821 LOC), fstab/crypttab/tmpfiles/udev/sysconfig, init, login, hostname, locale/timezone, modules, cron, inet services, issue/motd, sudoers, critical-files, and 60+ more. Flat (no subdirs).
- **Subprocess discipline is good:** `subprocess` in 34 files, **all arg-list, `shell=True`=0**; no `eval`/`exec`/`os.system`/`pickle`/`yaml.load`/`ctypes` anywhere (injection-safe). Positive examples: `critical_files.py` protects critical files with `os.chmod 0o440`/`0o644`; `fstab_manager.py` mounts tmpfs with secure defaults (`noexec,nodev,nosuid`) + arg-list `df` calls. `issue_motd.py` shells out to host (`who`/`uptime`/`last`) arg-list — already tracked (H5/§4.2).
- **💭 H70:** **0/81** files carry a tier label (largest raw count, 81 files — worse than `drivers/` 75 / `bin/` 44 / `dev/` 43).
- **🟡 H71:** license inconsistency — 28/81 GPLv3/GPL-3.0 (✓ canonical H7), **6/81 Apache-2.0 strays** (`__init__.py`, `config_manager.py`, `critical_files.py`, `network_config.py`, `passwd_group.py`, `shell_config.py` — violates H7), and **47/81 have NO license header at all** (incl. the biggest: `pam_config.py` 1821, `module_config.py` 1395, `locale_timezone.py` 1383, `sysconfig.py` 1335, `udev_rules.py` 1209, `fstab_manager.py` 1185, `tmpfiles.py` 1151, `sudoers.py`). Corrects earlier "0 missing" note — majority of `etc/` ships unlicensed.
- **🟡 H72:** `pam_config.py` has a weak-auth **detector** (`_WEAK_PATTERNS` flags `pam_permit.so`/`nullok` as critical/high) but `_validate_entries` only *appends warnings* — it does **NOT reject** the write, so `add_auth_rule` still persists a `pam_permit`/`nullok` rule to `/etc/pam.d/*` → auth-bypass enabler. Same "detect but don't fail-closed" family as H17/H27/H28/H46/H51/H64.
- **🟡 H73:** `etc/` config managers write **real privileged `/etc` paths with no capability gate**. `sudoers.py` (`SudoersManager`) writes `/etc/sudoers`+`/etc/sudoers.d/*` (incl. `NOPASSWD:` privilege-escalation rules) with no `CapabilityManager` check + no validation of blanket `NOPASSWD: ALL`. Whole subsystem un-gated (only `cups_config.py` mentions "capability" — false positive: printer *capability database*). Same "privileged op / no gate" family as H27/H28/H46/H51/H60/H66.
- Standard bumped to **v1.14**: header → +`etc/`; §4.4 added `etc/` study bullet (tier labels/license split/PAM non-blocking detector/privileged-write no-gate + secure perms/mount positives); §9 added **H70–H73**; §11 quick-ref added `etc/` study line. Total hotspots now H1–H73.

## examples/ folder study (v1.15 of the standard)
- User directed study of `examples/` (2 `.py` files, **62 LOC total**) — the illustrative/starter folder: `run_demo.py` (47 LOC, a `demo()` wiring real `kernel.scheduler`/`kernel.ipc`/`kernel.hal`/`quantum.simulator` + the scheduler) and `tasks.py` (15 LOC, two cooperative `asyncio` example tasks).
- **Security-clean:** no `subprocess`/`shell=True`/`eval`/`exec`/`os.system`/`pickle`/`yaml.load`/`ctypes`; no hardcoded secrets; no network calls.
- **💭 H74:** **0/2** files carry a tier label, **0/2** carry a license header (should be GPLv3 per H7), and the per-file baseline is skipped — no `from __future__ import annotations`, `print` not `logging`, and `demo`/`simple_io_task`/`long_compute_task` have no Google docstrings. Because `examples/` is the first code new contributors read/copy, it should be the canonical compliant template (tier label + GPLv3 + logging + docstrings).
- **💭 H75:** `run_demo.py` exercises **privileged surfaces** — `HAL.init_device`/`HAL.read_register` (HW access) + `IPCBroker.register` (IPC) — with **no `CapabilityManager` check and no HMAC-signed IPC** (opposite of §4.2 zero-trust mandate). Harmless as a demo, but it teaches contributors to touch HAL/IPC openly; the `success_prob` scheduler hints also prefigure the AI-orchestrator path (H19/H21) with no governance. Make it model the safe pattern.
- Standard bumped to **v1.15**: header → +`examples/`; §4.4 added `examples/` study bullet; §9 added **H74–H75**; §11 quick-ref added `examples/` study line. Total hotspots now H1–H75.

## feedback/ folder study (v1.16 of the standard)
- User directed study of `feedback/` (2 `.py` files, **185 LOC**) — a community-feedback subsystem **stub**: `models.py` (125 LOC — `FeedbackEntry`/`FeedbackKind`/`FeedbackStatus`/`FeedbackPriority` dataclasses + enums) and `__init__.py` (60 LOC — package init).
- **🟡 H76: the package is BROKEN.** `__init__.py` does `from collector/tracker/channels/gfdl/manager import ...` but **none of those 5 modules exist anywhere in the repo** (only `models.py` + `__init__.py` present) → `import feedback` raises `ModuleNotFoundError` at import time. The docstring advertises a full collector/tracker/channels/gfdl/manager/cli framework that is entirely unimplemented (real broken code, not just aspirational docs like H14). Also `__init__.py` hacks `sys.path.insert(0, _this_dir)` + absolute `from models import` instead of relative imports.
- **🟡 H77:** both files say `Licence: Apache 2.0` (violates H7 GPL-3.0 canonical; adds 2 more Apache strays to the running total — `boot/` 10, `etc/` 6, `feedback/` 2). `__init__.py` docstring also references a "GFDL engine" (a third license in play).
- **💭 H78:** 0/2 tier labels (same recurring gap).
- **💭 H79:** `FeedbackEntry` stores submitter PII (`submitter_name`/`submitter_contact` email+GitHub/`channel`) + `attachments` (paths/URLs) with no consent flag / anonymization / erasure hook; `to_dict()` serializes PII wholesale. Per §4.3, when the (missing) collector/channels implement transmission they must honor the H18 consent gate + erasure, and validate attachment paths (traversal).
- `models.py` is otherwise CLEAN: `from __future__`, type hints, docstrings, `uuid.uuid4()` IDs, no `subprocess`/`eval`/`exec`/`pickle`/`ctypes`/secrets.
- Standard bumped to **v1.16**: header → +`feedback/`; §4.4 added `feedback/` study bullet; §9 added **H76–H79**; §11 quick-ref added `feedback/` study line. Total hotspots now H1–H79.

## fs/ folder study (v1.17 of the standard)
- User directed study of `fs/` (3 `.py` files, **777 LOC total**) — the **Virtual Filesystem (VFS) layer over a Quantum Filesystem (QFS)** content-addressable store: `qfs.py` (651 LOC — `CASStore` SHA3-256-addressed in-memory block store w/ dedup+refcount, `QFSCompressor` LZMA+XOR-delta `MAGIC=b"QFS1"`, `AIFileIndexer` keyword/TF-IDF search, `QFS` facade: write/read/delete/snapshot/restore/search/stats), `vfs.py` (124 LOC — `VirtualFileSystem` POSIX-path → QFS shim used by user-space + Syscall-Shim), `__init__.py` (2 LOC — clean relative re-exports).
- **🔎 First folder in the whole study with a tier label present:** `qfs.py:2` docstring `[TODAY / EXPERIMENTAL]` (good — the other 2 files still lack one).
- **Security-clean:** no `subprocess`/`shell=True`/`eval`/`exec`/`pickle`/`ctypes`/`os.system`/`yaml.load` anywhere; `qfs.py` is a **model** for the rest of the repo — uses `log = logging.getLogger("UmerOS.QFS")` + `threading.Lock` on every shared store (`CASStore`/`AIFileIndexer`).
- **🟡 H80:** no capability gating on VFS/QFS mutations — `vfs.py` `write_file`/`mkdir` + `qfs.py` `QFS.write_file`/`delete_file`/`snapshot`/`restore_snapshot` are callable by any in-process code with **no `CapabilityManager` check**. The VFS is "the interface user-space programs (and the Syscall Shim) interact with" → an unprivileged user-space write into the CAS store is un-gated. Same "privileged op / no gate" family as H27/H28/H46/H51/H60/H66/H73.
- **🟡 H81:** license split — `qfs.py:27` says `Licence: Apache 2.0` (H7 stray), `vfs.py`+`__init__.py` have NO header at all; `vfs.py` uses `print("[VFS]...")` not `logging`; only `qfs.py` has `from __future__ import annotations`.
- **💭 H82:** `qfs.py:snapshot()` reads private `self._store._refs` (L580-583) — breaks `CASStore` encapsulation (should use a public refcount/snapshot API); and CAS addresses with **SHA3-256** (`hashlib.sha3_256`, L84) while the design mandate says SHA3-512 — confirm whether 256-bit is an intentional CAS-addressing choice vs the 512-bit crypto mandate, and document it. `vfs.py:_resolve` (L44-55) splits `path.strip("/").split("/")` and treats `..` as a literal child name (no up-traversal resolution/validation) — benign in an in-memory tree, note for the real mount path.
- Standard bumped to **v1.17**: header → +`fs/`; §4.4 added `fs/` study bullet (tier label present / no capability gate / license split / encapsulation + SHA3-256 notes); §9 added **H80–H82**; §11 quick-ref added `fs/` study line. Total hotspots now H1–H82.

## home/ folder study (v1.18 of the standard)
- User directed study of `home/` (10 `.py` files, **1,553 LOC**) — the **`/home` user-home-directory subsystem** (FHS 3.0): `home_manager.py` (`HomeManager`), `dotfiles.py` (`DotfilesManager`), `user_profile.py` (`UserProfileManager`), `home_dirs.py` (`HomeDirsManager`), `home_local.py` (`HomeLocalManager`), `home_ssh.py` (`HomeSSHManager`), `home_mail.py` (`HomeMailManager`), `home_quota.py` (`HomeQuotaManager`), `home_backup.py` (`HomeBackupManager`), `__init__.py` (re-exports).
- **Injection-safe at Python level:** no `subprocess`/`shell=True`/`eval`/`exec`/`pickle`/`ctypes`/`os.system`/`yaml.load` anywhere (path-based, not shell-based). **Good models:** `home_ssh` uses `secrets.token_hex` (CSPRNG) + strict perms (.ssh 0o700, priv key 0o600, config 0o600); `home_manager`/`home_dirs` build a correct FHS/XDG layout.
- **🔴 H83:** `home_backup.restore_backup` (L66-79) calls `tarfile.extractall(str(self.home_path))` with **no `filter`** → tar traversal / arbitrary file write (CVE-2007-4559); also `shutil.rmtree`s the live home *before* extracting (data loss if the tar is bad/swapped). `/var/backups/homes` is privileged + the tar is caller-supplied.
- **🟡 H84:** systemic **path traversal via unvalidated `username`** (`self.home_path / username` used for mkdir/write/rmtree with no segment check → `../etc` escapes `/home`); 🔴 for destructive callers `remove_home`/`restore_backup`/`remove_user_dirs`/`remove_local`; also `home_mail.deliver` uses `message.id` as a filename, `home_local` uses unsanitized `name`/`subdir`. Same family as H79.
- **🟡 H85:** **no capability gating** on privileged `/home` mutations (create/remove home, SSH keys/authorized_keys, `/var/spool/mail`, backup/restore) — same "privileged op / no gate" family as H27/H28/H46/H51/H60/H66/H73/H80.
- **🟡 H86:** `user_profile._generate_profile/_bashrc` interpolate **unsanitized env-var values & aliases** into sourced `.profile`/`.bashrc` (`export K="V"`, `alias n='cmd'`) → shell injection / config poisoning when sourced.
- **🟡 H87:** `home_ssh.generate_key` produces **FAKE non-functional SSH keys** (`secrets.token_hex` dressed as PEM/ssh — won't work with OpenSSH; overstates capability like H33/H49/H14); `add_authorized_key` appends **unsanitized** public keys to `authorized_keys` (unprivileged caller could grant SSH access); SHA-256 not mandated SHA3-512.
- **💭 H88:** 0/10 tier labels / license headers / `from __future__` / `logging` (whole folder silent — chmod errors swallowed with `pass`); `user_profile` unused `import os`; disk-usage scans use `rglob('*')` (symlink-loop/perf).
- Standard bumped to **v1.18**: header → +`home/`; §4.4 added `home/` study bullet; §9 added **H83–H88**; §11 quick-ref added `home/` study line. Total hotspots now H1–H88.

## HostFiles/ folder study (v1.19 of the standard)
- User directed study of `HostFiles/` (3 plain-text `.txt` files, ~347 B total) — a **non-code asset folder**: `welcome.txt` + `welcome_copy.txt` (both `Welcome to Umer OS v2.0.0-Quantum`) + `chat_log.txt` (a one-paragraph philosophy blurb: zero-trust/AI/quantum — **no PII, no secrets, no code**). Not Python → per-file §4 baseline (type hints/docstrings/logging/tier labels/license) + dangerous-call checks **N/A**; recorded for completeness per the standing "study every folder" convention.
- Only code reference: `ui/umer_de.py:16` (`HostBridge.HOST_DIR = os.getcwd()/"HostFiles"`) — the **retired Tkinter DE (H25)**. `HostBridge.extract_and_open` reads `kernel.vfs.read_file(vfs_path)` and writes it to `HostFiles/<basename>` where `basename = vfs_path.strip('/').split('/')[-1]`; a `vfs_path` ending in `..` makes `filename='..'` → write escapes `HostFiles/` to parent (host-disk traversal); `open_in_host` then `os.startfile`/`subprocess.call(('xdg-open'/'open', host_path))` auto-opens the dropped file → exfil + host-execution surface. Legacy (H25).
- Findings: 💭 H89 `welcome_copy.txt` is a **byte-for-byte duplicate** of `welcome.txt` (dead asset) + `chat_log.txt` is misnamed (not a log) + verify banner version `v2.0.0-Quantum` against canonical version; 🟡 H90 `HostBridge` writes VFS content to host disk via weak basename (`..` escapes) + auto-opens it — host-integration sink, legacy (H25). Once Flutter is canonical (H11/H25) the only consumer goes away → prune `HostFiles/` with the Tkinter DE.
- Standard bumped to **v1.19**: header → +`HostFiles/`; §4.4 added `HostFiles/` study bullet; §9 added **H89–H90**; §11 quick-ref added `HostFiles/` study line. Total hotspots now H1–H90.

## initrd/ folder study (v1.20 of the standard)
- User directed study of `initrd/` (17 `.py` modules, **5,112 LOC**) — the **early-boot initial-RAM-disk runtime, builder & tools** modelling the Linux `/initrd` (TLDP) eight phases: `linuxrc.py` (PID-1 orchestrator), `linuxrc_main.py`/`__main__.py`/`__init__.py` (entry points), `phase_machine.py`, `hooks.py`, `ramdisk.py`, `vfs_ops.py`, `mounts.py`, `pivot_root.py`, `cpio.py`, `archivers.py`, `module_resolver.py`, `ai_helper.py`, `scenarios.py`, `signals.py`, `builder.py`.
- **Best baseline of any folder studied:** all 17 have `from __future__ import annotations` + `logging.getLogger` + Google docstrings + `_selftest()`; all 17 say `Licence: Apache 2.0` (non-canonical, H7). It is the MOST security-critical subsystem — runs as PID 1 / uid 0, performs pivot_root/mount/chroot/`seteuid(0)`/`exec /sbin/init`.
- **🔴 H91:** `ai_helper.py:166` `eval(line)` over `/var/log/umeros_initrd_history.log` ("`# noqa: S307 - history file is trusted`") re-locates known **H2** into early boot — attacker-writable log → root code-exec (fail-open like H17/H27/H28/H46/H51/H64).
- **🔴 H92:** **no capability gating** on privileged boot ops (`pivot_root`/`mount`/`chroot_into`/`_drop_to_root`→`seteuid(0)`/`exec /sbin/init`) — most sensitive occurrence of the H27/H28/H46/H51/H60/H66/H73/H80/H85 gap.
- **🔴 H93:** **CPIO entry-name path traversal** in `InitrdBuilder._unpack_to_dir` (`builder.py:342-360`) — `entry.name` from cpio header, no `..` check, `dest = target / rel` escapes `target` (CWE-22, cpio analogue of tar H83). In-memory path SAFE because `vfs_ops._split` pops `..`.
- **🟡 H94:** dynamic `__import__` by name (`ai_helper.py:155`, `__main__.py:66`) — no allow-list (closed name sets today). **🟡 H95:** all 17 files Apache-2.0 (single sweep → GPLv3). **💭 H96:** 0/17 tier labels. **🟡 H97:** SHA-256 vs mandated SHA3-512 (CSPRNG `os.urandom` good).
- **Positives:** `vfs_ops._split` traversal-safe VFS (model for `fs/vfs.py` H80-82); `module_resolver.from_user_config` fails CLOSED on unknown modules; `signals.install` opt-in via env; `hooks` robust; `phase_machine` enforces legal transitions. 💭 Design caveat: `mounts.chroot_into` documented *not* security isolation (L31-33) — uid-0 INSTALL scenario + user installer → chroot escape = root.
- Standard bumped to **v1.20**: header → +`initrd/`; §4.4 added `initrd/` study bullet; §9 added **H91–H97**; §11 quick-ref added `initrd/` study line. Total hotspots now H1–H97.

## installer/ folder study (v1.21 of the standard)
- User directed study of `installer/` (3 `.py` files, **~22 KB total**) — the **deployment/install surface**: `installer.py` (17.9 KB — real `UmerInstaller` interactive wizard + `InstallLogger`), `install.py` (3.9 KB — a *second*, non-functional `UmerInstaller` stub whose `install()` only prints "Real installation would happen here"), `__init__.py` (re-exports `install.py`).
- **Injection-safe at Python level:** no `subprocess`/`shell=True`/`eval`/`exec`/`pickle`/`ctypes`/`os.system`/`yaml.load` anywhere (good). `installer.py` is a **baseline model** — `from __future__`, `logging.getLogger`, Google docstrings, `InstallLogger` with UTC timestamps + OSError-guarded `flush`, EULA gate, `check_prerequisites` Python-version check, `os.walk` skips hidden dirs, `show_eula` **fails CLOSED** in non-interactive (`answer=""` → `False`), `run()` catches OSError and calls `rollback()` on copy/bootloader failure.
- **🔴 H98:** two divergent `UmerInstaller` classes — `__init__.py` re-exports the dead `install.py` stub, so `from installer import UmerInstaller` gets the non-functional class while `installer.py` holds the real one (same dup anti-pattern as H38/H106).
- **🔴 H99:** `install.py:display_waiver` **fails OPEN** — auto-accepts the "I AGREE" liability waiver in `dry_run` (the *default* `dry_run=True`) AND `return True` when stdin is non-TTY (`else: return True`) → waiver bypassed non-interactively (same family as H29/H17/H27/H28/H46/H51/H64; breaks `installer.py`'s own "non-negotiable" EULA rule #2).
- **🔴 H101:** `installer.py:rollback` `shutil.rmtree(self._install_root)` with **no path-guard/scope validation**, auto-invoked on copy/bootloader failure — data-loss risk if `UMER_INSTALL_ROOT` is misconfigured or pointed at a non-UmerOS dir; no `/`/system-dir check (🔴 data-loss blocker).
- **🟡 H100:** `run(consent_override=True)` unguarded programmatic EULA bypass (public param, no `CapabilityManager`). **🟡 H102:** no `CapabilityManager` gate on privileged install ops (writes `/opt/umer_os`,`/opt/umer_backup`,`/var/log`, FUTURE bootloader — H27…H92 family). **🟡 H103:** `copy_os_files` `dst_path = dst/rel_path` with no `_safe_join`/`..` check + copies hidden files. **🟡 H104:** `installer.py` Apache-2.0 (H7), `install.py`/`__init__.py` no header. **💭 H105:** `install.py` off-baseline. **🟡 H106:** `install.py` dead stub. **💭 H107:** lazy stdlib imports. **🟡 H108:** no source-tree integrity/signature check before install. **🟡 H109:** EULA over-promises pre-install backup + rollback-restore that TODAY code doesn't do (`backup_bootloader` = JSON metadata only; `rollback` = rmtree install root only).
- **Fix order:** delete/merge `install.py` (H98/H106) → fail-CLOSED waiver (H99/H100) → guard `rollback` (H101) → `_safe_join` (H103) → capability gate (H102) → license/integrity sweep (H104/H108).
- Standard bumped to **v1.21**: header → +`installer/`; §4.4 added `installer/` study bullet; §9 added **H98–H109**; §11 quick-ref added `installer/` study line. Total hotspots now H1–H109.

## kernel/ folder study (v1.22 of the standard)
- User directed study of `kernel/` (40 files, **~10,131 LOC**) — the **microkernel + supporting subsystems** (scheduler/pid/signals/taint/sysctl/memory/resource/softirq/reboot/audit/workqueue/panic + IPC bus + capability manager + credentials + crypto engine + VFS shell commands + udev-style device subsystem + AI/quantum/hal/power stubs). Centerpiece `umer_kernel.py` (87 KB) is the `UmerKernel` runtime (SYSTEM_PID=0 omnipotent).
- **Two quality tiers inside `kernel/`:** a STRONG baseline cluster (real modules, `from __future__`+`logging`+locks+docstrings: `scheduler.py`/`panic.py`/`pid_allocator.py`/`signals.py`/`taint.py`/`sysctl.py`/`memory_manager.py`/`resource.py`/`softirq.py`/`reboot.py`/`audit.py`/`workqueue.py` + `modules/dev/*` = `device_manager.py`/`udev_rules.py`/`hotplug.py`/`demo_udev.py`) and an OFF-BASELINE toy/REPL cluster (`container_engine.py`/`installer.py`/`umer_ai.py`/`gui.py`/`hal.py`/`cpuidle.py`/`power_manager.py`/`qfs.py`/`quantum_sim.py`/`pqcrypto_.py`/`onnxruntime_.py`/`setup_umer_os.py`/`bootloader.py` — all `print`/`input` REPLs, no `from __future__`/logging/license/tier; `pqcrypto_.py` runs `kyber1024.generate_keypair()` at import time).
- **🔴 H110 (HEADLINE):** live `UmerKernel.__init__` (`umer_kernel.py:674-676`) wires **no-op placeholder** `MemoryManager`/`IPCBus`/`CapabilityManager` (`type(...)` lambdas returning dummy data) — the correctly-built real modules exist but are **commented out** at L1569-1571. So zero-trust capability gating, HMAC-signed IPC, and real memory accounting are all **INERT**. Most important occurrence yet of the fail-open/cap-gating family.
- **🔴 H111:** `umer_kernel.py:429-434` `CryptoEngine.verify` returns `True` (dummy) and `sign` returns `b"dummy_signature"` — every code-signing/integrity check using it passes trivially.
- **🔴 H112:** `security.py` `SecuritySandbox.register_process` only `print`s a "sandboxed" line and never applies any restriction — decorative sandbox (same family as H51).
- **🟡 H113:** `shell_commands.py` `SudoCommand`/`SuCommand` set `ctx.shell.current_user = "root"/user` with **no auth or capability check** (simulated VFS, bounded blast radius). **🟡 H114:** `KillCommand`/`ShutdownCommand` call `scheduler.terminate`/`request_shutdown` with no cap gate. **🟡 H115:** scheduler `exit_task` docstring claims "revoke capabilities" but holds no `CapabilityManager` → capability lifecycle unwired.
- **🟡 H116:** `umer_kernel.py:1040` launches GUI via `subprocess.Popen([sys.executable, launcher_script_path])` where `launcher_script_path` = retired Tkinter `ui/launch_gui.py` (contradicts Flutter H11/H25). **🟡 H117:** `ipc_bus.try_receive` skips HMAC verify + no cap gate (live path; cf. HMAC in `ipc_bus.py` used only by `receive`). **🟡 H118:** `cred.is_root()` (euid==0) bypasses capability checks → root always omnipotent. **🟡 H119:** cgroup ops implicit-allow.
- **💭 H120:** `umer_kernel.py:668-669` hard-depends on `umer_kernel1.py` (`AIResourceManager`, `QuantumScheduler`) — unverified second module. **💭 H121:** `bootloader.py` triple-duplicated header (L1-5/L7-16/L12-15) + `input("Do you agree...")` EULA. **💭 H122:** `gui.py` Kivy prototype (Kivy drift, H11/H25). **💭 H123:** dead REPLs (`container_engine.py`/`installer.py`/`umer_ai.py`). **💭 H124:** `pqcrypto_.py` import-time `kyber1024.generate_keypair()`. **💭 H125:** `requirements.txt` lists `kivy` (not Flutter) + `cryptography` (unused by dummy CryptoEngine). **💭 H126:** `modules/dev/udev_rules.py` advertises `RUN` action but `apply_actions` doesn't implement it (no injection vector, good) + `DeviceNode.mode` defaults `0o666`. **🟡 H127:** `umer_kernel.py:808` `panic()` calls `self.taint.add("TAINT_KERNEL_PANIC")` — string NOT in `KernelTaint._FLAG_INDEX` → `add()` raises `ValueError`, panic path crashes on itself (latent, not fail-open).
- **Positives:** `capability_manager.py` real `check()` raises `PermissionError` + `SYSTEM_PID=0`; `ipc_bus.py` HMAC-SHA256 `compare_digest`; `modules/dev/*` udev model is clean (no injection). `chmod`/`chown` are no-op prints; all shell ops run on simulated `ctx.kernel.vfs` (bounded blast radius).
- **File/LOC note:** `umer_kernel.py` 87 KB (~1900+ lines) + `shell_commands.py` 82 KB (~120 commands) dominate; the companion `umer_kernel_findings.md` doc is STALE (claims 1536 lines / L1-1058 active) — real split is L674-676 placeholders + L1569-1571 commented correct wiring.
- Standard bumped to **v1.22**: header → +`kernel/`; §4.4 added `kernel/` study bullet; §9 added **H110–H127**; §11 quick-ref added `kernel/` study line. Total hotspots now H1–H127.
