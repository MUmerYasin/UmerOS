/// UmerOS Flutter UI — Animations barrel
/// =====================================
/// Public re-exports of every animation primitive in the
/// `lib/src/animations/` directory.  Import this single file to get
/// the entire motion design system:
///
/// ```dart
/// import 'package:umer_ui/src/animations/animations.dart';
/// ```
///
/// Modules:
///   * `animation_tokens.dart`   — durations, curves, motion intents
///   * `widget_animations.dart`  — fade-in, slide-in, scale-in, stagger
///   * `micro_interactions.dart` — hover, press, glow, interactive card
///   * `quantum_animations.dart` — orb, particle field, boot progress
///   * `page_transitions.dart`   — fade / slide / zoom / modal routes
///   * `loading_indicators.dart` — dots, ring, skeleton, pulsing dot
library;

export 'animation_tokens.dart';
export 'widget_animations.dart';
export 'micro_interactions.dart';
export 'quantum_animations.dart';
export 'page_transitions.dart';
export 'loading_indicators.dart';
