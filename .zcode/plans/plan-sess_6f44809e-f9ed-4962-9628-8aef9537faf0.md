
# Plan: Finish Claude → Main Merge

## Summary

The Claude reference tree is **already 95% merged** into main. All subsystem modules are byte-identical. The remaining work is: cherry-pick 3 useful methods from Claude's kernel into main's kernel, fix the test suite and bootloader to match main's API, and delete legacy artifacts.

## Changes (7 files modified, 3 files deleted)

### Step 1: Merge 3 methods into `kernel/umer_kernel.py`

Add `import time` at top. Add `_boot_time` field in `__init__()`. Add three new methods to `UmerKernel`:
- `uptime()` → returns `time.monotonic() - self._boot_time`
- `status()` → returns dict with uptime, process_count (from scheduler), memory stats, running flag
- `shutdown()` → stops scheduler, sets running=False

These are self-contained health-monitoring methods with zero coupling to Claude's process-management design.

### Step 2: Fix `boot/bootloader.py` (6 line changes)

The Claude bootloader calls `UmerKernel(total_memory_bytes=..., quantum_simulator=...)` and `kernel.uptime()` / `kernel.status()`. After Step 1, `uptime()` and `status()` will work, but the constructor args won't. Fix:
- Change `load_kernel()` to call `UmerKernel()` (no args, matching main's signature)
- Keep `kernel.uptime()` and `kernel.status()` calls (they'll work after Step 1)
- Remove `total_memory_bytes` and `quantum_simulator` parameters from `load_kernel()` signature

### Step 3: Fix `tests/test_kernel.py` (TestUmerKernel class only)

The `TestUmerKernel` class (lines 344-394) uses Claude's API: `UmerKernel(total_memory_bytes=...)`, `.init()`, `.shutdown()`, `.uptime()`, `.spawn_process()`, `.kill_process()`, `.list_processes()`, `.status()`, `.inject_ai_manager()`. After Step 1, only `shutdown()`, `uptime()`, and `status()` exist on main's kernel. Rewrite the test class:
- `asyncSetUp`: `UmerKernel()` (no args)
- `asyncTearDown`: call `kernel.shutdown()` (works after Step 1)
- `test_kernel_constructs`: verify scheduler, memory, ipc, capabilities are created
- `test_uptime_increases`: works after Step 1
- `test_status_has_expected_keys`: works after Step 1
- Remove tests for `init()`, `spawn_process()`, `kill_process()`, `list_processes()`, `inject_ai_manager()` — these don't exist on main's kernel

All other test classes (TestTask, TestTaskState, TestHybridScheduler, TestMemoryManager, TestIPCBus, TestCapabilityManager) are unchanged — they test subsystem modules directly, not UmerKernel.

### Step 4: Delete legacy files

- **Delete `kernel/ipc.py`** — pre-Claude legacy IPC (IPCBroker/Message). Replaced by `kernel/ipc_bus.py` (IPCBus/IPCMessage). Nothing imports it after Step 3's test fixes.
- **Delete `tests/test_scheduler_ipc.py`** — completely broken: imports non-existent `Scheduler`, `IPCBroker` (from deleted ipc.py), uses stale API. No equivalent in Claude tree.
- **Delete `Claude/kernel/umer_kernel.py`** — Claude reference kernel. Main kernel is now canonical with cherry-picked methods merged in.

### Step 5: Update stale documentation

- Update `BUGFIXES_AND_IMPROVEMENTS.md` header to note that BUG-001 (scheduler race condition) and several other bugs are already resolved by the Claude merge.
- Update `MainTask/codex_project_context.md` to remove the stale "start() outside class" warning (that bug is long fixed).

## What is NOT changed

- All subsystem modules (scheduler, memory_manager, ipc_bus, capability_manager, quantum_sim, umer_ai, qfs, container_engine, installer, network_stack, umer_pkg, crypto_pqc) — already identical to Claude, no action needed
- `fs/qfs.py` — main is a superset of Claude (has legacy shims), leave as-is
- `boot/init.py` — the working boot entry point (`main.py` → `boot.init.boot()`), no changes needed
- All other test files (test_ai, test_compatibility, test_installer, test_qfs, test_quantum_sim, test_security) — already correct
- The `Claude/` reference tree (except `Claude/kernel/umer_kernel.py` which is deleted) — left as-is per your instruction
- `Claude/{boot,kernel` stray directory — left alone per your instruction
